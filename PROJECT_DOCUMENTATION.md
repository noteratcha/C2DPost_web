# สรุปคู่มือทักษะ เทคนิค สถาปัตยกรรม และสไตล์ (C2DPost Web Architecture & Master Guide)

เอกสารฉบับนี้รวบรวม **ทักษะ เทคนิค โซลูชันทางวิศวกรรม สไตล์ UI/UX และแนวทางการพัฒนาต่อยอด** ของโปรเจกต์ **C2DPost Web** ไว้อย่างละเอียดสมบูรณ์แบบ เพื่อใช้อ้างอิงและพัฒนาต่อในอนาคต

---

## 1. ปัญหาหลักและแนวทางสถาปัตยกรรม (Core Problem & Architecture)

### 🔴 โจทย์ความท้าทาย
1. **API บาร์โค้ดไปรษณีย์ไทย (PostOne) มีข้อจำกัด 2 ประการใหญ่**:
   - **Geo-Blocking**: เซิร์ฟเวอร์ `https://postone.thailandpost.com/api/bc.php` อนุญาตให้เฉพาะ IP Address ภายในประเทศไทยเท่านั้นที่จะส่งคำขอได้ Cloud Serverless ต่างประเทศ (เช่น Vercel สหรัฐฯ, AWS, DigitalOcean) จะถูกปฏิเสธ (HTTP 403 / Timeout)
   - **No CORS Header**: เซิร์ฟเวอร์ไม่มีการส่ง `Access-Control-Allow-Origin` ทำให้หน้าเว็บ Browser แบบ SPA ทั่วไปไม่สามารถเรียก API ผ่าน JavaScript `fetch()` ได้โดยตรง

### 🟢 ทางออก: สถาปัตยกรรมไฮบริด (Web App + Chrome Extension Bridge)
- **Frontend & Python Serverless (บน Vercel)**:
  - ทำหน้าที่เป็นศูนย์กลางของระบบ (เว็บใช้งาน, UI แดชบอร์ด, อัปโหลด PDF)
  - ประมวลผลเอกสาร PDF ด้วย Python Serverless (`pdfplumber`)
  - สร้างไฟล์ Excel DPost (`openpyxl`) และเอกสารพิมพ์พร้อมบาร์โค้ด Code128 (`reportlab`)
- **Chrome Extension Helper (Manifest V3)**:
  - ทำหน้าที่เป็น **API Proxy ภายในเครื่องของผู้ใช้ในไทย**
  - รับคำขอจำนวนและประเภทบาร์โค้ดจากหน้าเว็บผ่าน `window.postMessage`
  - ทำการ `fetch()` ไปยัง PostOne ด้วย IP ประเทศไทยของผู้ใช้เอง (โดยไม่มีปัญหาเรื่อง CORS เพราะมีสิทธิ์ `host_permissions`)
  - ส่งรายการหมายเลขบาร์โค้ด 13 หลักกลับเข้าสู่หน้าเว็บแบบเรียลไทม์

---

## 2. โครงสร้างและเทคโนโลยีที่ใช้ (Tech Stack & Structure)

```
C2DPost_web/
├── api/                          # Python Serverless API บน Vercel
│   ├── index.py                  # API Handler (/api/convert, /api/health)
│   ├── requirements.txt          # pdfplumber, reportlab, openpyxl, flask
│   ├── fonts/
│   │   └── tahoma.ttf            # ฟอนต์ไทย Tahoma สำหรับ ReportLab บน Linux
│   └── core/
│       ├── convert_dpost.py      # เอนจินสกัด PDF คำขอ สนง.ที่ดิน & สร้าง PDF/Excel
│       └── template.xlsx         # ไฟล์ต้นแบบ Excel DPost นำเข้า PostOne
├── extension/                    # Chrome Extension Helper (Manifest V3)
│   ├── manifest.json             # ประกาศ host_permissions & externally_connectable
│   ├── background.js             # Service Worker ยิง API PostOne
│   ├── content.js                # ตัวเชื่อมโยง DOM Handshake & postMessage
│   ├── popup.html & popup.js     # หน้าต่างทดสอบสถานะการเชื่อมต่อ
│   └── icons/                    # ไอคอนขนาด 16, 32, 48, 128 พิกเซล
├── src/                          # React + Vite Frontend
│   ├── components/
│   │   ├── ExtensionGate.jsx     # ตัวกักหน้าจอ ตรวจสอบการติดตั้ง Extension อัตโนมัติ
│   │   ├── LoginModal.jsx        # ระบบล็อกอินตรวจสอบสิทธิ์ผ่าน Google Sheets CSV
│   │   ├── Navbar.jsx            # แถบด้านบน พร้อมไฟสถานะ Extension แบบสด
│   │   ├── DropZone.jsx          # โซนลากวางไฟล์ PDF (Drag & Drop)
│   │   ├── PreviewGrid.jsx       # ตารางข้อมูลที่สกัดได้ พร้อมช่องแก้ไข/ลบ
│   │   └── ActionToolbar.jsx     # ปุ่มดึงบาร์โค้ด & ปุ่มส่งออกไฟล์ (Excel, PDF, ZIP)
│   ├── utils/
│   │   ├── api.js                # ฟังก์ชันยิง API ฝั่ง Vercel
│   │   └── parseCsv.js           # ตัวแยกข้อมูล CSV สำหรับระบบล็อกอิน
│   └── App.jsx                   # Component หลักรวมการทำงานทั้งหมด
└── vercel.json                   # ตั้งค่า Build & Routing บน Vercel
```

---

## 3. สไตล์และดีไซน์ UI/UX (Design System & Theme Switcher)

1. **ระบบ 2 ธีม (Dual Theme System: Light / Dark Mode)**:
   - **Light Mode (โหมดสว่าง - ค่าเริ่มต้น Default)**: ใช้โทนขาวคลีนสะอาดตา คอนทราสต์สูง (`#ffffff`, พื้นหลัง `#f8fafc`, ตัวหนังสือเข้มคมชัด `#0f172a`) เหมาะกับการทำงานเอกสารในเวลากลางวัน เป็นค่าเริ่มต้นสำหรับผู้ใช้งานใหม่ทุกคน พร้อม inline script ป้องกันการกระพริบแสง (Zero-FOUC)
   - **Dark Mode (โหมดมืด)**: ใช้โทน Slate Dark Glassmorphism (`#0b1120` ถึง `#0f172a`) แสงเงาสบายตา เหมาะกับการทำงานในที่แสงน้อยและลดความล้าของสายตา
   - **การจำค่าถาวร (Persistence)**: บันทึกโหมดที่ผู้ใช้เลือกลงใน `localStorage.getItem('c2dpost_theme')` และกำหนดค่า Attribute บน `<html>` อัตโนมัติ ทำให้เมื่อรีเฟรชหน้าเว็บแล้วธีมไม่รีเซ็ต
2. **Typography**:
   - หัวข้อและแบรนด์: ใช้ฟอนต์ **Prompt** (จาก Google Fonts) เพื่อความทันสมัย ชัดเจน และเป็นทางการ
   - ตัวเลขและตารางข้อมูล: ใช้ฟอนต์ **Inter** เพื่อความแม่นยำในการแสดงผลข้อมูลตัวเลขและรหัสไปรษณีย์
3. **Micro-Interactions**:
   - ปุ่มสลับธีม (`btn-theme-toggle`): บนแถบ Navbar มีไอคอน ☀️ ดวงอาทิตย์ / 🌙 พระจันทร์ พร้อมแอนิเมชันหมุนนุ่มนวล
   - ไฟสถานะ Extension (`ext-status-pill`): มีจุดกระพริบเขียวสดใสเมื่อ Extension เชื่อมต่อ และเปลี่ยนเป็นสีแดงทันทีหากยังไม่ได้ติดตั้ง
   - ตารางข้อมูลแบบ Inline Edit: ผู้ใช้สามารถแก้ไขหมายเลขบาร์โค้ด ชื่อ หรือที่อยู่ได้โดยตรงในตารางก่อนสั่งพิมพ์

---

## 4. เทคนิควิศวกรรมสำคัญ (Key Engineering Techniques)

### 1) การตรวจจับ Extension โดยไม่ต้องรีเฟรชหน้าเว็บ (Extension Gating)
- เมื่อหน้าเว็บโหลด `ExtensionGate.jsx` จะเช็ก Attribute `data-c2dpost-extension` บนแท็ก `<html>`
- Content Script จะรันที่ `run_at: document_start` และส่งสัญญาณ `postMessage` ยืนยันการมีอยู่ของ Extension ทันที
- มีระบบ Polling ส่ง Ping ทุก 1 วินาที เพื่อตรวจจับกรณีที่ผู้ใช้เพิ่งกดติดตั้ง Extension เสร็จ หน้าเว็บจะปลดล็อกเข้าสู่แดชบอร์ดอัตโนมัติทันที

### 2) การแก้ปัญหาฟอนต์ไทยบน Linux Serverless (ReportLab on Vercel)
- Vercel Serverless รันบนระบบปฏิบัติการ Linux ที่ไม่มีฟอนต์ภาษาไทยของ Windows (`C:/Windows/Fonts/tahoma.ttf`)
- **เทคนิค**: ฝังไฟล์ `tahoma.ttf` ไว้ในโฟลเดอร์ `api/fonts/` ภายในโปรเจกต์ และเขียนโค้ดค้นหาตามลำดับไดเรกทอรี ทำให้ระบบสามารถออกเอกสารใบนำส่งและซองจดหมายที่มีภาษาไทยถูกต้อง 100% ทั้งบน Local และ Production

### 3) ฐานข้อมูลสมาชิกแบบไม่ต้องใช้ Database Server (Google Sheets CSV Backend)
- ใช้ Google Spreadsheet เผยแพร่เป็นสาธารณะในรูปแบบ CSV:
  `https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/export?format=csv&gid=0`
- ทำให้แอดมินสามารถเพิ่ม/ลด/แก้ไขรายชื่อผู้มีสิทธิ์ใช้งาน สนง.ที่ดิน รหัสไปรษณีย์ และประเภทบาร์โค้ดผ่าน Google Sheet ได้โดยตรงแบบ Zero-Maintenance

### 4) เทคนิคการบันทึกภาพหน้าจอจริงสำหรับ Store (CDP Scaling & Profile Sanitization)
- **โจทย์ความละเอียด**: Chrome Web Store กำหนดขนาดภาพหน้าจอเคร่งครัดที่ `1280 x 800` พิกเซล แต่หน้าจอ Web App ที่มีทั้ง Header, Dropzone, แถบเครื่องมือคำสั่ง และตารางข้อมูลขนาดยาว จะถูกครอปส่วนล่างขาดหายไป
- **เทคนิคการย่อสัดส่วนผ่าน CDP**: ใช้ Node.js + Chrome DevTools Protocol สั่งรัน JavaScript `document.body.style.zoom = '0.72';` ก่อนสั่ง `Page.captureScreenshot` ทำให้ทั้ง แดชบอร์ด, ปุ่มคำสั่ง, และตารางข้อมูลผู้รับพร้อมหมายเลขบาร์โค้ด พอดีกับกรอบ 1280x800 อย่างสวยงามครบถ้วน
- **การตกแต่งข้อมูลให้เป็นทางการ (Profile Sanitization)**: แทนที่จะใช้ข้อมูล mock เช่น `admin / ADMIN` ได้ปรับระบบเดโมให้แสดงโปรไฟล์ผู้ใช้จริงระดับองค์กร เช่น `สนง.ที่ดินเรณูนคร` และตำแหน่ง `เจ้าหน้าที่` เพื่อความน่าเชื่อถือระดับมืออาชีพบน Store
- **ฟอร์แมตสี 24-bit RGB**: ใช้ PIL (Pillow) แปลงโหมดภาพเป็น RGB ตัด Alpha Channel (RGBA) ออก 100% ตามข้อกำหนดของ Store

### 5) เทคนิคการผ่านการตรวจสอบ Chrome Web Store (Submission & Compliance Checklist)
- **การตั้งค่าความปลอดภัยและนโยบาย**:
  - **Match Pattern ใน manifest.json**: ห้ามระบุ Port หรือ Wildcard Port ในส่วน `externally_connectable` (เช่น `http://localhost:*` จะถูก Store ปฏิเสธทันที) ต้องใช้โดเมนจริง เช่น `https://*.vercel.app/*`
  - **การห้ามใช้รีโมตโค้ด (Remote Code Prohibition)**: ในข้อกำหนด Manifest V3 ห้ามมีการโหลดไฟล์ JavaScript หรือสคริปต์ภายนอกมารันเด็ดขาด ในหน้าตั้งค่า Privacy ต้องเลือก **"ไม่ ฉันไม่ได้ใช้รีโมตโค้ด อยู่"**
  - **การยืนยันอีเมลผู้เผยแพร่ (Publisher Contact Email)**: Google จะบล็อกการส่งตรวจหากยังไม่ได้ยืนยันอีเมลในหน้า Developer Settings ต้องใส่อีเมลและกดลิงก์ยืนยันในกล่องข้อความ Gmail ก่อนเสมอ
  - **นโยบายความเป็นส่วนตัว (Privacy Policy)**: สร้างหน้าเว็บ HTML ทางการบรรจุไว้ใน `C2DPost_web/public/privacy.html` และขึ้นระบบจริงที่ `https://c2dpost-web.vercel.app/privacy.html` ระบุขอบเขตการใช้งานสิทธิ์ `storage` และ `host_permissions` อย่างโปร่งใส
  - **Trader Status**: เลือกเป็น **ไม่ใช่ผู้ค้า (Non-trader)**
  - **เนื้อหาสำหรับผู้ใหญ่ (Mature Content)**: ต้องปิดเป็น **OFF (สีเทา)**
  - **การเปิดเผย (Visibility)**: เลือกเป็น **สาธารณะ (Public)** ทั่วโลก (`ทุกภูมิภาค`) เพื่อให้ผู้ใช้จากทุกที่สามารถค้นหาและติดตั้งได้ทันที

---

## 5. ข้อมูลการเผยแพร่และรหัสรายการ (Store Deployment Details)

- **ชื่อส่วนขยาย**: C2DPost Helper - Thailand Post API Bridge
- **รหัสส่วนขยาย (Item ID)**: `cdkmibacceaacdiopcekkmfifaocapgk`
- **สถานะปัจจุบัน**: รอการตรวจสอบ (Pending Review)
- **ลิงก์หน้า Chrome Web Store อย่างเป็นทางการ (เมื่อผ่านการอนุมัติ)**:
  `https://chromewebstore.google.com/detail/cdkmibacceaacdiopcekkmfifaocapgk`
- **หน้านโยบายความเป็นส่วนตัว**:
  `https://c2dpost-web.vercel.app/privacy.html`

---

## 6. แนวทางการเชื่อมโยงหลังได้รับการอนุมัติ (Post-Approval Handover)

เมื่อทาง Google อนุมัติส่วนขยายเรียบร้อยแล้ว:
1. นำลิงก์หน้า Chrome Web Store ไปอัปเดตในไฟล์ `C2DPost_web/src/components/ExtensionGate.jsx`
2. ปรับปุ่ม "ดาวน์โหลดส่วนขยาย" ให้เปลี่ยนเป็น "ติดตั้งจาก Chrome Web Store" (คลิกเดียวติดตั้งลง Chrome ได้ทันที)
3. รันคำสั่ง `vercel --prod --yes` เพื่ออัปเดตหน้าเว็บหลัก

---

## 7. คำสั่งปฏิบัติการที่ใช้บ่อย (Useful Commands)

```bash
# 1. Build และ Deploy เว็บไซต์และ Python API ขึ้น Vercel
cd C2DPost_web
vercel --prod --yes

# 2. บีบอัด Extension เตรียมส่ง Chrome Web Store
Compress-Archive -Path "C2DPost_web\extension\*" -DestinationPath "C2DPost_web\public\c2dpost-extension.zip" -Force

# 3. บันทึกภาพหน้าจอจริงของระบบขนาด 1280x800 RGB แบบอัตโนมัติ
node C2DPost_web/capture_screenshot.cjs
```

---

## 8. ระบบเข้าสู่ระบบและการลงทะเบียนขอสิทธิ์ (Login & Registration Parity)

1. **หน้าต่าง Login (Login Screen)**:
   - **หัวข้อหลัก**: `C2DPost Login` โทนสีเขียว `#059669` พร้อมไฟสถานะ API คู่มุมขวาบน
   - **ลงทะเบียนขอสิทธิ์การใช้งาน**: ลิงก์เปิดแบบฟอร์มลงทะเบียนแบบ Progressive Unlocking ถอดแบบจาก Python Desktop (`RegistrationWindow`)
   - **คู่มือการใช้งาน**: ลิงก์ตรงสู่สื่อนำเสนอคู่มือการใช้งานบน Canva:
     `https://canva.link/dyl3brb47lyph8r`
   - **ติดต่อเจ้าหน้าที่**: ลิงก์ติดต่อเจ้าหน้าที่ส่วน ทข.ปข.10 ผ่าน LINE Official:
     `https://lin.ee/UzWqlKP`
   - **เลขเวอร์ชันเว็บ**: แสดง `v2026.0914.2046 (เวอร์ชันล่าสุด)` ด้านล่างการ์ด เชื่อมต่อโฟลเดอร์ Google Drive รวมตัวติดตั้ง:
     `https://drive.google.com/drive/folders/1ksrVAQVwkHDBE7qiqLT2ndyPHPQhz6vZ`

2. **ระบบลงทะเบียนขอสิทธิ์การใช้งาน (Registration Window Flow)**:
   - **ข้อความและโครงสร้าง 4 ส่วนถอดแบบจาก Python 100%**:
     - **ส่วนที่ 1 (ข้อมูลการเข้าสู่ระบบ)**:
       - Username & Password (ตัวอักษรสีส้มทอง `#d97706`)
       - ตรวจสอบความถูกต้อง Regex `^[a-zA-Z0-9.@$%&!<>?*/\\[\\]{}#_\\-=+]*$` หากพิมพ์ภาษาไทยจะขึ้นขอบแดงและแจ้งเตือน `❌ รองรับเฉพาะภาษาอังกฤษ ตัวเลข และสัญลักษณ์`
       - ปุ่ม `ตรวจสอบ` ยิงทดสอบไปยัง Web Service ของไปรษณีย์ไทยผ่าน `/api/verify_user` (`https://r_dservice.thailandpost.com/webservice/addItems`)
       - เมื่อผ่าน: ปุ่มเปลี่ยนเป็นสีเขียว, ขึ้นข้อความ `✅ ตรวจสอบ Username และ Password ถูกต้อง`, ล็อกช่อง Username/Password ไม่ให้แก้ไข และ**ปลดล็อกส่วนที่ 2**
     - **ส่วนที่ 2 (ข้อมูลหน่วยงาน)**:
       - ชื่อสำนักงาน/หน่วยงาน แบบเต็ม
       - Email ตรวจสอบรูปแบบ Regex
       - รหัสไปรษณีย์ (5 หลัก) ค้นหาชื่อที่ทำการอัตโนมัติจาก Google Sheets CSV:
         `https://docs.google.com/spreadsheets/d/12tt2MBVqBRMzoqfCjskt_Aft-SUGVMs7uH1Endp2cQI/export?format=csv`
       - เมื่อพบที่ทำการ (เช่น `ปณ.เรณูนคร`): แสดงชื่อที่ทำการในช่อง Readonly และ**ปลดล็อกส่วนที่ 3 และ 4**
     - **ส่วนที่ 3 (ข้อมูลผู้ประสานงาน)**:
       - ผู้ประสานงานหลัก (บังคับ) + เบอร์โทรศัพท์ 10 หลัก
       - ปุ่ม `+ เพิ่มผู้ประสานงาน` สามารถเพิ่มคนที่ 2 และคนที่ 3 ได้แบบ Dynamic (และซ่อนปุ่มเมื่อครบ 3 คน)
     - **ส่วนที่ 4 (ข้อตกลงและเงื่อนไข PDPA)**:
       - ข้อความเงื่อนไขทางกฎหมาย 5 ข้อเต็มรูปแบบตรงตามกฎหมาย PDPA และ พ.ร.บ.คอมพิวเตอร์
       - เช็คบ็อกซ์ยินยอม เมื่อติ๊กเลือกจะปลดล็อกปุ่ม `[ลงทะเบียน]`
   - **การส่งข้อมูลและจัดการสถานะ (Submission & Duplication)**:
     - ส่งข้อมูลผ่าน `/api/register_user` ไปยัง Google Apps Script:
       `https://script.google.com/macros/s/AKfycbwulS3437Gqf8tM_5pjYQhPfcSqcUNwM-PoKxjzw4cWL5FRCszE7VFDUKFuHEGYQg/exec`
     - ตรวจสอบ Username ซ้ำ: หากเคยลงทะเบียนแล้ว จะขึ้นเตือน `❌ Username นี้ มีการลงทะเบียนแล้ว` พร้อมปุ่ม `ลองใหม่อีกครั้ง`
      - หากลงทะเบียนสำเร็จ: ปุ่มเปลี่ยนเป็น `ส่งข้อมูลเสร็จสิ้น`, ล้างข้อมูลในฟอร์ม และนับถอยหลัง 10 วินาที `(ปิดหน้าต่างอัตโนมัติใน {count} วินาที)` ก่อนปิดหน้าต่างลงทะเบียนอัตโนมัติ

---

## 9. การบันทึกไฟล์ 3 ไฟล์แยกกันเทียบเท่าโปรแกรม Python (Exporting 3 Individual Files Parity)

- **การเปลี่ยนแปลงสำคัญ**: เดิมหน้าเว็บทำการรวบไฟล์ทั้งหมดเป็นไฟล์บีบอัด `.zip` เพียงไฟล์เดียว แต่ผู้ใช้ต้องการให้อ้างอิงตามโปรแกรม Python Desktop คือการได้รับไฟล์ 3 ไฟล์แยกกันทันที โดยไม่ต้องแตกไฟล์ ZIP
- **รายการไฟล์ที่ได้รับ**:
  1. 📊 **ไฟล์ Excel** (สำหรับ DPost นำเข้า PostOne) — `DPost_Export_<timestamp>.xlsx`
  2. 📄 **ไฟล์ PDF รวม** (เอกสารคำขอที่ดินพร้อมประทับตราสติกเกอร์บาร์โค้ดและ e-AR) — `DPost_Combined_<timestamp>.pdf`
  3. 📄 **ไฟล์ PDF ใบนำส่ง** (สำหรับยื่นที่ทำการไปรษณีย์) — `DeliveryNote_<timestamp>.pdf`
- **เทคนิคการดาวน์โหลดต่อเนื่องโดยไม่ถูกบล็อก (Sequential Controlled Throttling)**:
  - เบราว์เซอร์ส่วนใหญ่จะบล็อกคำสั่งดาวน์โหลดไฟล์หลายไฟล์พร้อมกันในคลิกเดียว (มองว่าเป็น Pop-up spam)
  - **วิธีแก้**: ในฟังก์ชัน `exportAllFiles` ใช้ `await new Promise(r => setTimeout(r, 500))` หน่วงเวลา 500ms ระหว่างไฟล์แต่ละประเภท ทำให้เบราว์เซอร์ดาวน์โหลดไฟล์ต่อเนื่องได้ราบรื่น 100% โดยไม่ติดบล็อก
  - แสดงความคืบหน้าแบบสด (Live Progress Status): `กำลังสร้างไฟล์ Excel...` ➔ `กำลังสร้างไฟล์ PDF (เอกสารพร้อมบาร์โค้ด)...` ➔ `กำลังสร้างไฟล์ PDF (ใบนำส่ง)...`
  - กล่องแจ้งเตือนความสำเร็จ (Custom Success Dialog): แสดงรายการไฟล์ 3 ชนิดที่ได้รับตรงตาม Python

---

## 10. การแก้ปัญหาและเทคนิค Serverless File Streaming สำหรับประทับตราบาร์โค้ดลงบน PDF

### 🔴 ปัญหาเชิงลึก (The 311-Byte Damaged PDF Problem)
- **พฤติกรรมบน Python Desktop**: ไฟล์ PDF ต้นฉบับอยู่ในไดรฟ์เครื่องผู้ใช้ ฟังก์ชัน `generate_combined_pdf` สามารถเปิดไฟล์จาก Path จริง (`C:\...`) ได้ตลอดเวลาเพื่อค้นหาคำว่า `"เรียน"` และนำสติกเกอร์บาร์โค้ดไปแปะทับ (Merge Page)
- **พฤติกรรมบน Cloud Serverless**: บน Vercel ฟังก์ชันเป็นแบบ **Stateless** ไฟล์ที่ถูกส่งขึ้นมาแปลงในรอบแรกจะถูกลบออกจาก Temp Directory ทันทีเมื่อตอบกลับ JSON
- เมื่อผู้ใช้กด "บันทึกไฟล์" โค้ดเดิมส่งไปเฉพาะ JSON ตาราง ทำให้เซิร์ฟเวอร์หาไฟล์ต้นฉบับไม่พบ (`os.path.exists == False`) ส่งผลให้ไม่มีหน้าเอกสารถูกเพิ่มลงในไฟล์ PDF เลย (`0 หน้า`) และ PyPDF สร้างไฟล์ขนาด **311 bytes** ซึ่งเปิดไม่ได้และขึ้นว่าไฟล์ชำรุดเสียหาย

### 🟢 สถาปัตยกรรมแก้ไขแบบไร้ช่องโหว่ (Multipart Form Streaming Architecture)
1. **Frontend**: เก็บ State อ็อบเจกต์ `File` ต้นฉบับไว้ในตัวแปร `selectedFiles`
2. **Streaming via FormData**: เมื่อส่งออก PDF ประเภท `combined` ฟังก์ชัน `exportPdf` จะแพ็กอ็อบเจกต์ `File` ทั้งหมดส่งผ่าน `FormData` ไปยัง Endpoint `/api/export-pdf`
3. **Backend Dynamic Remapping (`api/index.py`)**:
   - เซิร์ฟเวอร์อ่านไฟล์ PDF ต้นฉบับและเขียนลงใน `tempfile.TemporaryDirectory()` ชั่วคราว
   - สแกนและแมปชื่อไฟล์ `SOURCE_FILE` ของแต่ละแถวเข้ากับ Path ของไฟล์ต้นฉบับใน Temp Directory
   - ส่งต่อ DataFrame ที่มี Path ไฟล์จริงให้ `generate_combined_pdf`
4. **Zero-Byte PDF Guard (`convert_dpost.py`)**:
   - เพิ่มคำสั่งตรวจสอบ: `if len(writer.pages) == 0: raise ValueError(...)` เพื่อการันตีว่าจะไม่มีการปล่อยไฟล์ชำรุด 311 bytes ออกไปสู่ผู้ใช้เด็ดขาด
5. **ผลลัพธ์**: ไฟล์ `DPost_Combined.pdf` สร้างหน้าเอกสารจริงพร้อมประทับตราบาร์โค้ด Code128 และตรา e-AR ได้ครบถ้วนทุกหน้า ขนาดไฟล์ปกติ (~110 KB ขึ้นไป) เปิดอ่านและสั่งพิมพ์ได้ 100%

---

## 11. การซิงก์คีย์และสกัดที่อยู่/รหัสไปรษณีย์ให้ตรงกับ Python 100% (Data Normalization)

- **สาเหตุที่ที่อยู่และรหัสไปรษณีย์ไม่แสดงในบางกรณี**:
  - ฟังก์ชัน `process_pdf()` ของ Python ต้นฉบับ คืนค่าพจนานุกรมที่มีคีย์เว้นวรรค เช่น `'RECEIVER ADDRESS'`, `'RECEIVER ZIPCODE'`
  - แต่ในฟังก์ชัน `records_to_dataframe()` จะแปลงชื่อคีย์เป็นแบบ Underscore (`RECEIVER_ADDRESS`, `RECEIVER_ZIPCODE`)
  - หน้าเว็บตาราง React ค้นหาคีย์ที่มี Underscore หาก API ส่งคีย์แบบเว้นวรรคมา ค่าจะกลายเป็น undefined และแสดงเป็น `-`
- **แนวทางปฏิบัติที่เป็นเลิศ (Best Practice)**:
  - ฝั่ง API `/api/convert`: ส่งผ่าน `records_to_dataframe(all_records)` เสมอเพื่อให้ชื่อคีย์ถูกแปลงเป็นมาตรฐานเดียวกัน
  - ฝั่ง Frontend (`App.jsx`, `PreviewGrid.jsx`): ใช้ Fallback Coalescing 2 ชั้นเสมอ:
    ```javascript
    const address = row.RECEIVER_ADDRESS || row['RECEIVER ADDRESS'] || '';
    const zipcode = row.RECEIVER_ZIPCODE || row['RECEIVER ZIPCODE'] || '';
    ```
  - รองรับเอกสารคำขอรังวัดของกรมที่ดินทุกรูปแบบ: ท.ด. 38 (แบ่งแยกในนามเดิม), ท.ด. 38 ค. (เอกสารแจ้งการปักหลักเขตที่ดิน), ท.ด. 81 (รวมโฉนด), ออกโฉนดที่ดิน

---

## 12. เทคนิคตกแต่งปุ่มเลือกหัวตารางและแถวข้อมูล (Non-Wrapping Checkbox Badges)

- **สาเหตุของปัญหา**: สัญลักษณ์ `[ ✓ ]` และ `[   ]` มีอักขระเว้นวรรค เมื่อคอลัมน์ในตารางถูกบีบขนาด เบราว์เซอร์จะตัดคำขึ้นบรรทัดใหม่ตรงช่องว่าง ทำให้ก้ามปูปิด `]` หล่นไปอยู่อีกบรรทัด (กลายเป็น `[ ✓` บรรทัดบน และ `]` บรรทัดล่าง)
- **แนวทางการแก้ไข**:
  1. แยกโครงสร้างเป็น Checkbox Badge 3 ชิ้นส่วน:
     ```jsx
     <span className="chk-box-badge" title="เลือก/ยกเลิกเลือก">
       <span className="chk-bracket">[</span>
       <span className="chk-mark">{isSelected ? '✓' : '\u00A0'}</span>
       <span className="chk-bracket">]</span>
     </span>
     ```
  2. กำหนด CSS `white-space: nowrap !important;` ทั้งในคลาส `.th-select`, `.col-checkbox` และ `.chk-box-badge`
  3. ตรึงความกว้างของคอลัมน์คงที่ `width: 58px; min-width: 58px;`
  4. ตรึงความกว้างของตัวติ๊กถูก `.chk-mark { width: 14px; text-align: center; }` เพื่อให้ก้ามปูซ้ายและขวาอยู่ตำแหน่งเดิมเสมอ ไม่กระตุกเมื่อสลับสถานะเลือก
  5. ดีไซน์สไตล์ Glassmorphism Emerald Tint พร้อมเอฟเฟกต์ Transition และ Hover เมื่อเอาเมาส์ชี้ รองรับทั้ง Light Mode และ Dark Mode อย่างประณีต

---

## 13. ระบบรายงานการรับฝากและตรวจสอบประวัติพัสดุ (Deposit Report & Tracking Stepper)

1. **รายงานการรับฝากไปรษณีย์ (`DepositReportModal.jsx`)**:
   - เชื่อมต่อ API `POST /api/reports/received` รับ Parameter วันที่ (`DD/MM/YYYY`) เพื่อดึงรายการพัสดุที่ไปรษณีย์ไทยรับฝากเข้าระบบ e-Parcel
   - แสดงการ์ดสถิติรวม: จำนวนชิ้นทั้งหมด, รับฝากสำเร็จ (ชิ้น และ %), ยอดรวมค่าบริการ
   - ตารางรายการพร้อมระบบค้นหาและแท็บกรองสถานะ (`ทั้งหมด`, `🟢 รับฝากแล้ว`)
   - ส่งออกรายงานเป็นไฟล์ **Excel (.xlsx)** พร้อมสูตร SUM ค่าบริการอัตโนมัติ และ **PDF (.pdf)** หัวราชการสำหรับลงนามส่งมอบ
2. **การดูประวัติสถานะพัสดุรายชิ้น (`TrackingTimelineModal.jsx`)**:
   - เปิดจากปุ่มไอคอนนาฬิกาในตาราง (`btn-tool-track`)
   - แสดงไทม์ไลน์สถานะจริง (Stepper) จาก `GET /webservice/getHistoryStatus` เรียงตามลำดับเวลา
   - มีระบบแสดงแบนเนอร์ข้อสังเกต API (`api_notice`) และแบนเนอร์โหมดจำลอง (`is_mock`) อย่างโปร่งใส
3. **ระบบตรวจสอบรับฝากอัตโนมัติ (Auto-Reconcile)**:
   - ปุ่ม "ตรวจสอบรับฝาก" (`btn-check-deposit`) ตรวจสอบรายการในตารางแบบกลุ่ม
   - ไฮไลต์แถวที่รับฝากแล้วด้วยสีเขียวอ่อน (`.reconciled-row`) เพื่อความชัดเจน

---

## 14. ระบบบันทึกประวัติ UseBarcode และอัปเดตสถานะคอลัมน์ E เป็น "yes"

1. **การบันทึกประวัติ UseBarcode อัตโนมัติ**:
   - เมื่อกดขอหมายเลขบาร์โค้ดจาก PostOne สำเร็จ ระบบจะส่งข้อมูลไปบันทึกที่ `UseBarcode.gsheet` (แท็บ `Barcode`) โดยบันทึก Timestamp (GMT+7), UserName, Barcode และ รายละเอียดผู้รับ
   - บันทึกสรุปยอดรวมประเภทบาร์โค้ด (EMS, R, eCo) และประวัติเวลาการใช้งาน (`TimeUse`) ในเบื้องหลังแบบ Asynchronous
2. **การอัปเดตสถานะคอลัมน์ E ("ส่งข้อมูล e-Parcel")**:
   - เมื่อผู้ใช้กดส่งข้อมูลเข้าสู่ e-Parcel Web Service สำเร็จ ระบบจะค้นหาหมายเลขบาร์โค้ดในชีต `UseBarcode` และอัปเดตค่าคอลัมน์ E เป็น `"yes"` โดยอัตโนมัติ
   - เรียกผ่านทั้ง Browser ตรง (`no-cors`) และสำรองผ่าน Backend API Proxy เพื่อความเสถียร 100%

---

## 15. นโยบายการพัฒนาใน C2DPost_web และการอัปเดตทักษะ สกิล และสไตล์อย่างต่อเนื่อง

1. **ขอบเขตการทำงานหลัก (Primary Workspace)**:
   - การพัฒนาฟีเจอร์ ปรับแต่ง และแก้ไขบั๊กทั้งหมดจะดำเนินการภายในโฟลเดอร์ `C2DPost_web` เป็นหลัก
2. **การอัปเดตสกิลและคู่มืออย่างต่อเนื่อง (Continuous Skill & Doc Sync)**:
   - ทุกครั้งที่มีการพัฒนาเทคนิคใหม่ แก้บั๊ก หรือปรับปรุง UI/UX จะต้องอัปเดตทั้ง `.agents/skills/c2dpost-web-workflow/SKILL.md` และ `C2DPost_web/PROJECT_DOCUMENTATION.md` เสมอ
   - อัปเดตเช็กลิสต์ใน `ROADMAP_REPORT_FEATURE.md` ให้สะท้อนสถานะปัจจุบัน
3. **การรักษามาตรฐานสไตล์และระบบ 2 ธีม**:
   - รองรับทั้ง Light Mode (Clean White) และ Dark Mode (Slate Glassmorphism) 100%
   - ใช้ฟอนต์ Google Fonts `Prompt` (หัวข้อ) และ `Inter` (ตาราง/ตัวเลข)
   - ป้องกันการตัดคำสัญลักษณ์ด้วย `white-space: nowrap !important` ในปุ่ม Badge และปุ่มคำสั่ง
4. **นโยบายเลขเวอร์ชัน (Version Policy)**:
   - อัปเดตเลขเวอร์ชันตามรูปแบบ `vYYYY.MMDD.HHMM` ใน `src/config.js`, `package.json`, `AboutModal.jsx`, `LoginModal.jsx` และ `api/core/convert_dpost.py` ทุกครั้งก่อน Deploy

---

## 16. การตรวจรับฝากผ่านปุ่ม "รายงานรับฝาก" และระบบ Auto-Sync สู่ตารางหลัก

1. **จุดศูนย์กลางการตรวจรับฝาก**:
   - การตรวจรับฝากพัสดุใน C2DPost Web ได้รับการรวมศูนย์ไว้ที่ **ปุ่ม "รายงานรับฝาก"** บน ActionToolbar และ Navbar ซึ่งแสดงเด่นด้วยสไตล์ White Glassmorphism
2. **ขั้นตอนการทำงานและ Auto-Sync**:
   - คลิกปุ่ม "รายงานรับฝาก" เพื่อเปิด `DepositReportModal` พร้อมดึงข้อมูลจาก API `POST /api/reports/received` ตามวันที่เลือก
   - ปุ่ม **"ซิงก์ผลรับฝากกับตารางหลัก"** (`handleSyncFromDepositReport`) จะจับคู่หมายเลข Barcode ในรายงานรับฝากกับรายการในตารางหลักทันที
   - แถวที่รับฝากแล้วจะขึ้นไฮไลต์สีเขียวอ่อน `.reconciled-row` และอัปเดตสถานะการรับฝากแบบเรียลไทม์ พร้อมแจ้งเตือนสรุปจำนวนชิ้นที่ซิงก์สำเร็จ
   - รองรับการส่งออกเป็นไฟล์ Excel (`.xlsx`) และ PDF (`.pdf`) หัวราชการพร้อมลงนาม

---

## 17. สถาปัตยกรรม Google Apps Script Web App และการบันทึกคอลัมน์ E ("ส่งข้อมูล e-Parcel")

1. **Script Project ID vs Deployment Web App URL**:
   - Script ID (เช่น `16TAW2fgTXNcu2iwzSqlqNpDngK01WF8w1MGf66z2hIa6sJee63zfMJjp`) เป็นเพียงโค้ดต้นฉบับ
   - การเรียกใช้งานจริงต้องใช้ Deployment URL (`/exec`):
     `https://script.google.com/macros/s/AKfycbyyuEJ3pLdXUidsyYoHv84uspMDf8G93U8Mw1ZYCB9ELpFAPjmpwUxsuarxnklnnQ/exec`
2. **การบันทึกสถานะ "yes" ลงคอลัมน์ E ของ UseBarcode.gsheet**:
   - ค้นหาแถวในชีต `UseBarcode` (แท็บ `Barcode`) โดยอิงตาม Barcode ในคอลัมน์ C แล้วบันทึกค่า `"yes"` ลงในคอลัมน์ E (คอลัมน์ 5)
   - ใช้เทคนิค **Dual Dispatch**: ยิงตรงจาก Browser (`mode: 'no-cors'`) ร่วมกับการส่งผ่าน Python API Backend Proxy เพื่อการันตีความสำเร็จ 100%

---

## 18. เทคนิคการออกแบบ White Glassmorphism และกฎการจัดวาง UI/UX

1. **ปุ่ม White Glassmorphism High-Contrast (`.btn-deposit-report-white`)**:
   - ใน Light Mode ใช้พื้นหลังขาวใส `rgba(255, 255, 255, 0.95)` ผสาน `backdrop-filter: blur(12px)` ขอบเส้นสีเขียวมรกต `border: 1.5px solid #059669` ตัวอักษรสีเขียวเข้ม `#065f46` และเงาละมุน `box-shadow: 0 2px 8px rgba(0,0,0,0.08)`
   - โดดเด่น สะอาดตา ไม่กลืนกับปุ่มคำสั่งสีอื่นใน Toolbar
2. **กฎป้องกันคำตัดบรรทัดเด็ดขาด (`white-space: nowrap !important;`)**:
   - กำหนดใน Badge สถานะ ปุ่มคำสั่ง และแท็บกรองทุกจุด ป้องกันคำภาษาไทยหรือสัญลักษณ์แตกบรรทัด
3. **การเน้นแถวรับฝากสำเร็จ (`.reconciled-row`)**:
   - แสดงสีเขียวมรกตโปร่งแสง พร้อมแถบสีด้านซ้าย `border-left: 3px solid #10b981` ชัดเจนทั้งใน Light Mode และ Dark Mode

---

## 20. ระบบจัดกลุ่มและแสดงสถานะนำจ่าย: นำจ่ายสำเร็จ, ส่งคืน, และอยู่ระหว่างการนำจ่าย (v2026.0914.2135)

1. **เกณฑ์การจัดกลุ่มสถานะ (Canonical Delivery Status Classification)**:
   - **นำจ่ายสำเร็จ (Delivered)**: ตรวจจับจากคีย์เวิร์ด `นำจ่ายสำเร็จ`, `ผู้รับได้รับเรียบร้อย`, `จัดส่งสำเร็จ`, `ส่งมอบเรียบร้อย`, `ส่งถึงผู้รับแล้ว` หรือรหัสสถานะ `501`, `delivered` แสดงด้วยป้ายสีเขียว Emerald พร้อมไอคอนเครื่องหมายถูก
   - **ส่งคืน (Returned)**: ตรวจจับจากคีย์เวิร์ด `ส่งคืน`, `คืนต้นทาง`, `ส่งคืนผู้ส่ง`, `ตีกลับ`, `ไม่สามารถส่งมอบ`, `ไม่สามารถนำจ่าย` หรือรหัสสถานะ `502`, `503`, `401`, `402`, `returned` แสดงด้วยป้ายสีแดง Rose พร้อมไอคอนตีกลับ
   - **อยู่ระหว่างการนำจ่าย (In Transit)**: จัดกลุ่มสถานะอื่นๆ ทั้งหมดที่อยู่ระหว่างทาง (เช่น `ส่งออกจากศูนย์คัดแยกสินค้า/ที่ทำการ`, `รับฝากเข้าระบบแล้ว`, `อยู่ระหว่างการขนส่ง`, `เตรียมการนำจ่าย` ฯลฯ) ให้แสดงเป็น `อยู่ระหว่างการนำจ่าย` ด้วยป้ายสีส้ม Amber อย่างเป็นระเบียบชัดเจน
   - แสดง Tooltip ข้อความเหตุการณ์จริงจากไปรษณีย์ไทยเมื่อนำเมาส์ไปชี้ที่ป้ายสถานะ
2. **การ์ดสรุปสถิติด้านบน (Summary Stat Cards - 5 ช่อง)**:
   - แสดงตัวเลขสถิติครบทั้ง 5 มิติ: (1) รายการทั้งหมด, (2) นำจ่ายสำเร็จ (พร้อม % สำเร็จ), (3) อยู่ระหว่างการนำจ่าย (พร้อม %), (4) ส่งคืน, (5) ยอดรวมค่าบริการ
3. **แท็บตัวกรองข้อมูล (Filter Tabs Toolbar)**:
   - มี 4 แท็บกรองสถานะ: `ทั้งหมด (N)`, `🟢 นำจ่ายสำเร็จ (N)`, `🟠 อยู่ระหว่างการนำจ่าย (N)`, และ `🔴 ส่งคืน (N)`
4. **การส่งออกรายงาน Excel และ PDF**:
   - คอลัมน์สถานะแสดงค่าที่ Normalize แล้ว (`นำจ่ายสำเร็จ`, `อยู่ระหว่างการนำจ่าย`, `ส่งคืน`)
   - ส่วนหัวสรุปรายงานแสดงตัวเลขสถิติแยกตามทั้ง 3 กลุ่มสถานะอย่างเป็นทางการ

---

## 21. ระบบแสดงวัน-เวลา และสถานที่ในไทม์ไลน์ติดตามสถานะพัสดุ (v2026.0914.2155)

1. **การดึงข้อมูลวันและเวลาสถานะจริงจาก Thailand Post Web Service (`getHistoryStatus`)**:
   - ระบบเพิ่มการตรวจจับฟิลด์ `statusDate` และ `station` จาก e-Parcel Web Service ซึ่งส่งมาเป็น `statusDate: "14/09/2026 14:30:00"` และ `station: "เรณูนคร"`
   - รองรับฟิลด์วันที่เวลาทุกรูปแบบทั้ง `statusDate`, `createdDate`, `receivedDate`, `dateTime`, `eventDate`, `orderDate`, และเวลาแยก `statusTime`
2. **การแสดงผลในหน้าติดตามสถานะพัสดุ (Tracking Inquiry View)**:
   - **ส่วนหัวผลการค้นหา**: แสดงป้าย `อัปเดตล่าสุด: DD/MM/YYYY HH:mm:ss` และป้ายสถานที่ `📍 ที่ทำการล่าสุด` พร้อมป้ายสถานะ 3 ระดับ (`นำจ่ายสำเร็จ`, `อยู่ระหว่างการนำจ่าย`, `ส่งคืน`)
   - **แต่ละขั้นตอนใน Stepper**: แสดงป้ายวัน-เวลาที่ชัดเจนด้วยไอคอนปฏิทิน-นาฬิกา ป้ายสถานที่ที่ทำการไปรษณีย์ และชื่อผู้ลงนาม
3. **การแสดงผลในหน้าต่างป๊อปอัป (Tracking Timeline Modal)**:
   - ซิงค์โครงสร้างและสไตล์เดียวกันเมื่อเปิดดูประวัติสถานะรายชิ้นจากตารางรายงานรับฝากหรือตาราง Workspace หลัก

---

## 22. รองรับแบบพิมพ์เอกสาร ท.ด. 38 ค. (เอกสารแจ้งการปักหลักเขตที่ดิน) (v2026.0914.2200)

1. **การเพิ่มเอกสารในหน้าต่างเกี่ยวกับระบบ (About Modal)**:
   - เพิ่มรายการต่อจาก `ท.ด. 38` ได้แก่:
     - **ท.ด. 38** (คำขอรังวัดแบ่งแยกในนามเดิม)
     - **ท.ด. 38 ค.** (เอกสารแจ้งการปักหลักเขตที่ดิน)
     - **ท.ด. 81** (คำขอรังวัดรวมโฉนด)
     - **ออกโฉนดที่ดิน** (คำขอรังวัดออกโฉนด)
2. **การสกัดและแปลงไฟล์ PDF อัตโนมัติ (`convert_dpost.py`)**:
   - อัปเกรด Regex การตรวจจับรหัสแบบพิมพ์: `r'\(\s*(ท\s*\.\s*ด\s*\.\s*[๐-๙0-9]+(?:\s*[ก-ฮ]\.?)?)\s*\)'` เพื่อให้ตรวจจับตัวอักษรต่อท้ายเช่น `ค.` หรือ `ค` ได้อย่างแม่นยำ
   - เพิ่ม Fallback ตรวจจับข้อความ `แจ้งการปักหลักเขตที่ดิน` และ `ท.ด. 38 ค` เพื่อกำหนดประเภทเอกสารเป็น `ท.ด. 38 ค.` โดยอัตโนมัติ

---

## 23. ปรับเปลี่ยนข้อความปุ่มดึงข้อมูลเป็น "อัปเดตข้อมูล" (v2026.0914.2205)

1. **ปรับปรุงข้อความปุ่มในหน้ารายงานรับฝาก**:
   - เปลี่ยนข้อความจาก `ดึงข้อมูลรับฝาก` เป็น `อัปเดตข้อมูล`
   - ปรับใช้ทั้งในหน้ารายงานรับฝากแบบเต็มจอ ([DepositReportView.jsx](file:///e:/My%20Drive/ส่วน%20ทข.ปข.10/เว็บ/Convert%20PDF%20To%20Excel/C2DPost_web/src/components/DepositReportView.jsx)) และหน้าต่างป๊อปอัปรายงาน ([DepositReportModal.jsx](file:///e:/My%20Drive/ส่วน%20ทข.ปข.10/เว็บ/Convert%20PDF%20To%20Excel/C2DPost_web/src/components/DepositReportModal.jsx))
   - สอดคล้องกับพฤติกรรมผู้ใช้งานที่ต้องการกดเพื่อรีเฟรชหรืออัปเดตข้อมูลรับฝากล่าสุดจากระบบ e-Parcel ไปรษณีย์ไทย

---

## 24. คลิกหมายเลข Barcode เพื่อตรวจสอบสถานะพัสดุ (v2026.0914.2215)

1. **การคลิกหมายเลขบาร์โค้ดในตารางรายงานรับฝาก ([DepositReportView.jsx](file:///e:/My%20Drive/ส่วน%20ทข.ปข.10/เว็บ/Convert%20PDF%20To%20Excel/C2DPost_web/src/components/DepositReportView.jsx) และ [DepositReportModal.jsx](file:///e:/My%20Drive/ส่วน%20ทข.ปข.10/เว็บ/Convert%20PDF%20To%20Excel/C2DPost_web/src/components/DepositReportModal.jsx))**:
   - ปรับเปลี่ยนเซลล์คอลัมน์ "หมายเลข Barcode" จากข้อความธรรมดาให้กลายเป็นปุ่มแบบ Interactive (`table-barcode-btn` / `barcode-btn-mono`) พร้อมไอคอนแว่นขยายตรวจสอบสถานะ
   - มี Hover Effect, เส้นใต้, Tooltip คำแนะนำ `คลิกเพื่อดูสถานะการตรวจสอบพัสดุ ...`
   - เมื่อคลิก จะเปิดหน้าต่างป๊อปอัป [TrackingTimelineModal](file:///e:/My%20Drive/ส่วน%20ทข.ปข.10/เว็บ/Convert%20PDF%20To%20Excel/C2DPost_web/src/components/TrackingTimelineModal.jsx) เพื่อแสดงประวัติการนำส่ง, ไทม์ไลน์สถานะ, ปณ. ที่เกี่ยวข้อง, และสถานะล่าสุดแบบเรียลไทม์ โดยผู้ใช้งานยังคงอยู่ในหน้าและรายการเดิมของตารางรายงานรับฝาก ไม่สูญเสียลำดับหน้าหรือตัวกรอง
2. **ปุ่มเปิดหน้าตรวจสอบเต็มจอในหน้าต่าง Tracking Timeline**:
   - เพิ่มปุ่ม `เปิดหน้าตรวจสอบเต็มจอ` ในกรณีที่ผู้ใช้งานต้องการสลับไปยังหน้า [TrackingInquiryView](file:///e:/My%20Drive/ส่วน%20ทข.ปข.10/เว็บ/Convert%20PDF%20To%20Excel/C2DPost_web/src/components/TrackingInquiryView.jsx) แบบเต็มรูปแบบ
3. **การคลิกหมายเลขบาร์โค้ดในตาราง Workspace หลัก ([PreviewGrid.jsx](file:///e:/My%20Drive/ส่วน%20ทข.ปข.10/เว็บ/Convert%20PDF%20To%20Excel/C2DPost_web/src/components/PreviewGrid.jsx))**:
   - รองรับการคลิกหมายเลขบาร์โค้ดในคอลัมน์ "หมายเลข" เพื่อเปิดดูสถานะพัสดุได้เช่นเดียวกัน สะดวก รวดเร็ว และเป็นธรรมชาติ

---

## 25. ปรับเปลี่ยนชื่อแท็บเมนูเป็น "รายงานสถานะ" (v2026.0915.0931)

1. **การปรับชื่อแท็บเมนูหลักบน Navbar ([Navbar.jsx](file:///e:/My%20Drive/ส่วน%20ทข.ปข.10/เว็บ/Convert%20PDF%20To%20Excel/C2DPost_web/src/components/Navbar.jsx))**:
   - เปลี่ยนข้อความจาก `รายงานรับฝาก` ➜ `รายงานสถานะ`
   - ปรับ Title และเมนูด้านข้างให้เป็น `รายงานสถานะ` เพื่อความกระชับ ชัดเจน และสะท้อนถึงการตรวจเช็คสถานะการนำส่งพัสดุครบวงจร (ทั้งนำจ่ายสำเร็จ, อยู่ระหว่างการนำจ่าย, และส่งคืน)
2. **ปรับหัวข้อในหน้ารายงาน ([DepositReportView.jsx](file:///e:/My%20Drive/ส่วน%20ทข.ปข.10/เว็บ/Convert%20PDF%20To%20Excel/C2DPost_web/src/components/DepositReportView.jsx) และ [DepositReportModal.jsx](file:///e:/My%20Drive/ส่วน%20ทข.ปข.10/เว็บ/Convert%20PDF%20To%20Excel/C2DPost_web/src/components/DepositReportModal.jsx))**:
   - ปรับชื่อหัวเรื่องเป็น `รายงานสถานะไปรษณีย์ (e-Parcel Status Report)` ให้สอดคล้องกับชื่อแท็บเมนู

---

## 26. จัดเรียงการ์ดสรุปยอดสถานะและตัวกรองใหม่ พร้อมแสดงค่า % ในการ์ดส่งคืน (v2026.0915.0931)

1. **การจัดเรียงลำดับการ์ดสถิติสรุปภาพรวม 6 ช่อง (DepositReportView.jsx & DepositReportModal.jsx)**:
   - **การ์ด 1: รับฝากแล้ว** (	ext-blue, ไอคอนกล่องพัสดุ icon-received-deposit) แสดงจำนวนชิ้นและสัดส่วน {receivedCount} ({receivedRate}%)
   - **การ์ด 2: อยู่ระหว่างการนำจ่าย** (	ext-amber, ไอคอนรถขนส่ง icon-transit) แสดงจำนวนชิ้นและสัดส่วน {inTransitCount} ({inTransitRate}%)
   - **การ์ด 3: นำจ่ายสำเร็จ** (	ext-emerald, ไอคอนเครื่องหมายถูก icon-delivered) แสดงจำนวนชิ้นและสัดส่วน {deliveredCount} ({deliveredRate}%)
   - **การ์ด 4: ส่งคืน** (	ext-rose, ไอคอนลูกศรย้อนกลับ icon-returned) ปรับจากการแสดงผลเดิมที่ไม่มีเปอร์เซ็นต์ ให้แสดงค่าเปอร์เซ็นต์อัตโนมัติ {returnedCount} ({returnedRate}%)
   - **การ์ด 5: รายการทั้งหมด** (icon-total) แสดงจำนวนรวม {total_items} ฉบับ
   - **การ์ด 6: ยอดรวมค่าบริการ** (	ext-gold, ไอคอนการเงิน icon-fee) แสดงยอดเงินรวม ฿{total_fee}
2. **การจัดเรียงแท็บตัวกรอง (Filter Tabs)**:
   - ปรับลำดับแท็บตัวกรองให้สอดคล้องกับลำดับการ์ดด้านบน: ทั้งหมด ➔ รับฝากแล้ว ➔ อยู่ระหว่างการนำจ่าย ➔ นำจ่ายสำเร็จ ➔ ส่งคืน

---

## 27. การจดจำสถานะและข้อมูลล่าสุดของหน้ารายงานสถานะ (State & Cache Persistence - v2026.0915.0931)

1. **ปัญหาเดิม**:
   - เมื่อผู้ใช้งานอยู่ในหน้ารายงานสถานะ (เช่น ดึงข้อมูลของวันที่เมื่อวาน 14/09/2026 มี 12 รายการ) แล้วสลับไปหน้าอื่น เช่น หน้าแปลงไฟล์ PDF หรือหน้าตรวจสอบพัสดุ เมื่อกดกลับมาที่หน้ารายงานสถานะ ระบบจะ Unmount/Mount ใหม่ ทำให้ค่าถูกรีเซ็ตกลับเป็นวันที่ปัจจุบัน และต้องดึงข้อมูลใหม่ทุกครั้ง
2. **โซลูชันวิศวกรรม (sessionStorage Cache Persistence)**:
   - บันทึกและกู้คืน State อัตโนมัติผ่านคีย์ `c2dpost_deposit_report_cache` ใน `sessionStorage`:
     - `startDate` และ `endDate` (คงค่าช่วงวันที่เดิม เช่น เมื่อวาน พร้อม Active Button)
     - `reportData` (คงรายการพัสดุและสถิติการ์ดทั้ง 6 ช่องที่ดึงมาแล้ว ไม่ต้องยิง API ซ้ำ)
     - `filterTab` (คงแท็บตัวกรองที่เลือกไว้ เช่น ทั้งหมด, รับฝากแล้ว, ฯลฯ)
     - `searchQuery` (คงข้อความค้นหาที่พิมพ์ค้างไว้)
     - `currentPage` (คงเลขหน้าที่เปิดดูอยู่)
   - ข้ามการ Initial Fetch อัตโนมัติ หากมีข้อมูลในแคชอยู่แล้ว ผู้ใช้งานสามารถกดปุ่ม "อัปเดตข้อมูล" ได้ทุกเมื่อที่ต้องการรีเฟรชข้อมูลสดจากไปรษณีย์ไทย
   - เมื่อกด "ออกจากระบบ" (handleLogout) ระบบจะล้างแคชนี้ทิ้งอัตโนมัติเพื่อความปลอดภัย

---

## 28. การแสดงข้อมูลสถานะล่าสุดของแต่ละหมายเลขพัสดุ (Latest Status Date & Station Tracking - v2026.0915.0938)

1. **ปัญหาและความต้องการ**:
   - เดิมตารางรายงานสถานะแสดงคอลัมน์ `วัน-เวลารับฝาก` และ `ปณ.รับฝาก` ซึ่งเป็นเพียงข้อมูลจุดรับฝากเริ่มต้น ทำให้พัสดุที่อยู่ระหว่างการนำจ่าย, นำจ่ายสำเร็จ, หรือส่งคืน ยังคงแสดงวัน-เวลาและที่ทำการไปรษณีย์เดิมที่รับฝากวันแรกเหมือนกันหมด
2. **โซลูชันวิศวกรรม**:
   - **การปรับเปลี่ยนหัวตาราง (Table Headers)**:
     - `วัน-เวลารับฝาก` ➔ `วัน-เวลาล่าสุด` (พร้อม Tooltip: "วันและเวลาของสถานะล่าสุด")
     - `ปณ.รับฝาก` ➔ `ปณ./สถานที่ล่าสุด` (พร้อม Tooltip: "ที่ทำการไปรษณีย์หรือสถานที่ของสถานะล่าสุด")
   - **การประมวลผลข้อมูลฝั่ง Backend (`api/index.py`)**:
     - ดึงฟิลด์สถานะล่าสุดแบบ Comprehensive: `statusDate`, `status_date`, `dateTime`, `eventDate` ควบรวมกับ `statusTime` ลงในคีย์ `latest_date`
     - ดึงชื่อสถานที่/ศูนย์คัดแยก/ที่ทำการไปรษณีย์ล่าสุด: `station`, `stationName`, `location`, `officeName` ลงในคีย์ `latest_station`
     - ยังคงรักษา `received_date` และ `received_postoffice` ไว้เพื่อความสมบูรณ์ของประวัติและ Backward Compatibility
   - **การแสดงผลฝั่ง Frontend (`DepositReportView.jsx` & `DepositReportModal.jsx`)**:
     - แสดง `item.latest_date || item.received_date` ใน Badge สวยงามพร้อม Tooltip แสดงทั้งสถานะล่าสุดและวัน-เวลารับฝากต้นทาง
     - แสดง `item.latest_station || item.received_postoffice` พร้อม Tooltip ข้อมูล ปณ.รับฝาก
     - ปรับปรุงระบบค้นหาด่วน (Search Box) ให้ค้นหาตาม `latest_station` และ `latest_date` ได้ทันที
   - **การส่งออกเอกสาร Excel และ PDF (`convert_dpost.py`)**:
     - อัปเดตหัวคอลัมน์ของไฟล์ Excel (.xlsx) และ PDF Landscape A4 ให้เป็น `วัน-เวลาล่าสุด` และ `ปณ./สถานที่ล่าสุด` พร้อมดึงข้อมูลสถานะล่าสุดลงในเอกสารรายงานราชการอย่างถูกต้อง 100%

---

## 29. การแก้ไขหน้าต่างตรวจสอบประวัติสถานะพัสดุรายชิ้น (Tracking Timeline Modal Fix - v2026.0915.0947)

1. **ปัญหาที่พบ**:
   - เมื่อผู้ใช้งานคลิกที่หมายเลข Barcode ในตารางรายงานสถานะ หน้าต่างตรวจสอบประวัติพัสดุ (TrackingTimelineModal) ไม่ปรากฏ หรือเกิดข้อผิดพลาดในการแสดงผล
2. **สาเหตุ**:
   - ในคอมโพเนนต์ `TrackingTimelineModal.jsx` มีการเรียกใช้ Hook `useMemo` ในการคำนวณ `statusInfo` แต่ในคำสั่ง `import` ด้านบนสุดของไฟล์ไม่ได้ประกาศนำเข้า `useMemo` จาก `'react'` ส่งผลให้เกิดข้อผิดพลาดระดับ Runtime: `ReferenceError: useMemo is not defined`
3. **โซลูชันวิศวกรรม**:
   - นำเข้า `useMemo` ใน `TrackingTimelineModal.jsx`: `import React, { useState, useEffect, useCallback, useMemo } from 'react';`
   - ส่งข้อมูล `recInfo` จาก `DepositReportView.jsx` และ `DepositReportModal.jsx` ครบถ้วนทั้ง `status_key`, `status_label`, `status_description_raw`, และ `latest_date`
   - ปรับปรุงฟังก์ชัน `_build_demo_tracking(barcode)` ใน `api/index.py` ให้จำลองไทม์ไลน์ Stepper ที่สอดคล้องกับสถานะจริงของแต่ละหมายเลข (เช่น หมายเลขที่นำจ่ายสำเร็จจะแสดงขั้นตอนถึงผู้รับเรียบร้อย, หมายเลขที่ส่งคืนจะแสดงเหตุผลการส่งคืน) ทำให้แสดงผลได้สมจริง 100% ทั้งในโหมดสาธิตและระบบจริง

---

## 30. ระบบซิงก์และอัปเดตวัน-เวลา และสถานที่สถานะล่าสุดแบบเรียลไทม์ (Live Tracking Auto-Enrichment & Sync Parity - v2026.0915.1025)

1. **ปัญหาที่พบและความเป็นจริงของ API ไปรษณีย์ไทย**:
   - เมื่อผู้ใช้งานเปิดดูหน้าต่างประวัติสถานะพัสดุ (TrackingTimelineModal) เช่น หมายเลข `BC414110996TH` พบว่าไทม์ไลน์แสดงสถานะ "ถึง ปณ.ปลายทาง เตรียมนำจ่าย" วันที่ `15/09/2026 09:27:45` ณ `เรณูนคร`
   - แต่ในตารางรายงานสถานะเบื้องหลัง ข้อมูลในคอลัมน์ "วัน-เวลาล่าสุด" และ "ปณ./สถานที่ล่าสุด" ยังคงแสดงเป็นวันที่รับฝากวันแรก `14/09/2026 14:30:40` เหมือนกันทุกแถว
   - **สาเหตุเชิงสถาปัตยกรรม**: API `getAllOrderReceived` ของไปรษณีย์ไทยเป็นเพียงระบบบันทึกคำสั่งรับฝาก (Order Reception) ซึ่งจะอัปเดตเฉพาะรหัสสถานะ `status` (`003` อยู่ระหว่างการนำจ่าย) แต่ไม่ได้ส่งวัน-เวลาของเช็กพอยต์ล่าสุด (`statusDate`) และสถานที่ล่าสุด (`station`) กลับมาในก้อนข้อมูลรับฝาก ข้อมูลเช็กพอยต์สดเหล่านี้จะถูกส่งผ่าน Track & Trace API `getHistoryStatus?barcode=...` เท่านั้น

2. **โซลูชันวิศวกรรม 3 ระดับ (Three-Tier Real-Time Synchronization)**:
   - **ระดับที่ 1: Automatic Parallel Enrichment บน Backend (`/api/reports/received`)**:
     - เมื่อผู้ใช้งานร้องขอรายงานสถานะ หากมีจำนวนพัสดุไม่เกิน 35 รายการ Backend จะทำการดึง `getHistoryStatus` ควบคู่กันแบบ Parallel ทันทีโดยใช้ `ThreadPoolExecutor(max_workers=min(len(records), 10))` พร้อมกำหนด Timeout ป้องกันคอขวด
     - นำผลลัพธ์เช็กพอยต์ล่าสุด (`latest_datetime`, `latest_location`, `latest_status_key`) มารวมเข้ากับ `latest_date` และ `latest_station` ของแต่ละรายการในตารางโดยตรง ทำให้เมื่อเปิดหน้ารายงานขึ้นมา ตารางจะแสดงวันเวลาและสถานที่ล่าสุดที่แท้จริงทันที
   - **ระดับที่ 2: Callback เชื่อมโยงไทม์ไลน์กับตาราง (`onTrackingUpdated`)**:
     - ในคอมโพเนนต์ `TrackingTimelineModal.jsx` เมื่อการดึงข้อมูล `fetchTrackingStatus` สำเร็จ จะเรียกฟังก์ชัน `onTrackingUpdated(barcode, result)` กลับไปยัง Component แม่ (`DepositReportView.jsx` และ `DepositReportModal.jsx`)
     - คอมโพเนนต์แม่จะทำการอัปเดต Record แถวนั้นใน state `reportData.records` ทันที พร้อมคำนวณการ์ดสรุปยอดสถานะ (Summary Stats) ใหม่ และบันทึกลง `sessionStorage` แบบอัตโนมัติ ทำให้ผู้ใช้เห็นแถวข้อมูลในตารางเปลี่ยนเป็นข้อมูลล่าสุดทันทีที่ปิดหน้าต่าง Modal
   - **ระดับที่ 3: ปุ่มซิงก์สถานะล่าสุดแบบชุด (Batch Tracking Sync Endpoint)**:
     - พัฒนา Endpoint ใหม่ `POST /api/reports/batch-tracking` รับรายการบาร์โค้ด และประมวลผลดึงสถานะคู่ขนาน
     - เพิ่มปุ่ม "ซิงก์สถานะล่าสุด (n รายการ)" พร้อมแอนิเมชัน Spinner บนหน้ารายงานสถานะ เพื่อให้ผู้ใช้สามารถกดอัปเดตเช็กพอยต์พัสดุทั้งหมดในตารางได้ตามต้องการ

---

## 31. การนำปุ่ม "กลับหน้าแปลงไฟล์" ออกจากส่วนหัวของหน้ารายงานสถานะและหน้าตรวจสอบพัสดุ (v2026.0915.1029)

1. **ปัญหาและความต้องการของผู้ใช้งาน**:
   - ในหน้า `DepositReportView` และ `TrackingInquiryView` มีปุ่ม `[ ← กลับหน้าแปลงไฟล์ ]` แสดงอยู่ด้านขวาบนของ Header
   - เนื่องจากระบบได้เปลี่ยนผ่านสู่โครงสร้าง Page-Based Navigation อย่างสมบูรณ์แล้ว โดยมีแถบ Navigation Bar ด้านบนสุด (`[ แปลงไฟล์ PDF ]  [ รายงานสถานะ ]  [ ตรวจสอบพัสดุ ]`) ที่ผู้ใช้งานสามารถคลิกสลับหน้าได้ทุกเมื่อ การมีปุ่ม "กลับหน้าแปลงไฟล์" ซ้ำซ้อนจึงทำให้ UI รกและเปลืองพื้นที่โดยไม่จำเป็น
2. **โซลูชันวิศวกรรม**:
   - นำบล็อกปุ่ม `btn-back-to-workspace` ออกจาก Header ของ `DepositReportView.jsx` และ `TrackingInquiryView.jsx`
   - ทำให้ส่วนหัวของหน้าเว็บสะอาด โปร่ง โฟกัสกับชื่อรายงานและองค์กรผู้ใช้งานอย่างเต็มที่

---

## 32. การรวมการดึงข้อมูลสถานะล่าสุดและซ่อนปุ่มซิงก์ที่ซ้ำซ้อน (Consolidated Auto-Enrichment & Button Cleanup - v2026.0915.1032)

1. **ปัญหาและความสับสนของผู้ใช้งาน**:
   - บนหน้ารายงานสถานะมีทั้งปุ่ม `[ 🔍 อัปเดตข้อมูล ]` (สีเขียว) และ `[ 🔄 ซิงก์สถานะล่าสุด ]` (สีฟ้า) แสดงอยู่คู่กัน
   - ในความเป็นจริง ปุ่ม "อัปเดตข้อมูล" ได้รวมขั้นตอนการดึงรายงานและ Auto-Enrichment ดึงสถานะนำจ่าย/สถานที่ล่าสุดจากไปรษณีย์ไทยแบบขนานมาให้อัตโนมัติอยู่แล้ว การมีปุ่ม "ซิงก์สถานะล่าสุด" จึงเป็นการทำงานที่ซ้ำซ้อนและสร้างความสับสน
2. **โซลูชันวิศวกรรม**:
   - นำปุ่ม `btn-sync-tracking` ออกจากหน้าจอ (ซ่อนปุ่มตามคำขอ)
   - ปรับปรุงให้ปุ่ม `[ 🔍 อัปเดตข้อมูล ]` เป็น Single Source of Truth:
     - หากข้อมูลมีไม่เกิน 35 รายการ: Backend API ทำการดึงสถานะนำจ่ายสดแบบคู่ขนานส่งกลับมาทันที
     - หากข้อมูลมีมากกว่า 35 รายการ: Frontend จะสั่งดึงสถานะชุดผ่าน Batch Tracking ในเบื้องหลังแบบไร้รอยต่ออัตโนมัติ
   - คงเหลือเฉพาะปุ่ม "อัปเดตข้อมูล" เพียงปุ่มเดียว สะอาดตา ชัดเจน ใช้งานง่าย 100%

---

## 33. การเพิ่มปุ่มลัดเลือกช่วงเวลา "เดือนที่แล้ว" (Last Month Date Shortcut Preset - v2026.0915.1044)

1. **ปัญหาและความต้องการ**:
   - ผู้ใช้งานจำเป็นต้องตรวจสอบรายงานรับฝากและสถานะพัสดุย้อนหลังสำหรับรอบบัญชีเดือนก่อนหน้า ซึ่งเดิมมีปุ่มลัดเพียง: วันนี้, เมื่อวาน, 7 วันล่าสุด, เดือนนี้ ทำให้หากต้องการดูข้อมูลเดือนก่อน ต้องคลิกเลือกวันที่เริ่มต้นและสิ้นสุดในปฏิทินเองทีละช่อง
2. **โซลูชันวิศวกรรม**:
   - **การคำนวณวันเริ่มต้นและสิ้นสุดเดือนก่อนหน้าแบบไดนามิก**:
     - วันแรกของเดือนที่แล้ว: `new Date(year, month - 1, 1)`
     - วันสุดท้ายของเดือนที่แล้ว: `new Date(year, month, 0)` (การใช้วันที่ 0 ของเดือนปัจจุบัน จะคืนค่าวันสุดท้ายของเดือนก่อนหน้าโดยอัตโนมัติ พร้อมรองรับการข้ามปีปฏิทินจากมกราคมไปธันวาคม)
   - **การเพิ่มปุ่มและสถานะ Active**:
     - เพิ่มปุ่ม `[ เดือนที่แล้ว ]` ใน `DepositReportView.jsx` และ `DepositReportModal.jsx`
     - เพิ่มการตรวจสอบคลาส `.active` เมื่อช่วงวันที่ตรงกับ `lastMonthStartIso` และ `lastMonthEndIso`
     - เพิ่ม `flex-wrap: wrap; align-items: center;` ใน `.deposit-quick-date-btns` เพื่อป้องกันการล้นกรอบบนหน้าจอขนาดเล็ก

---

## 34. การนำสัญลักษณ์สกุลเงิน ฿ ออกจากค่าบริการ (Currency Symbol ฿ Removal - v2026.0915.1051)

1. **ปัญหาและความต้องการ**:
   - ผู้ใช้งานระบุความต้องการไม่ให้แสดงสัญลักษณ์ ฿ ในค่าบริการ เพื่อความสะอาดตาและความสอดคล้องกับตารางสรุปข้อมูลราชการ
2. **โซลูชันวิศวกรรม**:
   - **หน้ารายงานสถานะและหน้าต่าง Modal (DepositReportView.jsx, DepositReportModal.jsx)**:
     - การ์ดสรุปยอดรวมค่าบริการ: ตัด ฿ ออก แสดงเฉพาะตัวเลขฟอร์แมต 252.00
     - เซลล์ค่าบริการในตารางข้อมูล: ตัด ฿ ออก แสดงเฉพาะตัวเลข 21.00
   - **การส่งออกเอกสาร Excel และ PDF (convert_dpost.py)**:
     - ปรับหัวตารางจาก 'ค่าบริการ (฿)' เป็น 'ค่าบริการ'
     - ปรับข้อความสรุปหัวเรื่องจาก 'ยอดค่าบริการรวม: ฿...' เป็น 'ยอดค่าบริการรวม: ...'

---

## 35. ระบบรองรับการลากและวางไฟล์ PDF (Drag & Drop File Upload Support - v2026.0915.1105)

1. **ปัญหาและความต้องการ**:
   - ผู้ใช้งานต้องการลากไฟล์ PDF มาวาง (Drop) เพื่อเริ่มการประมวลผลแปลงไฟล์ได้ทันที จากเดิมที่รองรับเพียงการคลิกปุ่ม "เพิ่มไฟล์ PDF" หรือลากได้เฉพาะแถบเครื่องมือด้านบนแบบไม่มีการแสดงผลไฮไลต์ (Visual Feedback) และหากลากไฟล์ปล่อยนอกกรอบ บราว์เซอร์จะเปิดไฟล์ PDF แทนหน้าเว็บ (Browser Tab Hijack)
2. **โซลูชันวิศวกรรม**:
   - **การป้องกันการแย่งสิทธิ์ของบราว์เซอร์ (Window Drag Prevention)**:
     - ติดตั้ง Event Listener ระดับ `window` สำหรับ `dragover` และ `drop` เพื่อเรียก `e.preventDefault()` ป้องกันไม่ให้บราว์เซอร์เปลี่ยนเส้นทางไปเปิดไฟล์ PDF
   - **ระบบ Glassmorphic Drop Overlay ทั่วทั้งหน้าจอ**:
     - เพิ่ม State ตรวจจับการลากไฟล์เข้าสู่พื้นที่ Workspace (`isWorkspaceDragOver`) โดยใช้ `dragCounterRef` เพื่อขจัดปัญหาการกะพริบ (Flicker) เมื่อลากผ่านองค์ประกอบลูก (Child DOM elements)
     - แสดง Overlay แบบกระจกเงาเบลอ (Glassmorphism) สวยงาม พร้อมไอคอน PDF เคลื่อนไหว (Bounce Animation) และข้อความแนะนำการวางไฟล์
   - **การทำให้พื้นที่ว่างของตาราง (Empty State) รองรับการคลิกและลากวางแบบ Interactive**:
     - ปรับแต่ง `.empty-table-state` ใน `PreviewGrid.jsx` ให้สามารถคลิกเพื่อเปิดกล่องเลือกไฟล์ หรือลากไฟล์ PDF มาวางลงในกล่องตารางได้โดยตรง
     - เพิ่มเอฟเฟกต์ `.drag-over` ด้วยขอบประสีเขียวมรกต (`#10b981`) และพื้นหลังเรืองแสง
   - **การรองรับหลายจุดรับไฟล์ (Multi-Target Drag & Drop Parity)**:
     - รองรับการลากวางไฟล์ทั้งใน `App.jsx` (Workspace ระดับหน้า), `ActionToolbar.jsx` (แถบเลือกไฟล์ด้านบน), และ `PreviewGrid.jsx` (ตารางข้อมูล) โดยเชื่อมโยงเข้ากับฟังก์ชัน `handleFilesSelected` เดียวกันอย่างไร้รอยต่อ

---

## 36. การปรับขนาดพื้นที่ Drop ไฟล์ให้พอดีกับกรอบตาราง (Drop Zone Full-Frame Fit - v2026.0915.1118)

1. **ปัญหาและความต้องการ**:
   - พื้นที่กรอบลากวางไฟล์ (`.empty-table-state`) มีความสูงเฉพาะเนื้อหาภายใน ทำให้เกิดช่องว่างขนาดใหญ่ด้านล่างของการ์ดตาราง (`.card-preview-table`) ขอบประสีเขียวไม่พอดีกับกรอบ ทำให้การแสดงผลดูไม่สมดุล
2. **โซลูชันวิศวกรรม**:
   - กำหนดให้ `.table-wrapper` ใช้ `flex: 1; display: flex; flex-direction: column; width: 100%; min-height: 380px;`
   - กำหนดให้ `.empty-table-state` ใช้ `flex: 1; width: 100%; min-height: 380px; box-sizing: border-box;` ขยายความสูงเต็มพื้นที่การ์ด
   - ปรับเส้นขอบประเป็น `2px dashed rgba(16, 185, 129, 0.35)` ที่แสดงผลอย่างนุ่มนวลตลอดเวลา และขยายสีเป็น `#10b981` เมื่อ Hover หรือลากไฟล์วาง ขอบด้านบน ล่าง ซ้าย ขวา พอดีกับขอบในของการ์ดอย่างสมบูรณ์แบบ



---

## 37. ระบบรองรับเอกสารแจ้งผลสอบเขตและซองจดหมายราชการ (Envelope & Land Notice Parser Upgrade - v2026.0915.1145)

1. **ปัญหาและสาเหตุหลัก (Root Cause Analysis)**:
   - **การสกัดสระอำกระจายตัว (Decomposed Sara Am `ำ`)**: ในไฟล์ PDF เอกสารราชการบางประเภท ฟอนต์เข้ารหัสสระอำด้วยพยัญชนะ + ช่องว่าง (`0x20`) + สระอา (`า`) เช่น `ส านักงานที่ดิน`, `ต าบล`, `อ าเภอ`, `ช าระค่าฝากส่ง` ทำให้การตรวจสอบคำสำคัญและ Regex ปกติล้มเหลว
   - **การสับสนเลขโฉนดที่ดินเป็นรหัสไปรษณีย์**: ในหน้าแรกของเอกสาร (เนื้อหาหนังสือ) มีข้อความระบุ `โฉนดที่ดินเลขที่ 19583` ซึ่งเป็นตัวเลข 5 หลัก ทำให้ระบบเดิมดึงเลข `19583` ไปเป็นรหัสไปรษณีย์ และเข้าใจว่าข้อความรอบข้างคือที่อยู่ผู้รับ
   - **หน้าซองจดหมายไม่มีคำว่า "เรียน"**: เอกสารรูปแบบใหม่มี 2 หน้า โดยหน้าที่ 2 คือหน้าซองจดหมายราชการที่มีตราอนุญาตฝากส่งเป็นเงินเชื่อ และใช้คำว่า `ผู้รับ` หรือวางบล็อกที่อยู่อย่างเป็นทางการ ทำให้เงื่อนไข `if "เรียน" in text` ข้ามการประมวลผลหน้าซองไป
   - **ข้อผิดพลาด e-Parcel API (Code: 027)**: เนื่องจากที่อยู่ถูกดึงผิดพลาด ฟิลด์ `amphur` และ `province` จึงว่างเปล่า (`null / blank`) ทำให้การส่งข้อมูล e-Parcel ล้มเหลวด้วยข้อความ `Code: 027, Detail: Parameter amphur must not be null or blank value, Status: false`

2. **โซลูชันวิศวกรรมและการแก้ปัญหา**:
   - **ระบบ Normalize สระและคำภาษาไทย (`normalize_pdf_text`)**:
     - ทำการแปลง `([ก-ฮ])\s+า` -> `\1ำ` ครอบคลุมสระอำทุกตัว
     - แทนที่คำสำคัญที่กระจายตัว: `ส านักงาน` -> `สำนักงาน`, `ต าบล` -> `ตำบล`, `อ าเภอ` -> `อำเภอ`, `ช าระ` -> `ชำระ`, `ด าเนินการ` -> `ดำเนินการ`
   - **การกรองเลขโฉนดที่ดินไม่ให้เป็นรหัสไปรษณีย์**:
     - เพิ่มการตรวจสอบใน `parse_receiver_label`: หากบรรทัดมีคำว่า `โฉนด`, `ที่ดิน`, `หน้าสำรวจ`, `ระวาง`, หรือ `คำขอ` จะไม่นำตัวเลขในบรรทัดนั้นมาเป็นรหัสไปรษณีย์เด็ดขาด
   - **เอนจินสกัดหน้าซองจดหมายราชการ (`parse_envelope_label`)**:
     - ตรวจจับหน้าซองจดหมายจากตราไปรษณีย์ (`ชำระค่าฝากส่ง`, `ใบอนุญาตเลขที่`, `ปณ.`) หรือคำว่า `ผู้รับ` ร่วมกับรหัสไปรษณีย์ 5 หลัก
     - สกัด **เลขที่อ้างอิง (INV_NO)**: รองรับกรณีมีวรรค เช่น `ที่ นพ 0020.05/644 ผู้รับ` -> `นพ0020.05/644`
     - สกัด **ชื่อผู้รับ (RECEIVER)**: ตรวจจับคำนำหน้า `นาย`, `นางสาว`, `นาง`, `ว่าที่`, ฯลฯ หรือหลังคำว่า `ผู้รับ` -> `นาย สมบัติ บางศิริ`
     - สกัด **ที่อยู่ผู้รับ (RECEIVER_ADDRESS)**: รวมบ้านเลขที่ หมู่ที่ และตำบล พร้อมปรับเป็น `ต.` ที่มีมาตรฐาน -> `63 หมู่ที่ 7 ต.หนองย่างชิ้น`
     - สกัด **อำเภอ/เขต (RECEIVER_AMPHUR)** และ **จังหวัด (RECEIVER_PROVINCE)**: ใช้ Regex ภาษาไทยเต็มรูปแบบ `[\u0e00-\u0e7f]+` เพื่อรองรับสระหน้า (เ, แ, โ, ใ, ไ) -> อำเภอ `เรณูนคร`, จังหวัด `นครพนม`
     - สกัด **รหัสไปรษณีย์ (RECEIVER_ZIPCODE)**: สกัดเลข 5 หลักบนหน้าซอง -> `48170`
     - สกัด **ประเภทเอกสาร (PRODUCT_IN_BOX)**: ตรวจจับจากหัวเรื่อง เช่น `ขอให้ไปดำเนินการเรื่องสอบเขต` -> `สอบเขต`
   - **ระบบ Overwrite และ Data Parity**:
     - หากเอกสารมีทั้งหน้าเนื้อหาและหน้าซอง ข้อมูลที่ถูกต้องและสมบูรณ์จากหน้าซองจะถูกนำมาอัปเดตทับข้อมูลเดิม ทำให้ได้ข้อมูลที่อยู่และรหัสไปรษณีย์ที่ถูกต้อง 100% พร้อมส่ง e-Parcel สำเร็จโดยไม่มีข้อผิดพลาด Parameter amphur อีกต่อไป

---

## 38. การแก้ปัญหาการส่ง e-Parcel ล้มเหลว Code 073 & 017 ในไฟล์แจ้งผู้ขอจดทะเบียน (Shipper Postcode & e-Parcel Submission Fix - v2026.0916.2105)

1. **ปัญหาและข้อผิดพลาด (Issue & Root Cause Analysis)**:
   - **อาการ**: เมื่อเพิ่มไฟล์ `แจ้งผู้ขอจดทะเบียน-ซอง.pdf` และดึงบาร์โค้ดแล้วกดปุ่ม "ส่งข้อมูล e-Parcel" ระบบแสดงกล่องข้อความเตือน `สำเร็จบางส่วน / พบข้อผิดพลาด 1 รายการ` และในตารางระบุข้อผิดพลาดสีแดง:
     `✕ Code: 073, Detail: Postcode ยังไม่เปิดให้บริการ., Status: false | Code: 017, Detail: Please check your input Zipcode or Prodinbox or Barcode., Status: false`
   - **สาเหตุที่ 1 (Shipper Address Garbage Selection)**:
     - ในหน้าแรกของไฟล์ `แจ้งผู้ขอจดทะเบียน-ซอง.pdf` มีข้อความเนื้อหาจดหมายหลังชื่อสำนักงานที่ดิน เช่น `เจ้าพนักงานที่ดิน... ณ สำนักงานที่ดิน... ในวันที่... หรือภายใน 30 วัน บัดนี้ พนักงานเจ้าหน้าที่ได้ดำเนินการแล้ว จึงขอให้ท่านไปจดทะเบียน...` (ความยาว 314 ตัวอักษร)
     - ขณะที่หน้าที่ 2 (หน้าซองจดหมาย) มีที่อยู่จริงของสำนักงานที่ดินคือ `116 หมู่ที่ 9 ตำบลโพนทอง นพ 48170` (ความยาว 117 ตัวอักษร)
     - ระบบเดิมใช้เงื่อนไขคัดเลือกที่อยู่ผู้ส่ง `if len(addr) > len(best_shipper_address):` ทำให้ข้อความร่างจดหมายที่มีความยาวมากกว่าบนหน้าแรกถูกเลือกเป็นที่อยู่ผู้ส่ง และเนื่องจากข้อความดังกล่าวไม่มีรหัสไปรษณีย์ ฟังก์ชัน `parse_address_components` จึงคืนค่า `shipper_zipcode = ""` (ว่างเปล่า)
   - **สาเหตุที่ 2 (e-Parcel Postcode Validation Failure)**:
     - เมื่อผู้ใช้กดส่งข้อมูล e-Parcel ฟิลด์ `shipperZipcode` ถูกส่งเป็นค่าว่าง (`""`) ทำให้เซิร์ฟเวอร์ e-Parcel ของไปรษณีย์ไทยปฏิเสธคำขอทันทีด้วยรหัสข้อผิดพลาด `Code: 073` ("Postcode ยังไม่เปิดให้บริการ") และ `Code: 017` ("Please check your input Zipcode or Prodinbox or Barcode")
   - **สาเหตุที่ 3 (Frontend Shipper Fallback Missing)**:
     - ฟังก์ชัน `handleSendEparcel` ใน `App.jsx` นำค่า `row.SHIPPER_ZIPCODE` ส่งตรงไปยัง API โดยไม่ได้ Fallback ไปยังรหัสไปรษณีย์ของที่ทำการที่เลือกใช้บริการ (`currentPerson?.ResponsibleZipcode`) ในกรณีที่ค่าในเรคคอร์ดว่างเปล่า

2. **โซลูชันวิศวกรรมและการแก้ปัญหา (Engineering Solutions)**:
   - **1. ระบบให้คะแนนคุณภาพที่อยู่ผู้ส่ง (Smart Address Quality Scoring)**:
     - ยกเลิกการวัดความถูกต้องจากความยาวของสตริงเพียงอย่างเดียว และเพิ่มระบบคำนวณคะแนน `score_shipper_addr`:
       - เพิ่มคะแนนอย่างมาก (+1,000 คะแนน) ให้กับสตริงที่มีรหัสไปรษณีย์ 5 หลัก (`\b[0-9]{5}\b`)
       - เพิ่มคะแนน (+500 คะแนน) ให้กับสตริงที่มีคำระบุที่อยู่ (`หมู่`, `ตำบล`, `ต.`, `ถนน`, `ถ.`, `โพนทอง`)
       - ตัดคะแนนอย่างหนัก (-2,000 คะแนน) หากพบข้อความร่างหนังสือราชการ (`บัดนี้`, `ในวันที่`, `พนักงานเจ้าหน้าที่`, `เจ้าพนักงาน`, `ยกเลิกคำขอ`)
     - ส่งผลให้ที่อยู่จริงที่มีรหัสไปรษณีย์บนหน้าซองจดหมาย (`116 หมู่ที่ 9 ตำบลโพนทอง นพ 48170`) ชนะข้อความร่างจดหมาย 100%
   - **2. การสกัดที่อยู่ผู้ส่งจากหน้าซองจดหมาย (`parse_envelope_label`)**:
     - ปรับปรุงให้ `parse_envelope_label` ส่งคืนข้อมูล `SHIPPER NAME` และ `SHIPPER ADDRESS` ออกมาด้วย และนำมาผสานร่วมใน `process_pdf`
   - **3. การแปลงเลขไทยใน `parse_address_components`**:
     - ทำการแปลงเลขไทยเป็นเลขอารบิก (`clean_thai_digits`) ตั้งแต่เริ่มต้นฟังก์ชัน เพื่อให้เอกสารราชการที่ระบุรหัสไปรษณีย์เป็นเลขไทย เช่น `๔๘๑๗๐` สามารถแยกสกัดเป็น `48170` ได้อย่างถูกต้อง 100%
   - **4. ระบบ Fallback ข้อมูลผู้ส่งอัจฉริยะ (Dual Fallback)**:
     - ฝั่ง Backend (`convert_dpost.py`): หากสกัดรหัสไปรษณีย์ผู้ส่งไม่ได้ จะสแกนหาเลขไปรษณีย์ของที่ทำการ/สาขาในเอกสารทั้งหมดเป็น Fallback
     - ฝั่ง Frontend (`App.jsx` ใน `handleSendEparcel`):
       - `shipperZipcode`: `row.SHIPPER_ZIPCODE || currentPerson?.ResponsibleZipcode`
       - `shipperDistrict`: `row.SHIPPER_AMPHUR || currentPerson?.ResponsiblePostoffice?.replace(/^ปณ\./, '')`
       - `shipperProvince`: `row.SHIPPER_PROVINCE || (currentPerson?.Organization?.includes('นครพนม') ? 'นครพนม' : '')`
       - `shipperMobile`: `shipperTel || currentPerson?.TelContactPerson1`
     - รับประกันว่าข้อมูลผู้ส่งที่ส่งเข้าสู่ API e-Parcel (`/webservice/addItems`) จะมีรหัสไปรษณีย์ 5 หลักที่ถูกต้องและเปิดให้บริการเสมอ
   - **ผลลัพธ์**: ส่งข้อมูล e-Parcel สำเร็จ 100% ตอบกลับ Status 200 (Empty list `[]`) บันทึกสถานะ "✓ สำเร็จ" ในตาราง และอัปเดตสถานะคอลัมน์ E ใน Google Sheet UseBarcode เป็น `"yes"` โดยอัตโนมัติ


---

## 39. การปรับตำแหน่งตราสัญลักษณ์ e-AR, QR Code และ Barcode ให้อยู่ใต้ที่อยู่ผู้ฝากส่ง (Envelope Overlay Positioning Under Sender Address) (v2026.0916.2125)

### 39.1 ปัญหาที่พบ (Issue)
- เมื่อส่งออกไฟล์ PDF รวม หรือสร้างหน้าซองจดหมายจากไฟล์ PDF รูปแบบที่มีหน้าซองแบบผู้รับ (เช่น `แจ้งผู้ขอจดทะเบียน-ซอง.pdf`, `แจ้งผลสอบเขต-ซอง.pdf`) ตราประทับ e-AR, QR Code และ Barcode ตกไปอยู่ชิดขอบล่างสุดของหน้ากระดาษ A4 (`base_y = 40`) 
- ส่งผลให้เมื่อพับหรือตัดกระดาษเป็นซองจดหมาย บาร์โค้ดไม่ได้อยู่บนหน้าซอง แต่อยู่บนชายล่างหรือแถบพับด้านหลัง ทำให้เจ้าหน้าที่ไปรษณีย์ไม่สามารถสแกนบาร์โค้ดได้

### 39.2 สาเหตุเชิงเทคนิค (Technical Cause)
1. ในฟังก์ชัน `generate_combined_pdf` ของ `convert_dpost.py` การคำนวณพิกัด `base_x` และ `base_y` เดิม อ้างอิงจากตำแหน่งของคำว่า `"เรียน"` (`y_coord_rian` และ `x_coord_rian`)
2. เอกสารแจ้งสิทธิและแจ้งผลสอบเขตของกรมที่ดิน ใช้คำนำหน้าว่า `"ผู้รับ"` หรือพิมพ์ชื่อผู้รับโดยไม่มีคำว่า `"เรียน"` ทำให้ `y_coord_rian` เป็น `None`
3. เมื่อไม่พบคำว่า `"เรียน"` ระบบจะตกลงไปยังเงื่อนไขสำรอง (`else: base_x = width * 0.05, base_y = 40`) ซึ่งวางบาร์โค้ดไว้ที่ขอบล่างสุดของกระดาษ A4

### 39.3 แนวทางการแก้ไข (Solution)
1. **ตรวจจับพิกัดกลุ่มที่อยู่ผู้ฝากส่ง (Sender Address Block Detection)**:
   - ใน `visitor_body` เพิ่มการตรวจจับบรรทัดข้อความของส่วนราชการ/ผู้ส่งที่อยู่ฝั่งซ้ายของหน้า (`x < width * 0.40` และ `y > height * 0.35`) เช่น สำนักงานที่ดิน, ฝ่าย, ที่ นพ..., ตำบล, อำเภอ, รหัสไปรษณีย์
   - หาค่าพิกัดล่างสุดของบล็อกผู้ส่ง `sender_bottom = min(sender_y_list)` และขอบซ้าย `sender_left = min(sender_x_list)`
2. **จัดวางตราสัญลักษณ์ใต้ที่อยู่ผู้ฝากส่ง (Positioning Under Sender Address)**:
   - เมื่อไม่พบคำว่า `"เรียน"` แต่พบข้อมูลที่อยู่ผู้ส่ง:
     - กำหนด `base_y = sender_bottom - 18 - (210 * scale)` เพื่อให้ขอบบนของกล่อง e-AR เว้นระยะ 18pt ใต้บรรทัดเลขที่หนังสือ/ที่อยู่ผู้ส่งพอดี
     - กำหนด `base_x = max(18, sender_left)` เพื่อให้ขอบซ้ายของบาร์โค้ดตรงกับแนวข้อความผู้ส่ง
   - บาร์โค้ด e-AR จะปรากฏที่ช่วง `y ≈ 500 - 667 pt` บนหน้ากระดาษ A4 ขนานพอดีกับข้อมูลผู้รับทางขวา (`y ≈ 594 - 687 pt`)
3. **รองรับ Envelope Cropping (`envelope_only`)**:
   - ปรับการคำนวณ `ref_y` ของการตัดหน้าซอง ให้ใช้ `sender_bottom - 30` เป็น Fallback กรณีไม่พบคำว่า `"เรียน"` ทำให้ตัดหน้าซอง DL ได้อย่างถูกต้อง


---

## 40. การแสดงสถานะละเอียดย่อยใต้ป้ายสถานะหลักตามแนวทางแบบที่ 1 (Detailed Sub-Status Display & Search Enhancement) (v2026.0916.2215)

### 40.1 ความต้องการและปัญหาเดิม (Background & Problem)
- ในระบบเดิม รายงานการรับฝากพัสดุ (`DepositReportView` และ `DepositReportModal`) แสดงเฉพาะป้ายกลุ่มสถานะหลัก 4 แบบ คือ `อยู่ระหว่างการนำจ่าย` (สีส้ม), `นำจ่ายสำเร็จ` (สีเขียว), `รับฝากแล้ว` (สีฟ้า), และ `ส่งคืน` (สีแดง)
- เมื่อพัสดุเกิดเหตุติดขัดระหว่างนำจ่าย เช่น เจ้าหน้าที่ไปรษณีย์ไปส่งแล้วพบว่า "บ้านปิด", มีการ "ออกใบแจ้ง", "รอจ่าย ณ ที่ทำการไปรษณีย์", หรือ "จ่าหน้าไม่ชัดเจน" หน้าตารางจะแสดงเพียงว่า "อยู่ระหว่างการนำจ่าย" ทำให้ผู้ใช้งานไม่ทราบสาเหตุที่แท้จริงจนกว่าจะคลิกเปิดหน้าต่างประวัติสถานะ (Tracking Timeline)
- ในคู่มือมาตรฐานระบบ e-Parcel ของไปรษณีย์ไทย สถานะของระบบมีมากกว่า 20 รหัสสถานะและมีความหมายเฉพาะเจาะจง

### 40.2 แนวทางการปรับปรุงแบบที่ 1 (Chosen Solution - Option 1)
ผู้ใช้งานเลือกแนวทางการปรับปรุงแบบที่ 1 ซึ่งเป็นแนวทางที่เหมาะสมที่สุดทั้งด้าน Visual Design, UX และการใช้งานจริง:
1. **คงป้ายสถานะหลัก 4 กลุ่ม (Primary Status Badge)**:
   - รักษาสีและไอคอนของ 4 กลุ่มสถานะหลัก (`นำจ่ายสำเร็จ`, `อยู่ระหว่างการนำจ่าย`, `รับฝากแล้ว`, `ส่งคืน`) เพื่อให้การ์ดสรุปยอดด้านบน (Summary Cards) และแถบกรอง (Filter Tabs) ทำงานได้อย่างมีประสิทธิภาพและคุ้นเคย
2. **แสดงข้อความสถานะละเอียดย่อยใต้ป้าย (Detailed Sub-status underneath)**:
   - ตรวจสอบว่าข้อความสถานะย่อยจริง (`status_description_raw`) มีความเฉพาะเจาะจงแตกต่างจากป้ายสถานะหลักหรือไม่
   - หากมีข้อมูลเฉพาะเจาะจง (เช่น "บ้านปิด", "ออกใบแจ้ง", "ถึง ปณ.ปลายทาง เตรียมนำจ่าย", "รอจ่าย ณ ที่ทำการไปรษณีย์"):
     - กรณีเป็นข้อยกเว้น/ปัญหาการนำจ่าย (`/บ้านปิด|ออกใบแจ้ง|ไม่ชัดเจน|ไม่มีเลขบ้าน|ไม่ยอมรับ|ไม่มีผู้รับ|ไม่มารับตามกำหนด|รอจ่าย|ย้าย|เสียหาย|ระงับ|คืน/`): แสดงกล่องข้อความย่อยสีส้มสะดุดตา (`.status-subtext-alert`) พร้อมไอคอน `⚠️`
     - กรณีเป็นการเคลื่อนไหวตามปกติ: แสดงกล่องข้อความย่อยสีเทาคมชัด (`.status-subtext-transit`)
3. **เพิ่มประสิทธิภาพการค้นหา (Sub-status Search Filtering)**:
   - ปรับปรุงตรรกะตัวกรอง `filteredRecords` ในทั้ง `DepositReportView` และ `DepositReportModal` ให้ค้นหาข้อความใน `status_description_raw`, `status_description`, และ `status_label` ได้ทันที
   - ผู้ใช้งานสามารถพิมพ์ค้นหาคำว่า "บ้านปิด", "ออกใบแจ้ง", หรือ "รอจ่าย" ในช่อง Search เพื่อดึงรายการที่ต้องการจัดการเร่งด่วนได้ทันที
4. **ปรับปรุงการส่งออกเอกสาร Excel และ PDF**:
   - ในไฟล์ Excel (`generate_deposit_report_excel`): คอลัมน์ "สถานะ" แสดงเป็น `อยู่ระหว่างการนำจ่าย (บ้านปิด)` เมื่อมีสถานะย่อย
   - ในไฟล์ PDF (`generate_deposit_report_pdf`): เพิ่มคอลัมน์ "สถานะ" (รวม 10 คอลัมน์ ขนาดพอดีหน้า A4 แนวนอน) พร้อมแสดงชื่อสถานะหลักและสถานะย่อยด้วยสีที่สอดคล้องกับหน้าเว็บ


---

## 41. การย้ายเมนูออกจากระบบเข้าสู่เมนูผู้ใช้งานใต้ป้ายชื่อ (User Profile Dropdown Menu & Navbar Cleanup) (v2026.0916.2255)

### 41.1 ความต้องการและปัญหาเดิม (Background & Problem)
- ในแถบนำทางหลัก (Navbar) ด้านขวามือ เดิมมีปุ่มสีแดงส้ม "ออกจากระบบ" (`.btn-standalone-logout`) วางอยู่ถัดจากป้ายชื่อผู้ใช้งาน (`.user-profile-badge`) ตลอดเวลา
- ส่งผลให้แถบ Navbar ดูแน่นและมีปุ่มกระจัดกระจาย อีกทั้งผู้ใช้งานอาจเผลอกดปุ่มออกจากระบบโดยไม่ตั้งใจ
- ผู้ใช้งานต้องการให้แสดงเฉพาะป้ายชื่อผู้ใช้งาน (ตามรูปที่ 2: `[ 👤 DOL.Renunakhon / DOL ]`) และเมื่อคลิกที่ป้ายชื่อจึงจะแสดงเมนูผู้ใช้งานที่มีตัวเลือก "ออกจากระบบ"

### 41.2 แนวทางการปรับปรุง (Solution Architecture)
1. **Interactive User Profile Trigger**:
   - ปรับเปลี่ยน `.user-profile-badge` ให้เป็น `<button>` ที่สามารถคลิกโต้ตอบได้
   - เพิ่มไอคอน Chevron Down ขนาด 11px ที่จะหมุน 180° เมื่อเปิดเมนู เพื่อให้ผู้ใช้ทราบว่าป้ายชื่อสามารถกดเปิดเมนูได้
   - จัดวางโครงสร้างภายในคอนเทนเนอร์ `.user-profile-menu-container` พร้อมอ้างอิง `userMenuRef`
2. **User Profile Dropdown Menu (`.user-profile-dropdown`)**:
   - ออกแบบการ์ดเมนูดรอปดาวน์แบบ Glassmorphism สไตล์โมเดิร์น พร้อมเงาและแอนิเมชันเปิดที่นุ่มนวล
   - **Header Card**: แสดงไอคอนผู้ใช้ในกรอบ Gradient มรกต, ชื่อผู้ใช้งาน (`UserName`), ชื่อหน่วยงาน (`Organization`), และชิปบทบาท (`Status`) กับไปรษณีย์รับฝาก
   - **Action Item**: ปุ่ม "ออกจากระบบ" สไตล์พรีเมียม พร้อมไอคอนกล่องสีแดง และคำอธิบายย่อย ("ลงชื่อออกจากบัญชี {user}")
3. **Event Listeners & Accessibility**:
   - เพิ่ม `handleClickOutside` ตรวจจับการคลิกภายนอกเมนูเพื่อปิดดรอปดาวน์อัตโนมัติ
   - เพิ่ม `handleKeyDown` ดักจับปุ่ม `Escape` เพื่อปิดเมนูอย่างรวดเร็ว

## 42. การย้ายลิงก์บริการภายนอก DPost และ e-AR ขึ้นสู่แถบนำทางหลักข้างตรวจสอบพัสดุ (External Services Integration in Navbar Tabs) (v2026.0916.2315)

### 42.1 ความต้องการและปัญหาเดิม (Background & Problem)
- เดิมลิงก์บริการภายนอกของไปรษณีย์ไทย ได้แก่ **DPost** (`https://dpost.thailandpost.com`) และ **e-AR** (`https://e-ar.thailandpost.com/`) ถูกจัดเก็บไว้ในเมนูดรอปดาวน์แฮมเบอร์เกอร์ขวามือ
- ผู้ใช้งานต้องคลิกเปิดเมนูแฮมเบอร์เกอร์ก่อนจึงจะมองเห็นลิงก์ทั้งสอง ทำให้ไม่สะดวกในการสลับไปใช้งานระบบของไปรษณีย์ไทยบ่อยครั้ง
- ผู้ใช้ต้องการย้ายปุ่มลิงก์ทั้งสองมาแสดงต่อจากปุ่ม **ตรวจสอบพัสดุ** บนแถบนำทางหลัก (Navbar center tabs) เพื่อให้สามารถกดเปิดใช้งานได้ในคลิกเดียว

### 42.2 แนวทางการปรับปรุง (Solution Architecture)
1. **จัดวางบน Navbar Tabs (`.navbar-nav-tabs`)**:
   - เพิ่มเส้นคั่นแนวตั้ง `.nav-tab-divider` คั่นระหว่างปุ่มเมนูระบบภายใน ("ตรวจสอบพัสดุ") กับลิงก์ภายนอก เพื่อสร้างการแบ่งสัดส่วนที่ชัดเจน
   - เพิ่มแท็ก `<a>` คลาส `.nav-tab-btn.nav-tab-ext` พร้อมคุณสมบัติ `target="_blank"` และ `rel="noreferrer"`
   - แสดงไอคอนประจำระบบ: ไอคอนกล่องพัสดุสำหรับ DPost และไอคอนจดหมายสำหรับ e-AR
   - แสดงไอคอนลูกศรเฉียงชี้ขึ้นขวา (`↗` `.nav-tab-ext-icon`) เพื่อสื่อความหมายว่าเป็นลิงก์เปิดหน้าต่างภายนอก
2. **ปรับแต่งสไตล์และความสวยงาม (`Navbar.css`)**:
   - เส้นคั่นแนวตั้ง `.nav-tab-divider`: ความกว้าง 1px, สูง 18px, สีขาวโปร่งแสง รองรับทั้ง Light และ Dark mode
   - Hover Animation: ปุ่มและไอคอนลูกศรเฉียงขยับขึ้นเล็กน้อยเมื่อนำเมาส์ไปชี้ (`transform: translate(1px, -1px)`)
3. **ลบส่วนซ้ำซ้อนในเมนูแฮมเบอร์เกอร์**:
   - นำกลุ่มเมนู "บริการและระบบภายนอก" ออกจากเมนูแฮมเบอร์เกอร์ เพื่อลดความซ้ำซ้อนและทำให้เมนูแฮมเบอร์เกอร์กระชับคงเหลือเฉพาะคู่มือและการตั้งค่าที่จำเป็น

## 43. การรวมปุ่มบันทึกไฟล์และสร้างจ่าหน้าซองเป็นปุ่มเดียว "ดาวน์โหลดเอกสาร" พร้อมเมนูเลือก 4 ไฟล์ (Consolidated Document Download Hub) (v2026.0916.2345)

### 43.1 ความต้องการและปัญหาเดิม (Background & Problem)
- เดิมในแถบเครื่องมือดำเนินการ (`ActionToolbar`) มีปุ่มดาวน์โหลดเอกสารแยกกัน 2 ปุ่ม คือ **"บันทึกไฟล์"** (สีเขียว สำหรับดาวน์โหลด 3 ไฟล์: Excel, PDF รวม, ใบนำส่ง) และ **"สร้างจ่าหน้าซอง"** (สีม่วง สำหรับดาวน์โหลดซองจดหมาย)
- การมี 2 ปุ่มทำให้แถบเครื่องมือดูแน่น และผู้ใช้งานไม่สามารถเลือกดาวน์โหลดเฉพาะไฟล์ที่ต้องการได้ (เช่น หากต้องการเฉพาะไฟล์ Excel ต้องรอให้ระบบสร้างและดาวน์โหลดไฟล์ PDF รวม และใบนำส่งไปด้วย)
- ผู้ใช้งานต้องการให้รวมทั้งสองปุ่มเข้าเป็นปุ่มเดียวชื่อว่า **"ดาวน์โหลดเอกสาร"** และเมื่อคลิกที่ปุ่มนี้ จะสามารถเลือกดาวน์โหลดไฟล์แต่ละไฟล์ได้อย่างอิสระจากทั้งหมด 4 ไฟล์

### 43.2 สถาปัตยกรรมและการปรับปรุง (Solution Architecture)
1. **Consolidated Action Button (`.btn-download-docs`)**:
   - รวมปุ่ม `.btn-export-excel` และ `.btn-envelope` เข้าเป็นปุ่มเดียว: **"ดาวน์โหลดเอกสาร"**
   - ตกแต่งด้วยโทนสี Gradient เขียวมรกต-ทีล (`linear-gradient(135deg, #059669 0%, #0d9488 100%)`) พร้อมไอคอนดาวน์โหลดและลูกศร Chevron Down
   - สถานะความพร้อม (`canDownloadDocs`): ใช้งานได้เมื่อระบบส่งข้อมูล e-Parcel สำเร็จครบทุกรายการ (`allApiSuccess && !isProcessing`) ตรงตามเงื่อนไขเดิมของทั้งสองปุ่ม
2. **Interactive Dropdown Menu (`.download-docs-dropdown`)**:
   - เมื่อคลิกปุ่มจะเปิดการ์ดเมนูดรอปดาวน์ที่ลอยอยู่ใต้ปุ่มอย่างสวยงาม
   - รายการไฟล์ทั้ง 4 ชนิดพร้อมไอคอนประจำประเภทไฟล์และป้ายบอกนามสกุล:
     1. 📊 **1. ไฟล์ Excel (สำหรับ DPost)** (`.xlsx`): ข้อมูลนำเข้าสำหรับระบบ DPost ไปรษณีย์ไทย
     2. 📄 **2. ไฟล์ PDF (เอกสารพร้อมบาร์โค้ด)** (`.pdf`): เอกสารต้นฉบับพร้อมประทับตราและหมายเลขบาร์โค้ด
     3. 📋 **3. ไฟล์ PDF (ใบนำส่ง)** (`.pdf`): ใบนำส่งสิ่งของส่งทางไปรษณีย์สรุปรายการฝากส่ง
     4. ✉️ **4. ไฟล์ PDF (สร้างจ่าหน้าซอง)** (`.pdf`): ใบปะหน้าซองจดหมายติดซอง (รองรับทั้งรายการที่ติ๊กเลือก หรือทั้งหมด)
   - **Download All Option**: ปุ่ม **"ดาวน์โหลดทั้งหมด (ครบทั้ง 4 ไฟล์)"** ด้านล่างสุด สำหรับผู้ใช้งานที่ต้องการส่งออกเอกสารครบชุดพร้อมกันในคลิกเดียว
3. **UX & Accessibility Enhancements**:
   - ดักจับการคลิกภายนอก (Click Outside) และปุ่ม `Escape` เพื่อปิดเมนูดรอปดาวน์อัตโนมัติ
   - ซ่อน Tooltip ทับซ้อนเมื่อเปิดเมนู (`.dropdown-active::after { display: none }`)
   - รองรับ Responsive และ Dark Mode 100%

## 44. การปรับปรุงขนาดเมนูดาวน์โหลดเป็น Tooltip, จัดหมวดหมู่นำจ่ายถึงผู้รับแล้ว, และระบบตรวจสอบสถานะพัสดุหลายบรรทัดแบบย่อ-ขยาย (v2026.0917.0515)

### 44.1 การปรับขนาดความกว้างเมนูดาวน์โหลดเอกสารและเปลี่ยนคำอธิบายเป็น Tooltip
- **ปัญหาเดิม**: การ์ดเมนูในดรอปดาวน์ "ดาวน์โหลดเอกสาร" มีความกว้างตายตัว (360px) และแสดงข้อความคำอธิบาย 2 บรรทัด ส่งผลให้ชื่อไฟล์ยาวๆ เช่น "2. ไฟล์ PDF (เอกสารพร้อมบาร์โค้ด)" และป้าย `.PDF` ถูกตัดคำด้วยเครื่องหมาย `...` (Ellipsis) และกินพื้นที่แนวตั้งมากเกินไป
- **การปรับปรุง**:
  1. นำข้อความอธิบายแบบบรรทัดในบัตรออก (`.download-item-desc`) แล้วเปลี่ยนมาแสดงเป็น Native Tooltip (`title="..."`) เมื่อนำเมาส์ไปชี้ที่แต่ละบัตรแทน
  2. ปรับขนาดความกว้างของดรอปดาวน์ `.download-docs-dropdown` ให้ยืดหยุ่นตามเนื้อหาจริง (`min-width: 320px; width: max-content; max-width: 390px;`)
  3. ปรับ `.download-item-title` ให้เป็น `white-space: nowrap !important;` เพื่อให้ชื่อไฟล์และป้ายนามสกุลแสดงผลครบถ้วน สวยงาม กระชับ และไม่มีข้อความถูกตัดคำอีกต่อไป

### 44.2 การแก้ไขการจัดกลุ่มสถานะ "นำจ่ายถึงผู้รับแล้ว" เข้าระบบนำจ่ายสำเร็จ (Delivered Classification Fix)
- **ปัญหาเดิม**: ในรายงานสถานะการรับฝากและภาพรวมการนำจ่าย รายการพัสดุที่มีสถานะดิบจากไปรษณีย์ไทยว่า "นำจ่ายถึงผู้รับแล้ว" (รหัสสถานะ `4` หรือ `501`) ไม่ถูกนับรวมในช่อง "นำจ่ายสำเร็จ" แต่กลับตกไปอยู่ใน "อยู่ระหว่างการนำจ่าย" ทำให้ยอดนำจ่ายสำเร็จแสดงเป็น 0
- **การปรับปรุง**:
  1. ปรับปรุงฟังก์ชัน `classify_delivery_status` ใน `api/index.py` ให้ครอบคลุมคีย์เวิร์ด `"นำจ่ายถึงผู้รับแล้ว"`, `"ถึงผู้รับแล้ว"`, `"นำจ่ายสำเร็จ"`, และรหัส `"4"`, `"501"`, `"delivered"`
  2. ปรับปรุงฟังก์ชัน `getDeliveryStatusInfo` ใน `DepositReportView.jsx` ให้ตรวจสอบคำว่า `"นำจ่ายถึงผู้รับแล้ว"` ก่อนเป็นลำดับแรก พร้อมทั้งรองรับทั้งสระอำแบบรวมและแบบกระจายรูป
  3. ปรับปรุงการคำนวณสรุปผลยอดรวม (`deliveredCount`, `inTransitCount`, `returnedCount`, `receivedCount`) ให้ประเมินจากข้อมูลจริงทุกแถวแบบ Dynamic ทำให้สถิตินำจ่ายสำเร็จแสดงผลถูกต้องตรงกับความเป็นจริง

### 44.3 ระบบตรวจสอบสถานะพัสดุแบบหลายบรรทัดและการ์ดย่อ-ขยาย (Multiline Tracking Inquiry with Collapsible Cards)
- **ปัญหาเดิม**: หน้าตรวจสอบสถานะพัสดุรองรับการพิมพ์ค้นหาได้ครั้งละ 1 หมายเลขเท่านั้น และเมื่อค้นหาจะแสดงไทม์ไลน์ยาวทันที ทำให้ตรวจสอบหลายชิ้นได้ช้า
- **การปรับปรุง**:
  1. เปลี่ยนช่องค้นหาเป็น Multiline Textarea (`.tracking-barcode-textarea`) รองรับการพิมพ์หรือวางหมายเลขบาร์โค้ด 13 หลักได้หลายบรรทัดพร้อมกัน คั่นด้วยการขึ้นบรรทัดใหม่หรือเครื่องหมายจุลภาค
  2. มีระบบตรวจนับหมายเลขบาร์โค้ดอัตโนมัติ (`ตรวจพบ X หมายเลข`) พร้อมปุ่มล้างข้อความ และปุ่ม "+ ใส่ทั้งหมด (X หมายเลข)" จากงานปัจจุบัน
  3. ส่งคำค้นหาตรวจสอบแบบขนานด้วย `Promise.allSettled` รวดเร็วและไม่สะดุดหากมีหมายเลขใดหมายเลขหนึ่งค้นหาไม่พบ
  4. ผลลัพธ์แสดงเป็นการ์ดของแต่ละหมายเลข โดยเริ่มต้นในสถานะ **ย่อ (Collapsed)** แสดงเฉพาะข้อมูลสำคัญล่าสุด: หมายเลข, ชื่อผู้รับ, วันเวลาปรับปรุงล่าสุด, ที่ทำการ, ป้ายสถานะหลัก และสถานะย่อย
  5. เมื่อคลิกที่การ์ดหมายเลขใด จะกางขยาย (**Expanded**) ออกมาแสดงไทม์ไลน์จุดเช็กพอยต์ (Stepper Timeline) ครบทุกขั้นตอน
  6. มีปุ่ม "เปิดทั้งหมด" และ "ย่อทั้งหมด" ให้เลือกใช้งานได้อย่างสะดวกรวดเร็ว

## 45. การนำเมนูนำทางหน้าหลัก 3 รายการออกจากเมนู Hamburger (v2026.0917.0555)

### 45.1 ความต้องการและเหตุผล (Background & Problem)
- ในเมนูแฮมเบอร์เกอร์ขวามือเดิม มีกลุ่มเมนู "หน้าเมนูระบบ" ที่ประกอบไปด้วย 3 เมนู ได้แก่:
  1. **แปลงไฟล์ PDF** (`workspace`)
  2. **รายงานสถานะ** (`deposit-report`)
  3. **ตรวจสอบพัสดุ** (`tracking`)
- เนื่องจากทั้ง 3 เมนูนี้ได้ถูกย้ายขึ้นมาเป็นแท็บเมนูหลักตรงกลางของ Navbar ด้านบน (`.navbar-nav-tabs`) ซึ่งผู้ใช้งานสามารถมองเห็นและคลิกสลับหน้าได้ทันทีอยู่แล้ว การแสดงซ้ำในเมนูแฮมเบอร์เกอร์จึงเกิดความซ้ำซ้อนและทำให้พื้นที่เมนูแฮมเบอร์เกอร์ยาวเกินจำเป็น
- ผู้ใช้งานจึงต้องการให้นำทั้ง 3 เมนูนี้ออกจากเมนูแฮมเบอร์เกอร์

### 45.2 การปรับปรุง (Solution Architecture)
1. **นำการ์ดเมนูทั้ง 3 รายการออก (`Navbar.jsx`)**:
   - ลบปุ่มการ์ด `workspace` ("แปลงไฟล์ PDF"), `deposit-report` ("รายงานสถานะ"), และ `tracking` ("ตรวจสอบพัสดุ") ออกจากเมนูดรอปดาวน์แฮมเบอร์เกอร์
2. **ปรับปรุงส่วนที่เหลือให้กระชับและตรงจุดประสงค์**:
   - คงเหลือเฉพาะปุ่ม **"จัดการระบบ" (Admin)** (แสดงเฉพาะเมื่อเป็นผู้ดูแลระบบ) และปุ่ม **"About"** (เกี่ยวกับระบบ)
   - ปรับชื่อหัวข้อกลุ่มเมนูให้เหมาะสมเป็น **"ข้อมูลระบบ"** (หรือ **"การจัดการ & ข้อมูลระบบ"** สำหรับ Admin)
3. **ผลลัพธ์**:
   - เมนูแฮมเบอร์เกอร์มีความกระชับ สวยงาม สบายตา ไม่ซ้ำซ้อนกับแท็บนำทางหลักบน Navbar

## 46. คลังทักษะ สกิล สไตล์ และเทคนิคขั้นสูงในการพัฒนาระบบ C2DPost Web (Advanced Engineering & Style Codex) (v2026.0917.0845)

### 46.1 เทคนิคการออกแบบ Dynamic Intrinsic Sizing สำหรับเมนูดรอปดาวน์และการแทนที่ด้วย Tooltip
1. **การแก้ปัญหาข้อความล้น (Text Truncation)**:
   - การกำหนดความกว้างตายตัว เช่น `width: 360px` ทำให้ชื่อไฟล์ที่มีความยาวมากและมีป้ายแสดงนามสกุล (`.xlsx`, `.pdf`) ถูกตัดข้อความด้วย `...`
   - เทคนิคที่ถูกต้องคือการใช้ **Dynamic Intrinsic Sizing**:
     ```css
     min-width: 320px;
     width: max-content;
     max-width: 390px;
     ```
     ร่วมกับการกำหนด `white-space: nowrap !important;` บน `.download-item-title` และ `.download-item-title-row`
2. **การถ่ายทอดข้อความอธิบายสู่ Native Tooltip**:
   - การแสดงข้อความคำอธิบาย 2 บรรทัดในการ์ดทุกใบทำให้เมนูดูเทอะทะและสูงเกินจำเป็น
   - แก้ไขโดยตัดองค์ประกอบ `.download-item-desc` ออก แล้วนำข้อความคำอธิบายทั้งหมดไปผูกไว้กับคุณลักษณะ `title="..."` บนแท็ก `<button>` ของแต่ละไฟล์
   - ผู้ใช้จะเห็นหน้าต่างเมนูที่กะทัดรัด สะอาดตา แต่ยังคงสามารถนำเมาส์ไปชี้เพื่ออ่านรายละเอียดเต็มได้อย่างสมบูรณ์

### 46.2 เทคนิคการจัดการ Unicode สระอำของภาษาไทยและการจัดลำดับความสำคัญของสถานะไปรษณีย์
1. **การแก้ปัญหา Decomposed Thai Vowels**:
   - ฐานข้อมูลหรือ Web Service รุ่นเก่าของไปรษณีย์ไทยมักส่งสระอำ (`ำ`) ในรูปอักขระผสม 2 ตัว คือ *นิคหิต* (`ํ`) + *สระอา* (`า`) แทนที่จะเป็นสระอำเดี่ยว (`ำ`)
   - ทำให้การใช้เงื่อนไขตรวจสอบคำ เช่น `if "นำจ่าย" in text` เกิดผลลัพธ์เป็นเท็จ
   - มาตรการบังคับ: ต้องทำการแปลงกลับด้วย `.replace("ํา", "ำ")` เสมอก่อนการเปรียบเทียบข้อความ
2. **ลำดับการตรวจสอบสถานะ (Status Evaluation Precedence)**:
   - ไปรษณีย์ไทย e-Parcel กำหนดให้รหัส `"4"` พร้อมข้อความ `"นําจ่ายถึงผู้รับแล้ว"` เป็นสถานะการนำจ่ายสำเร็จอย่างเป็นทางการ
   - ในการเขียน Regular Expression หรือเงื่อนไขตรวจสอบ ต้องวางการตรวจสอบสถานะนำจ่ายสำเร็จไว้เป็นลำดับแรกสุด ก่อนการตรวจสอบสถานะอยู่ระหว่างการนำจ่าย เนื่องจากข้อความ "นำจ่ายถึงผู้รับแล้ว" มีคำว่า "นำจ่าย" แฝงอยู่ หากตรวจสอบอยู่ระหว่างการนำจ่ายก่อน จะทำให้สถานะถูกจำแนกผิดพลาด

### 46.3 เทคนิคการสืบค้นสถานะพัสดุแบบ Multiline Batch ด้วย Promise.allSettled และการ์ดแบบ Accordion
1. **การแยกวิเคราะห์ข้อมูลนำเข้าหลายบรรทัด**:
   - ใช้ Regular Expression `input.split(/[
,;]+/)` ในการตัดคำ รองรับทั้งการเว้นบรรทัด, การคั่นด้วยเครื่องหมายจุลภาค, หรืออัฒภาค พร้อมทั้งตัดช่องว่างและแปลงเป็นตัวพิมพ์ใหญ่อัตโนมัติ
2. **การประมวลผลแบบขนานที่ไม่บล็อกการทำงาน (Non-blocking Parallel Fetching)**:
   - ใช้ `Promise.allSettled` ในการส่งคำร้องขอข้อมูลสถานะทุกหมายเลขพร้อมกัน เพื่อให้ระบบตอบสนองอย่างรวดเร็วที่สุด และหากมีหมายเลขใดหมายเลขหนึ่งค้นหาไม่พบ หรือเชื่อมต่อขัดข้อง จะไม่กระทบต่อการแสดงผลของหมายเลขอื่น
3. **การออกแบบการ์ดแบบ Accordion Stepper**:
   - **สถานะย่อ (Collapsed - ค่าเริ่มต้น)**: แสดงเฉพาะข้อมูลสรุปที่สำคัญที่สุด ได้แก่ หมายเลขบาร์โค้ด, ข้อมูลคู่เทียบผู้รับ/โฉนด, เวลาล่าสุด, ที่ทำการ, ป้ายสถานะหลัก และป้ายสถานะย่อย (พร้อมไอคอนแจ้งเตือน `⚠️` สำหรับกรณีพิเศษ เช่น "บ้านปิด" หรือ "ออกใบแจ้ง")
   - **สถานะกางขยาย (Expanded - เมื่อคลิก)**: กางแสดง Stepper Timeline ในแนวตั้ง พร้อมไอคอนเครื่องหมายถูก (`✓`) สำหรับขั้นตอนรับฝาก และเลขลำดับขั้นตอน พร้อมรายละเอียดสถานที่ วันเวลา และผู้ลงนามรับของ

### 46.4 เทคนิคการผูกโปรเจกต์และการดีพลอยผ่าน Vercel CLI
1. **ข้อจำกัดชื่อโปรเจกต์ตัวพิมพ์เล็ก**:
   - Vercel กำหนดให้ชื่อโปรเจกต์ต้องเป็นตัวพิมพ์เล็กทั้งหมด หากโฟลเดอร์ในเครื่องมีตัวพิมพ์ใหญ่ (เช่น `C2DPost_web`) การรัน `vercel link --yes` จะเกิดข้อผิดพลาด 400 Bad Request
2. **คำสั่งผูกโปรเจกต์ที่ถูกต้อง**:
   - ต้องระบุชื่อโปรเจกต์ปลายทางอย่างชัดเจน:
     ```bash
     npx vercel link --project c2dpost-web --yes
     ```
   - จากนั้นสามารถสั่งดีพลอยสู่ Production ได้ทันทีด้วย:
     ```bash
     npx vercel --prod --yes
     ```
