# แผนงานและเช็กลิสต์พัฒนาระบบรายงานการรับฝาก (Deposit Report Checklist)
> **โปรเจกต์**: C2DPost Web Edition  
> **เป้าหมาย**: เพิ่มระบบดึงข้อมูลและรายงานตรวจสอบรายการรับฝากจากไปรษณีย์ไทย (e-Parcel Web Service)  
> **สถานะ**: ✅ เสร็จสมบูรณ์ครบถ้วน 100% ทุกรายการตามแผนงาน (All Features & Parity Complete)

---

## 📌 ภาพรวมขั้นตอนการทำงาน (Phased Roadmap)

```
[Phase 1: Backend API] ➔ [Phase 2: UI Dashboard] ➔ [Phase 3: Export Excel/PDF] ➔ [Phase 4: Timeline & Polish]
```

---

## 🚀 Phase 1: การเตรียมโครงสร้างพื้นฐานและการเชื่อมต่อ API (API & Data Pipeline)
- [x] **1.1 กำหนดค่าการเชื่อมต่อ (Config & Credentials)**
  - กำหนดตัวแปรสำหรับเก็บ Username / Password และ Endpoint ของ `r_dservice.thailandpost.com`
  - ตรวจสอบความปลอดภัย ไม่ให้รหัสผ่านรั่วไหลไปยังฝั่ง Browser
- [x] **1.2 สร้าง Backend Endpoint บน Python Serverless (`api/index.py`)**
  - สร้าง Endpoint `/api/reports/received` รับ Parameter `date` (`DD/MM/YYYY`)
  - เชื่อมต่อ API `GET https://r_dservice.thailandpost.com/webservice/getAllOrderReceived?date=:date` ผ่าน Basic Authentication
  - ดักจับ Error กรณีเชื่อมต่อไม่สำเร็จ หรือไม่มีข้อมูลในวันนั้น
- [x] **1.3 Data Normalization & Data Cleansing**
  - แปลงฟิลด์ข้อมูลจาก e-Parcel ให้เป็นมาตรฐานเดียวกัน (เช่น `barcode`, `customerName`, `receivedDate`, `status`, `productWeight`)
  - แปลงวันที่และเวลาให้อยู่ในฟอร์แมตภาษาไทยที่อ่านง่าย

---

## 💻 Phase 2: หน้าต่างแสดงผลรายงานบนเว็บ (Web UI & Interactive Modal)
- [x] **2.1 เมนูปุ่มเปิดรายงาน (Navbar / ActionToolbar)**
  - เพิ่มปุ่มไอคอน **"📋 รายงานรับฝาก"** บนแถบเมนูด้านบน พร้อม Tooltip
- [x] **2.2 คอมโพเนนต์หน้าต่างรายงาน (`DepositReportModal.jsx`)**
  - ตัวเลือกวันที่ (DatePicker) ย้อนหลัง/ปัจจุบัน
  - ปุ่มกด **"ดึงข้อมูลรับฝาก"** พร้อมไฟแสดงสถานะกำลังโหลด (Loading Spinner)
- [x] **2.3 การ์ดสรุปยอดภาพรวม (Executive Stat Cards)**
  - 📦 ยอดรวมรายการทั้งหมด
  - 🟢 รับฝากสำเร็จ (ชิ้น และ %)
  - ⏳ รอยืนยันเข้าระบบ
  - ⚖️ น้ำหนักรวม (กรัม/กก.) & ค่าบริการรวม
- [x] **2.4 ตารางรายการข้อมูล (Report Data Grid)**
  - ตารางแสดง: ลำดับ, วัน-เวลารับฝาก, เลขบาร์โค้ด, ผู้รับ, ปณ.รับฝาก, น้ำหนัก, สถานะ
  - ระบบค้นหาด่วน (Search Box) ค้นหาจากชื่อหรือเลขบาร์โค้ด
  - แท็บกรองสถานะ: `ทั้งหมด`, `🟢 รับฝากแล้ว`
- [x] **2.5 ออกแบบ UI & ธีม (Design System & Dual Theme)**
  - รองรับทั้ง **Light Mode** (คลีน ขาว คอนทราสต์สูง) และ **Dark Mode** (Slate Glassmorphism) 100%

---

## 📄 Phase 3: การส่งออกรายงาน (Exporting Excel & PDF)
- [x] **3.1 ส่งออกตาราง Excel (.xlsx) สำหรับกระทบยอด**
  - ใช้ `openpyxl` ออกแบบตารางรายงานค่าใช้จ่ายประจำวัน
  - มีหัวตารางสีสวยงาม ฟอนต์อ่านง่าย
  - ใส่สูตร Auto-SUM คำนวณยอดรวมน้ำหนักและค่าจัดส่งล่างสุด สำหรับฝ่ายการเงิน/บัญชี
- [x] **3.2 ส่งออกรายงาน PDF (.pdf) สำหรับงานราชการ**
  - ใช้ `reportlab` ออกแบบรายงานทางการ มีหัวหนังสือราชการ, ชื่อ สนง.ที่ดิน, วันที่
  - ตารางสรุปรายการพร้อมช่องลงนามเจ้าหน้าที่ผู้ส่งมอบและผู้รับฝากไปรษณีย์
  - ใช้ฟอนต์ภาษาไทย Tahoma/Prompt 100% ไม่เพี้ยนบน Cloud Serverless
- [x] **3.3 ปุ่มดาวน์โหลดแบบแยกไฟล์ (One-Click Downloads)**
  - ปุ่ม `[📊 ดาวน์โหลด Excel]` และ `[📄 ดาวน์โหลด PDF]` บนหน้าต่างรายงาน

---

## 🔍 Phase 4: ประวัติสถานะเชิงลึกและปรับแต่งสมบูรณ์แบบ (Advanced Features & Polish)
- [x] **4.1 ดูประวัติสถานะรายชิ้น (Tracking Timeline Modal)**
  - เมื่อคลิกที่แถว สามารถดึงไทม์ไลน์สถานะจริงจาก `getHistoryStatus` มาแสดงเป็น Stepper
- [x] **4.2 Auto-Reconcile กับตารางหน้าแรก**
  - สามารถกดตรวจสอบรายการที่เพิ่งแปลงไฟล์ PDF ไปว่า ชิ้นไหนไปรษณีย์รับฝากแล้วบ้างแบบไฮไลต์แถว
- [x] **4.3 บันทึกประวัติและเลขเวอร์ชัน**
  - อัปเดตเลขเวอร์ชันของเว็บ (`APP_VERSION`) เป็น `v2026.0914.2046` ตามกฎ `vYYYY.MMDD.HHMM` (Section 15)
- [x] **4.21 แก้ไขการคลิกดูสถานะพัสดุรายชิ้น (Tracking Timeline Modal Fix) (`v2026.0915.0947`)** — แก้ไข Bug การลืม Import Hook `useMemo` ใน `TrackingTimelineModal.jsx` ซึ่งทำให้เกิด ReferenceError เมื่อคลิกหมายเลข Barcode และเพิ่มการจำลองไทม์ไลน์สถานะพัสดุใน `_build_demo_tracking` ให้ตรงตามสถานะจริงของพัสดุแต่ละรายการ (นำจ่ายสำเร็จ, ส่งคืน, อยู่ระหว่างการนำจ่าย) พร้อมแสดงผล Stepper ไทม์ไลน์ได้อย่างราบรื่น 100%
- [x] **4.4 Deploy ขึ้น Production** — `vercel --prod --yes` สำเร็จ (aliased → `https://c2dpost-web.vercel.app`)
- [x] **4.5 โชว์ข้อผิดพลาดของ API จริงใน UI** — เพิ่มแบนเนอร์แจ้งเตือน `api_notice` (Amber Warning) และแบนเนอร์โหมดจำลอง `is_mock` (Sky Info) ทั้งบนหน้าตาราง Reconcile, Tracking Timeline Stepper และ Deposit Report Modal แสดงผลสวยงามทั้ง Light/Dark Mode 100%
- [x] **4.6 ปรับแต่ง UI ตาราง (Table Polish)** — นำคอลัมน์ "การรับฝาก" ออกจากตารางหลัก เพื่อให้พื้นที่การแสดงผลที่อยู่และชื่อผู้รับกว้างขึ้น ชัดเจนขึ้น โดยยังคงแถบแบนเนอร์สรุปและระบบไฮไลต์แถวสีเขียว (.reconciled-row) เมื่อตรวจสอบรับฝากไว้ครบถ้วน
- [x] **4.7 บันทึกประวัติ UseBarcode (Google Sheet)** — เชื่อมต่อการดึงหมายเลขบาร์โค้ดทุกครั้ง ให้บันทึกประวัติลง `UseBarcode` (แท็บ `Barcode`) โดยบันทึก `Timestamp`, `UserName`, `Barcode`, `Details` พร้อมบันทึกสรุปยอดและประวัติผู้ใช้งานแบบเดียวกับโปรแกรม Python Desktop 100%
- [x] **4.8 อัปเดตสถานะคอลัมน์ E "ส่งข้อมูล e-Parcel" เป็น "yes"** — เมื่อกดปุ่ม "ส่งข้อมูล e-Parcel" สำเร็จ ระบบจะค้นหาแถวของหมายเลขบาร์โค้ดนั้นในชีต `UseBarcode` และบันทึกค่าลงคอลัมน์ E เป็น "yes" ทันทีแบบอัตโนมัติ
- [x] **4.9 ปรับปรุง UI/UX สู่ Page-Based Navigation (`v2026.0914.2046`)** — ปรับโครงสร้างระบบหน้าเว็บให้กดแต่ละเมนูแล้วเปิดเป็นหน้านั้นๆ แบบ Full Page (แปลงไฟล์ PDF, รายงานรับฝาก, ตรวจสอบพัสดุ, จัดการระบบ Admin) พร้อมตัดปุ่ม "ซิงก์กับตารางหลัก" ออกตามคำขอ และทำการ Auto-Sync อัตโนมัติในเบื้องหลัง
- [x] **4.21 แก้ไขการคลิกดูสถานะพัสดุรายชิ้น (Tracking Timeline Modal Fix) (`v2026.0915.0947`)** — แก้ไข Bug การลืม Import Hook `useMemo` ใน `TrackingTimelineModal.jsx` ซึ่งทำให้เกิด ReferenceError เมื่อคลิกหมายเลข Barcode และเพิ่มการจำลองไทม์ไลน์สถานะพัสดุใน `_build_demo_tracking` ให้ตรงตามสถานะจริงของพัสดุแต่ละรายการ (นำจ่ายสำเร็จ, ส่งคืน, อยู่ระหว่างการนำจ่าย) พร้อมแสดงผล Stepper ไทม์ไลน์ได้อย่างราบรื่น 100%
- [x] **4.10 ระบบเลือกช่วงวันที่ (Date Range) และการแบ่งหน้าตาราง (Pagination 20 รายการ/หน้า) (`v2026.0914.2105`)** — เพิ่มฟังก์ชันเลือกตั้งแต่วันที่ถึงวันที่ พร้อมปุ่มลัด (วันนี้, เมื่อวาน, 7 วันล่าสุด, เดือนนี้), ดึงข้อมูลคู่ขนานด้วย ThreadPoolExecutor และแบ่งหน้าตารางรายงานหน้าละ 20 รายการพร้อมตัวควบคุมเลขหน้าสมบูรณ์แบบ
- [x] **4.11 ความสูงตารางแบบ Active Dynamic Height (`v2026.0914.2119`)** — ยกเลิกข้อจำกัด max-height: 600px ของตารางรายงานรับฝาก ปรับให้ความสูงของตารางขยายและปรับตามจำนวนข้อมูลจริงในแต่ละหน้าโดยตรง (Active Height) แถวไม่ถูกตัดขาดครึ่ง และไม่มี Scrollbar แนวตั้งซ้อนภายในกล่องตาราง
- [x] **4.12 ระบบจัดกลุ่มและแสดงสถานะนำจ่าย 3 มิติ (`v2026.0914.2135`)** — เพิ่มการแสดงสถานะ "นำจ่ายสำเร็จ" (เขียว Emerald), "ส่งคืน" (แดง Rose), และจัดกลุ่มสถานะระหว่างทางทั้งหมดให้แสดงเป็น "อยู่ระหว่างการนำจ่าย" (ส้ม Amber) ทั้งบนการ์ดสถิติ 5 ช่อง, แท็บตัวกรอง 4 แท็บ, ป้ายสถานะตารางข้อมูลพร้อม Tooltip, และรายงานส่งออก Excel/PDF
- [x] **4.13 แสดงข้อมูลวัน-เวลา และสถานที่ในไทม์ไลน์ติดตามสถานะพัสดุ (`v2026.0914.2155`)** — รองรับฟิลด์ `statusDate` และ `station` จาก Thailand Post `getHistoryStatus` แสดงวัน-เวลาและสถานที่ในแต่ละขั้นตอนของ Stepper, ป้ายอัปเดตล่าสุดใน Header ผลลัพธ์, และป้ายสถานะ 3 มิติในหน้าต่างตรวจสอบพัสดุและ Modal
- [x] **4.14 รองรับแบบพิมพ์เอกสาร ท.ด. 38 ค. (เอกสารแจ้งการปักหลักเขตที่ดิน) (`v2026.0914.2200`)** — เพิ่ม ท.ด. 38 ค. ในหน้าต่างเกี่ยวกับระบบ (About Modal) ต่อจาก ท.ด. 38 และอัปเกรด Regex และ Fallback ใน `convert_dpost.py` ให้ตรวจจับเอกสารรหัส ท.ด. 38 ค. ได้อย่างถูกต้อง
- [x] **4.15 ปรับเปลี่ยนข้อความปุ่มดึงข้อมูลเป็น "อัปเดตข้อมูล" (`v2026.0914.2205`)** — เปลี่ยนข้อความปุ่มค้นหา/ดึงรายงานจาก `ดึงข้อมูลรับฝาก` เป็น `อัปเดตข้อมูล` ทั้งในหน้ารายงานรับฝากแบบเต็มจอและ Modal
- [x] **4.16 คลิกหมายเลข Barcode เพื่อตรวจสอบสถานะพัสดุ (`v2026.0914.2215`)** — ทำให้หมายเลข Barcode ในตารางรายงานรับฝาก (ทั้ง Full Page และ Modal) และตาราง Workspace หลักเป็นปุ่ม Interactive ที่คลิกได้เพื่อเปิดดูประวัติสถานะพัสดุและไทม์ไลน์การนำส่งในหน้าต่าง TrackingTimelineModal ได้ทันที พร้อมปุ่มเปิดหน้าตรวจสอบเต็มรูปแบบ
- [x] **4.17 ปรับเปลี่ยนชื่อแท็บเมนูเป็น "รายงานสถานะ" (`v2026.0914.2220`)** — เปลี่ยนข้อความบนแท็บ Navbar และเมนูจาก `รายงานรับฝาก` เป็น `รายงานสถานะ` พร้อมปรับหัวข้อหน้าเป็น `รายงานสถานะไปรษณีย์ (e-Parcel Status Report)` ให้ตรงกับบริบทการติดตามสถานะครบวงจร
- [x] **4.18 จัดเรียงการ์ดสรุปยอดสถานะและตัวกรองใหม่ พร้อมแสดงค่า % ในการ์ดส่งคืน (`v2026.0915.0931`)** — จัดเรียงลำดับการ์ดสถิติ 6 ช่องใหม่ตามลำดับ: 1. รับฝากแล้ว, 2. อยู่ระหว่างการนำจ่าย, 3. นำจ่ายสำเร็จ, 4. ส่งคืน (เพิ่มการคำนวณและแสดงผล % อัตโนมัติ), 5. รายการทั้งหมด, 6. ยอดรวมค่าบริการ ทั้งบนหน้ารายงานสถานะเต็มจอ (DepositReportView) และหน้าต่าง Modal (DepositReportModal) พร้อมจัดเรียงแท็บตัวกรองให้ตรงกันอย่างสมบูรณ์แบบ
- [x] **4.19 จดจำค่าและสถานะล่าสุดของหน้ารายงานสถานะ (State & Cache Persistence) (`v2026.0915.0931`)** — เพิ่มระบบบันทึกสถานะทั้งหมดของหน้ารายงานสถานะ (ช่วงวันที่, ผลข้อมูลที่ดึงมา, สรุปสถิติ, แท็บตัวกรอง, คำค้นหา, และเลขหน้า) ลงใน sessionStorage อัตโนมัติ เพื่อให้เมื่อผู้ใช้สลับไปหน้าอื่น (เช่น แปลงไฟล์ PDF หรือ ตรวจสอบพัสดุ) แล้วกลับมา จะสามารถดูข้อมูลและสถานะล่าสุดที่ดูค้างไว้ได้ทันทีแบบ 0ms ไม่ต้องรอโหลดใหม่
- [x] **4.20 แสดงข้อมูลสถานะล่าสุดของแต่ละหมายเลขพัสดุ (Latest Status Date & Station Tracking) (`v2026.0915.0938`)** — ปรับเปลี่ยนการแสดงผลในตารางรายงานสถานะ (ทั้ง DepositReportView และ DepositReportModal) จากคอลัมน์เดิม "วัน-เวลารับฝาก" และ "ปณ.รับฝาก" ให้เป็น "วัน-เวลาล่าสุด" และ "ปณ./สถานที่ล่าสุด" พร้อมดึง `latest_date` และ `latest_station` มาแสดงผลอัตโนมัติ และอัปเกรดการส่งออกรายงาน Excel/PDF ให้สอดคล้องกัน 100%
- [x] **4.21 แก้ไขการคลิกดูสถานะพัสดุรายชิ้น (Tracking Timeline Modal Fix) (`v2026.0915.0947`)** — แก้ไข Bug การลืม Import Hook `useMemo` ใน `TrackingTimelineModal.jsx` ซึ่งทำให้เกิด ReferenceError เมื่อคลิกหมายเลข Barcode และเพิ่มการจำลองไทม์ไลน์สถานะพัสดุใน `_build_demo_tracking` ให้ตรงตามสถานะจริงของพัสดุแต่ละรายการ (นำจ่ายสำเร็จ, ส่งคืน, อยู่ระหว่างการนำจ่าย) พร้อมแสดงผล Stepper ไทม์ไลน์ได้อย่างราบรื่น 100%
- [x] **4.22 ระบบซิงก์และอัปเดตวัน-เวลา และสถานที่สถานะล่าสุดแบบเรียลไทม์ (Live Tracking Auto-Enrichment & Sync Parity) (`v2026.0915.1025`)** — แก้ไขปัญหาข้อมูลวัน-เวลา และ ปณ. ในตารางแสดงแค่วันเวลารับฝากเดิม โดยการเพิ่ม Auto-Enrichment ดึงข้อมูลประวัติการนำจ่ายล่าสุด (`getHistoryStatus`) แบบขนานผ่าน ThreadPoolExecutor สูงสุด 35 รายการอัตโนมัติเมื่อดึงรายงาน, เพิ่มปุ่ม "ซิงก์สถานะล่าสุด" สำหรับดึงข้อมูลเรียลไทม์เป็นชุด (`/api/reports/batch-tracking`), และเชื่อมโยง Callback `onTrackingUpdated` ให้เมื่อผู้ใช้งานคลิกเปิดดูไทม์ไลน์รายชิ้น ข้อมูลวัน-เวลาล่าสุด สถานที่ และสถานะในตารางจะถูกอัปเดตทันทีพร้อมบันทึกลงแคชอัตโนมัติ
- [x] **4.23 นำปุ่ม "กลับหน้าแปลงไฟล์" ออกจากส่วนหัวของหน้ารายงานสถานะและหน้าตรวจสอบพัสดุ (`v2026.0915.1029`)** — ตัดปุ่ม `btn-back-to-workspace` (← กลับหน้าแปลงไฟล์) ออกจาก Header ของ `DepositReportView` และ `TrackingInquiryView` เพื่อลดความซ้ำซ้อนและทำให้หน้าเว็บสะอาดตา โดยผู้ใช้งานสามารถสลับหน้าจอผ่าน Navigation Bar ด้านบนได้โดยตรงแบบ 1-คลิก
- [x] **4.24 รวมการดึงข้อมูลสถานะล่าสุดและซ่อนปุ่มซิงก์ที่ซ้ำซ้อน (Consolidated Auto-Enrichment & Button Cleanup) (`v2026.0915.1032`)** — รวมระบบการดึงข้อมูลสถานะพัสดุสดทั้งหมดไว้ภายใต้ปุ่มเดียวคือ "อัปเดตข้อมูล" โดยระบบจะทำการ Auto-Enrich ดึงประวัติสถานะล่าสุดแบบขนานทันที พร้อมซ่อนปุ่ม "ซิงก์สถานะล่าสุด" ออกเพื่อไม่ให้เกิดความสับสนและซ้ำซ้อนใน UI
- [x] **4.25 เพิ่มปุ่มลัดเลือกช่วงเวลา "เดือนที่แล้ว" (Last Month Date Shortcut Preset) (`v2026.0915.1044`)** — เพิ่มปุ่มลัด `[ เดือนที่แล้ว ]` ถัดจาก `[ เดือนนี้ ]` ทั้งบนหน้ารายงานสถานะเต็มจอ (`DepositReportView`) และหน้าต่าง Modal (`DepositReportModal`) พร้อมฟังก์ชันคำนวณวันแรกและวันสิ้นสุดของเดือนก่อนหน้าอัตโนมัติ (รองรับการข้ามปีปฏิทิน) และจัดรูปแบบ Flex-wrap เพื่อการแสดงผลที่สวยงามในทุกขนาดหน้าจอ
- [x] **4.26 นำสัญลักษณ์สกุลเงิน ฿ ออกจากค่าบริการ (Currency Symbol ฿ Removal) (`v2026.0915.1051`)** — ปรับปรุงการแสดงผลตัวเลขค่าบริการทั้งในการ์ดสรุปยอดรวมค่าบริการ, เซลล์ค่าบริการในตารางข้อมูล, เอกสารส่งออก Excel และ PDF โดยตัดสัญลักษณ์ `฿` ออก เหลือเพียงตัวเลขทศนิยม 2 ตำแหน่งตามมาตรฐานงานสารบรรณภาครัฐ
- [x] **4.27 รองรับการลากและวางไฟล์ (Drag & Drop PDF Files) (`v2026.0915.1105`)** — เพิ่มระบบลากและวางไฟล์ PDF ได้อย่างอิสระครอบคลุมทั้งหน้าจอ Workspace, แถบเครื่องมือเลือกไฟล์ (`ActionToolbar`), และตารางแสดงข้อมูล (`PreviewGrid`) พร้อมหน้าต่างจำลองการวางไฟล์แบบ Glassmorphic Drop Overlay และระบบป้องกัน Browser Tab Hijack (ป้องกันบราว์เซอร์เปิดไฟล์ PDF แทนหน้าเว็บ) รวมทั้งทำให้พื้นที่ว่างของตาราง ("ยังไม่มีข้อมูลในระบบ") เป็นจุดคลิกและวางไฟล์แบบ Interactive ทันที
- [x] **4.28 ปรับขนาดพื้นที่ Drop ไฟล์ให้พอดีกับกรอบตารางอย่างสมบูรณ์แบบ (Drop Zone Full-Frame Fit) (`v2026.0915.1118`)** — ปรับปรุงโครงสร้าง Layout ของ `.table-wrapper` และ `.empty-table-state` ใน PreviewGrid ให้ขยายความสูงแบบ `flex: 1` และ `min-height: 380px` เต็มพื้นที่กรอบการ์ด พร้อมแสดงเส้นขอบประสีเขียวมรกตโปร่งแสงที่โอบล้อมพอดีกรอบอย่างสมดุล ไม่เว้นช่องว่างด้านล่างอีกต่อไป
- [x] **4.29 รองรับเอกสารแจ้งผลสอบเขตและซองจดหมายราชการ (Envelope & Land Notice Parser Upgrade) (`v2026.0915.1145`)** — แก้ไขปัญหาการดึงข้อมูลจากไฟล์ PDF รูปแบบใหม่ "แจ้งผลสอบเขต-ซอง.pdf" และ "แจ้งผู้ขอจดทะเบียน-ซอง.pdf" ที่มีข้อมูลสระอำกระจายตัว (Decomposed Thai vowels) และใช้หน้าซองแบบผู้รับ (Inverted layout) โดยเพิ่มระบบ Normalize สระและช่องว่างภาษาไทย, เพิ่มฟังก์ชัน `parse_envelope_label` ดึงข้อมูลผู้รับ ที่อยู่ อำเภอ จังหวัด รหัสไปรษณีย์ และเลขที่อ้างอิงจากหน้าซองอย่างแม่นยำ 100%, ป้องกันการสับสนเลขโฉนดที่ดินเป็นรหัสไปรษณีย์ และแก้ไขข้อผิดพลาด Code: 027 (Parameter amphur must not be null or blank) ของ e-Parcel API สำเร็จ
- [x] **4.30 แก้ไขการส่งข้อมูล e-Parcel ข้อผิดพลาด Code 073 และ 017 (Shipper Postcode & e-Parcel Submission Fix) (`v2026.0916.2105`)** — แก้ไขปัญหาการส่งข้อมูล e-Parcel ไม่สำเร็จในไฟล์ "แจ้งผู้ขอจดทะเบียน-ซอง.pdf" ที่ระบบตอบกลับ "Code: 073, Detail: Postcode ยังไม่เปิดให้บริการ | Code: 017, Detail: Please check your input Zipcode or Prodinbox or Barcode" สาเหตุจากระบบสกัดข้อมูลดึงข้อความเนื้อหาจดหมายบนหน้าแรกมาเป็นที่อยู่ผู้ส่งแทนที่อยู่จริงบนหน้าซอง ทำให้รหัสไปรษณีย์ผู้ส่ง (SHIPPER_ZIPCODE) ว่างเปล่า โดยได้ปรับปรุงระบบคัดกรองที่อยู่ผู้ส่ง (Smart Address Quality Scoring) ป้องกันข้อความร่างจดหมายปะปน, ดึงข้อมูลที่อยู่และรหัสไปรษณีย์ผู้ส่งจากหน้าซอง (Envelope Shipper Integration), และเพิ่มระบบ Fallback ที่อยู่/รหัสไปรษณีย์ผู้ส่งจากบัญชีผู้ใช้งานที่ล็อกอิน (ResponsibleZipcode / ResponsiblePostoffice) ทั้งฝั่ง Backend และ Frontend ส่งผลให้การส่งข้อมูล e-Parcel สำเร็จ 100% สมบูรณ์แบบ
- [x] **4.31 ปรับตำแหน่งตราสัญลักษณ์ e-AR, QR Code และ Barcode ให้อยู่ใต้ที่อยู่ผู้ฝากส่ง (Envelope Overlay Positioning Under Sender Address) (`v2026.0916.2125`)** — ปรับปรุงระบบประทับตราบนหน้าซองจดหมาย (`generate_combined_pdf`) ในไฟล์ที่ไม่มีคำว่า "เรียน" (เช่น เอกสารแจ้งผู้ขอจดทะเบียนและแจ้งผลสอบเขตที่ใช้คำว่า "ผู้รับ") จากเดิมที่ระบบตกเข้า Fallback ทำให้รูปบาร์โค้ดตกไปอยู่ชิดขอบล่างสุดของหน้ากระดาษ A4 ให้ตรวจจับพิกัดกลุ่มข้อความที่อยู่ผู้ฝากส่ง (`sender_bottom` และ `sender_left`) และจัดวางตรา e-AR, QR Code และ Barcode ไว้ใต้ที่อยู่ผู้ฝากส่ง (ด้านซ้ายของหน้าซอง) อย่างสมดุลและสวยงาม พร้อมปรับขนาดและระยะห่างไม่ให้ทับซ้อนกับข้อมูลผู้รับทางขวา รวมทั้งรองรับการตัดหน้าซองแบบ `envelope_only` อย่างสมบูรณ์แบบ
- [x] **4.32 แสดงสถานะละเอียดย่อยใต้ป้ายสถานะหลักตามแนวทางแบบที่ 1 (Detailed Sub-Status Display & Filter Enhancement) (`v2026.0916.2215`)** — ปรับปรุงการแสดงผลสถานะการนำจ่ายพัสดุในหน้ารายงานการรับฝาก (`DepositReportView`), หน้าต่างโมดอลรายงาน (`DepositReportModal`), หน้าต่างไทม์ไลน์ (`TrackingTimelineModal`), และหน้าค้นหาสถานะ (`TrackingInquiryView`) ให้แสดงป้ายสถานะหลัก 4 กลุ่มเดิม (นำจ่ายสำเร็จ, อยู่ระหว่างการนำจ่าย, รับฝากแล้ว, ส่งคืน) เพื่อคงสีและระบบกรองแถบหลักไว้ พร้อมแสดงข้อความสถานะละเอียดย่อยจริงใต้ป้าย (เช่น `⚠️ บ้านปิด`, `ออกใบแจ้ง`, `ถึง ปณ.ปลายทาง เตรียมนำจ่าย`, `รอจ่าย ณ ที่ทำการไปรษณีย์`, ฯลฯ) โดยเน้นสไตล์ Alert สีส้มสำหรับข้อยกเว้นการนำจ่าย, เพิ่มช่องค้นหา (Search Filter) ให้ค้นหาด้วยข้อความสถานะย่อยได้ทันที, และปรับปรุงการส่งออกรายงาน Excel และ PDF ให้ระบุข้อความสถานะย่อยอย่างครบถ้วนตามคู่มือ e-Parcel ของไปรษณีย์ไทย
- [x] **4.33 ย้ายปุ่มออกจากระบบเข้าสู่เมนูผู้ใช้งานใต้ป้ายชื่อ (User Profile Dropdown Menu & Navbar Cleanup) (`v2026.0916.2255`)** — ปรับปรุงแถบนำทาง (Navbar) ให้สะอาด สบายตา และเป็นไปตามมาตรฐาน UX สากล โดยยกเลิกปุ่มออกจากระบบแบบปุ่มเดี่ยว (`btn-standalone-logout`) ที่อยู่ถัดจากป้ายชื่อผู้ใช้งาน แล้วเปลี่ยนป้ายข้อมูลผู้ใช้งาน (`.user-profile-badge`) ให้เป็นปุ่มคลิกเปิดเมนูผู้ใช้งาน (User Profile Dropdown Menu) พร้อมลูกศร Chevron เมื่อคลิกจะแสดงการ์ดข้อมูลส่วนตัวของผู้ใช้งาน (ชื่อ, หน่วยงาน, บทบาท, ปณ. ที่รับผิดชอบ) และปุ่ม "ออกจากระบบ" พร้อมไอคอนสีแดงสวยงาม และระบบปิดเมนูอัตโนมัติเมื่อคลิกพื้นที่ภายนอกหรือกดปุ่ม Escape
- [x] **4.34 ย้ายลิงก์ระบบภายนอก DPost และ e-AR ขึ้นสู่แถบนำทางหลักข้างตรวจสอบพัสดุ (External Services Integration in Navbar Tabs) (`v2026.0916.2315`)** — ย้ายลิงก์บริการภายนอกของไปรษณีย์ไทย 2 ลิงก์ ได้แก่ DPost (เว็บอัปโหลดข้อมูลฝากส่งไปรษณีย์) และ e-AR (ตรวจใบตอบรับอิเล็กทรอนิกส์) จากเดิมที่ซ่อนอยู่ในเมนูแฮมเบอร์เกอร์ นำมาจัดวางเป็นปุ่มเมนูบนแถบนำทางหลัก (`.navbar-nav-tabs`) โดยตรง ต่อจากปุ่ม "ตรวจสอบพัสดุ" พร้อมเส้นคั่นแนวตั้ง (`.nav-tab-divider`) เพื่อแบ่งแยกหมวดหมู่ระบบภายในกับบริการภายนอกอย่างชัดเจน พร้อมไอคอนลิงก์ภายนอก (`↗`) ที่ขยับเล็กน้อยเมื่อ Hover และเปิดแท็บใหม่อย่างสะดวก รวดเร็ว พร้อมลบลิงก์ซ้ำซ้อนออกจากเมนูแฮมเบอร์เกอร์
- [x] **4.35 รวมปุ่มบันทึกไฟล์และสร้างจ่าหน้าซองเป็นปุ่มเดียว "ดาวน์โหลดเอกสาร" พร้อมเมนูเลือกดาวน์โหลด 4 ไฟล์ (Consolidated Document Download Hub) (`v2026.0916.2345`)** — รวมปุ่ม "บันทึกไฟล์" และ "สร้างจ่าหน้าซอง" เข้าเป็นปุ่มเดียวชื่อ "ดาวน์โหลดเอกสาร" (`.btn-download-docs`) พร้อมเมนูดรอปดาวน์สไตล์โมเดิร์น (`.download-docs-dropdown`) ที่แสดงรายการเอกสารให้ผู้ใช้งานเลือกดาวน์โหลดได้อย่างยืดหยุ่นครบทั้ง 4 ไฟล์: (1) ไฟล์ Excel สำหรับ DPost (.xlsx), (2) ไฟล์ PDF เอกสารพร้อมบาร์โค้ด (.pdf), (3) ไฟล์ PDF ใบนำส่ง (.pdf), (4) ไฟล์ PDF สร้างจ่าหน้าซอง (.pdf) พร้อมปุ่ม "ดาวน์โหลดทั้งหมด (ครบทั้ง 4 ไฟล์)" สำหรับผู้ใช้งานที่ต้องการดาวน์โหลดครบชุดในคลิกเดียว พร้อมระบบตรวจจับการคลิกภายนอกและปุ่ม Escape เพื่อปิดเมนูอย่างราบรื่น


---

## ✅ งานทั้งหมดเสร็จสมบูรณ์ 100% (Completed Tasks & Extended Enhancements)

- [x] ~~**4.6 สร้าง Git commit แรก + int remote ของโฟลเดอร์แม่**~~ — *(ยกเลิกตามความประสงค์ของผู้ใช้งาน: โฟลเดอร์ `C2DPost_web` แยก Git Repository อิสระและ push ขึ้น GitHub สมบูรณ์แล้ว)*
- [x] **4.45 เผยแพร่ส่วนขยาย C2DPost Helper v1.1.0 ขึ้น Chrome Web Store (`v2026.0917.1430`)** — อัปโหลดไฟล์แพ็กเกจ `C2DPost_Helper_v1.1.0_WebStore.zip` ขึ้นแดชบอร์ด Google Chrome Web Store Developer Console เพื่อส่งตรวจสอบ (Review) และอนุมัติการอัปเดตอัตโนมัติให้ผู้ใช้งานเดิมเรียบร้อยแล้ว
- [x] **4.36 ปรับขนาดเมนูดาวน์โหลดเป็น Tooltip, แก้ไขสถานะนำจ่ายถึงผู้รับแล้ว, และระบบค้นหาพัสดุหลายบรรทัดแบบย่อ-ขยาย (`v2026.0917.0515`)**
  - ปรับความกว้าง `.download-docs-dropdown` เป็น `min-width: 320px; width: max-content; max-width: 390px;` และเปลี่ยนคำอธิบายเป็นการ์ด Tooltip (`title="..."`)
  - อัปเดต `classify_delivery_status` และ `getDeliveryStatusInfo` ให้จัดหมวดหมู่ `"นำจ่ายถึงผู้รับแล้ว"` (code 4/501) เป็น `delivered` ("นำจ่ายสำเร็จ") พร้อมคำนวณยอดรวมถูกต้อง
  - พัฒนาระบบค้นหาพัสดุ `TrackingInquiryView` รองรับข้อความหลายบรรทัด (Multiline Textarea), ตรวจสอบพร้อมกันแบบขนาน (Parallel Fetch), แสดงการ์ดย่อแสดงเฉพาะสถานะล่าสุด และคลิกเพื่อกางดูไทม์ไลน์

- [x] **4.37 นำเมนูแปลงไฟล์ PDF, รายงานสถานะ และตรวจสอบพัสดุออกจากเมนู Hamburger (`v2026.0917.0555`)**
  - นำ 3 เมนูหลักที่ซ้ำซ้อนกับแท็บด้านบนออกจากดรอปดาวน์แฮมเบอร์เกอร์ใน `Navbar.jsx`
  - ปรับชื่อกลุ่มเมนูที่เหลือเป็น "ข้อมูลระบบ" (หรือ "การจัดการ & ข้อมูลระบบ" สำหรับ Admin) ให้กระชับ สวยงาม และไม่ซ้ำซ้อน

- [x] **4.38 เพิ่มพูนสกิล สไตล์ เทคนิค และแนวปฏิบัติขั้นสูงสู่คู่มือระบบและ SKILL (`v2026.0917.0845`)**
  - บันทึกเทคนิค Dynamic Intrinsic Sizing และ Tooltip Delegation สำหรับ Action Menu
  - บันทึกมาตรการจัดการ Unicode สระอำของภาษาไทย และลำดับความสำคัญของสถานะไปรษณีย์
  - บันทึกสถาปัตยกรรม Multiline Batch Tracking ด้วย `Promise.allSettled` และ Accordion Stepper Cards
  - บันทึกคู่มือการผูกโปรเจกต์และการดีพลอย Vercel CLI อย่างเป็นทางการ

- [x] **4.39 ส่งข้อมูล Username และ Password ไปแสดงและกรอกอัตโนมัติในบริการภายนอก DPost และ e-AR (Cross-Origin Credential Bridge & Auto-fill Assistant) (`v2026.0917.0910`)**
  - พัฒนาระบบส่งผ่านข้อมูล Username และ Password ของผู้ใช้งานที่เข้าสู่ระบบ ไปยังหน้าเข้าสู่ระบบของบริการภายนอก 2 ลิงก์บน Navbar (DPost และ e-AR)
  - อัปเกรด Chrome Extension `C2DPost Helper` สู่เวอร์ชัน `1.1.0` เพิ่ม Content Script `external_autofill.js` และ `host_permissions` สำหรับ `https://dpost.thailandpost.com/*` และ `https://e-ar.thailandpost.com/*`
  - รองรับการกรอกข้อมูลอัตโนมัติบน DOM ของ DPost (`#txtUsername`, `#txtPassword`) และ React Controlled Inputs ของ e-AR (`input[name="username"]`, `input[name="password"]`) ผ่าน Native Property Setter เพื่ออัปเดต React State ได้อย่างสมบูรณ์แบบ
  - ซิงก์ข้อมูลเข้าสู่ระบบอัตโนมัติเมื่อผู้ใช้ล็อกอิน สลับผู้ใช้ หรือคลิกลิงก์บริการภายนอก และล้างข้อมูลเมื่อออกจากระบบ
  - อัปเดตและบีบอัดไฟล์ส่วนเสริม `c2dpost-extension.zip` สำหรับให้ผู้ใช้งานดาวน์โหลดและติดตั้ง

- [x] **4.40 ปรับเป็นระบบ 1-คลิกเปิดเว็บปลายทางพร้อมกรอกข้อมูลอัตโนมัติ 100% โดยไม่ต้องกดผ่านป๊อปอัป (Zero-Modal Direct Seamless Autofill) (`v2026.0917.0925`)**
  - นำหน้าต่าง `CredentialAssistantModal` ออกจาก Flow การคลิกลิงก์ DPost และ e-AR บนแถบนำทาง (Navbar) ทั้งหมด 100% เพื่อประสบการณ์ใช้งานที่ลื่นไหลไร้รอยต่อ
  - เมื่อผู้ใช้งานคลิกปุ่ม DPost หรือ e-AR ระบบจะซิงก์ข้อมูล Username และ Password ไปยัง Extension ทันที และเปิดแท็บใหม่ไปยังเว็บไซต์ปลายทางทันทีแบบ 1-คลิก ไม่มีการเปิดหน้าต่าง Modal มาขัดจังหวะ
  - ปรับปรุง `external_autofill.js` ในส่วนเสริมให้ตรวจจับ `chrome.storage.onChanged` ดักจับข้อมูลที่อัปเดตล่าสุด และกรอกข้อมูลลงช่องอินพุตบน DPost และ e-AR ทันที พร้อมแสดงป้ายยืนยันสีเขียว "⚡ C2DPost Helper • กรอกข้อมูลสำเร็จ" ที่มุมขวาล่างของเว็บปลายทาง

- [x] **4.41 จัดเก็บไฟล์ C2DPost_Helper_v1.1.0_WebStore.zip ในโปรเจกต์ C2DPost_web และแทนที่ไฟล์ดาวน์โหลดเดิม (`v2026.0917.1020`)**
  - นำไฟล์แพ็กเกจ `C2DPost_Helper_v1.1.0_WebStore.zip` มาจัดเก็บไว้ในโปรเจกต์ `C2DPost_web/` ทั้งในโฟลเดอร์ Root (`C2DPost_Helper_v1.1.0_WebStore.zip`) และโฟลเดอร์สำหรับเผยแพร่ดาวน์โหลด (`public/` และ `dist/`)
  - อัปเดตและแทนที่ไฟล์ `c2dpost-extension.zip` เดิมด้วยไฟล์เวอร์ชัน 1.1.0 ใหม่ล่าสุดทันที เพื่อให้ลิงก์ดาวน์โหลดเดิมดาวน์โหลดได้เวอร์ชันใหม่อัตโนมัติ
  - อัปเดตปุ่มดาวน์โหลดในหน้า `ExtensionGate.jsx` ให้ระบุเวอร์ชัน `v1.1.0 (พร้อมระบบกรอกข้อมูลอัตโนมัติ)` และดาวน์โหลดไฟล์ `C2DPost_Helper_v1.1.0_WebStore.zip` โดยตรง
  - คอมไพล์และ Deploy ขึ้นสู่ Vercel Production สำเร็จ 100%



- [x] **4.42 แสดงเวอร์ชันส่วนขยายที่ติดตั้งบนป้ายสถานะแบบเรียลไทม์ (Installed Extension Version Status Chip) (`v2026.0917.1105`)**
  - พัฒนาระบบตรวจจับและส่งต่อหมายเลขเวอร์ชันของส่วนขยาย Chrome Extension จาก `content.js` ผ่าน DOM Attribute (`data-c2dpost-version`), Custom Event (`C2DPOST_EXTENSION_READY`) และ Handshake Ping-Pong (`C2DPOST_PONG`) เข้าสู่ `extensionBridge.js`
  - เชื่อมโยง State `extensionVersion` ใน `App.jsx` และส่งต่อไปยัง `<Navbar />` แบบเรียลไทม์
  - อัปเดตป้ายสถานะ Extension Helper ในเมนูดรอปดาวน์แฮมเบอร์เกอร์ให้แสดงหมายเลขเวอร์ชันที่ติดตั้งอย่างชัดเจน เช่น `• ติดตั้งแล้ว (v1.1.0)` (หรือแสดง `• ติดตั้งแล้ว (v1.0.0)` สำหรับรุ่นเดิม และ fallback `ไม่พบ` หากยังไม่ได้ติดตั้ง)
  - เพิ่ม CSS `white-space: nowrap;` และ `flex-shrink: 0;` บน `.status-badge-chip` เพื่อป้องกันข้อความเวอร์ชันล้นตกบรรทัด
  - คอมไพล์โปรเจกต์ `npm run build` และ Deploy ขึ้นสู่ Vercel Production สำเร็จเรียบร้อย

- [x] **4.43 แสดงเวอร์ชันของระบบด้านล่างธีมการแสดงผลในเมนูดรอปดาวน์ (System Version Pill Under Theme Display) (`v2026.0917.1335`)**
  - เพิ่มการแสดงผลแถบป้ายเวอร์ชันระบบ (`.drawer-footer-version` และ `.drawer-version-pill`) จัดวางไว้ด้านล่างส่วน "ธีมการแสดงผล" ในเมนูดรอปดาวน์แฮมเบอร์เกอร์ของ `Navbar.jsx`
  - แสดงข้อความเวอร์ชันระบบเต็มรูปแบบตามดีไซน์ About: `C2DPost Web Edition v2026.0917.1335 — ส่วน ทข.ปข.10`
  - ออกแบบสไตล์ Pill โค้งมนสวยงามตามมาตรฐาน Glassmorphism รองรับทั้ง Light Mode (`#f1f5f9`) และ Dark Mode (`rgba(15, 23, 42, 0.6)`) พร้อม Micro-interaction เมื่อนำเมาส์ไปชี้ (Hover effect) และคลิกเพื่อเปิดหน้าต่าง About ได้ทันที
  - คอมไพล์โปรเจกต์และ Deploy ขึ้นสู่ Vercel Production สำเร็จ 100%

- [x] **4.44 แก้ไขปัญหาดึงข้อมูลไฟล์ซองจดหมาย 25.pdf ไม่ได้ (Abbreviated Title Regex & Tambon Preservation) (`v2026.0917.1415`)**
  - วิเคราะห์สาเหตุที่ไฟล์ `25.pdf` (ในโฟลเดอร์ `รอบ4`) ดึงข้อมูลไม่สำเร็จ: ไฟล์นี้เป็นหน้าซองจดหมายเดี่ยวที่ไม่มีคำว่า "เรียน" และชื่อผู้รับขึ้นต้นด้วยคำนำหน้าแบบย่อ `"น.ส.ภัครดา ธงยศ"` ซึ่งระบบเดิมตรวจจับเฉพาะคำว่า "นางสาว", "นาย", "นาง" ทำให้ไม่พบชื่อผู้รับและตีกลับเป็นไฟล์ไม่สำเร็จ
  - ขยาย Regex ตรวจจับคำนำหน้าชื่อไทยให้ครอบคลุมทั้งแบบย่อและแบบเต็ม: `น.ส.`, `ด.ช.`, `ด.ญ.`, ยศทหาร/ตำรวจ (`ด.ต.`, `พ.ต.`, `ร.ต.`, ฯลฯ), คำนำหน้าวิชาชีพ (`นพ.`, `พญ.`, `ดร.`), บรรดาศักดิ์ (`ม.ร.ว.`, `ม.ล.`) และพระสงฆ์
  - เพิ่ม Fallback ตรวจจับชื่อ-นามสกุลไทยอัตโนมัติแม้ไม่มีคำนำหน้าชื่อ
  - แก้ไขจุดบกพร่องการลบชื่อตำบล: เพิ่ม Negative Lookbehind `(?<!ต\.)(?<!ต\.\s)(?<!ตำบล\s)(?<!ตำบล)` ป้องกันการลบชื่อตำบลในกรณีที่ชื่อตำบลซ้ำกับชื่ออำเภอหรือจังหวัด (เช่น `ต.มุกดาหาร` ใน อ.เมืองมุกดาหาร จ.มุกดาหาร)
  - อัปเดตทั้งใน `C2DPost_web/api/core/convert_dpost.py` และ `C2DPost_Python/convert_dpost.py` พร้อมทดสอบกับไฟล์ตัวอย่างทุกรอบผ่านฉลุย และ Deploy สู่ Vercel Production สำเร็จ

- [x] **4.46 ปรับปรุงคอลัมน์สถานะให้แสดงข้อมูลแบบมินิมอล สะอาดตา ไร้กล่องซ้ำซ้อน (Minimal Clean Status Column & Selective Alert Pill) (`v2026.0917.1445`)**
  - ปรับการแสดงผลในคอลัมน์ "สถานะ" ของหน้า `DepositReportView`, โมดอล `DepositReportModal`, และการ์ดย่อ `TrackingInquiryView` ให้เรียบง่าย สบายตา และเป็นระเบียบตามแนวคิด Minimal UI Design
  - กรณีสถานะปกติ เช่น "นำจ่ายสำเร็จ", "รับฝากแล้ว" หรือ "อยู่ระหว่างการนำจ่าย" ทั่วไป จะแสดงเพียงป้ายหลักมน (Pill) สวยงามป้ายเดียว ไม่แสดงกล่องข้อความย่อยซ้ำซ้อน (เช่น ไม่แสดง "นำจ่ายถึงผู้รับแล้ว" หรือขั้นตอนศูนย์คัดแยกยาวๆ ให้รกตา)
  - กรณีมีข้อยกเว้น/ปัญหาในการนำจ่าย (เช่น `⚠️ บ้านปิด`, `⚠️ ออกใบแจ้ง`, `⚠️ รอจ่าย ณ ที่ทำการไปรษณีย์`, `⚠️ ย้าย`, `⚠️ ไม่มีผู้รับ`) ระบบจะตรวจจับและแสดงป้ายแจ้งเตือนสีส้มมนสไตล์ Alert Tag (`.status-subtext-alert`) ด้านล่างอย่างชัดเจนและกระชับ
  - ข้อมูลสถานะละเอียดยังคงเปิดอ่านได้ครบถ้วน 100% ผ่าน Tooltip เมื่อนำเมาส์ไปชี้ และแสดงในไทม์ไลน์เมื่อคลิกดูรายละเอียดพัสดุ
  - ค้นหาผ่านช่อง Search Bar ยังคงกรองได้ทั้งคำว่า "บ้านปิด" หรือข้อความสถานะย่อยได้อย่างแม่นยำ
  - คอมไพล์โปรเจกต์ `npm run build` และ Deploy ขึ้นสู่ Vercel Production สำเร็จเรียบร้อย

- [x] **4.47 สลับตำแหน่งคอลัมน์ "น้ำหนัก" และ "ค่าบริการ" มาแสดงก่อนคอลัมน์ "วัน-เวลาล่าสุด" (Reorder Weight & Fee Columns Before Latest Timestamp) (`v2026.0917.1455`)**
  - ปรับลำดับคอลัมน์ในตารางรายงานการรับฝากทั้งบนหน้าแดชบอร์ดหลัก `DepositReportView` และหน้าต่างโมดอล `DepositReportModal`
  - ย้ายคอลัมน์ "น้ำหนัก" และ "ค่าบริการ" จากเดิมที่อยู่ด้านหลังสุดถัดจาก "ปณ./สถานที่ล่าสุด" นำมาจัดวางต่อจาก "ที่อยู่ปลายทาง" ทันที เพื่อให้อยู่ก่อนคอลัมน์ "วัน-เวลาล่าสุด" ตามที่ผู้ใช้งานต้องการ
  - ปรับลำดับคอลัมน์และสูตรคำนวณแถวสรุปผลรวม (Summary Row) ในการส่งออกรายงาน Excel (`.xlsx`) และ PDF (`.pdf`) ให้ตรงตามลำดับบนหน้าเว็บ 100%
  - อัปเดตทั้งใน `C2DPost_web/api/core/convert_dpost.py` และ `C2DPost_Python/convert_dpost.py` เพื่อ Parity ครบถ้วน
  - คอมไพล์โปรเจกต์ `npm run build` และ Deploy ขึ้นสู่ Vercel Production สำเร็จเรียบร้อย

- [x] **4.48 แสดงรหัสไปรษณีย์ต่อท้ายชื่อที่ทำการไปรษณีย์/สถานที่ล่าสุด (Append Postal Code to Latest Station & Location Column) (`v2026.0917.1505`)**
  - พัฒนาโมดูลและฟังก์ชันอัจฉริยะ `formatStationWithZipcode` ใน `src/utils/postalUtils.js` (Web UI) และ `format_station_with_zipcode` ใน `convert_dpost.py` (Excel & PDF Export)
  - เพิ่มรหัสไปรษณีย์ 5 หลัก ต่อท้ายชื่อที่ทำการไปรษณีย์หรือสถานที่ล่าสุดในคอลัมน์ "ปณ./สถานที่ล่าสุด" โดยอัตโนมัติ เช่น:
    - `เรณูนคร` ➔ `เรณูนคร 48170`
    - `นาแก` ➔ `นาแก 48130`
    - `ศป. หาดใหญ่` ➔ `ศป. หาดใหญ่ 90110`
    - `ศป. นครพนม` ➔ `ศป. นครพนม 48000`
    - `บัวใหญ่` ➔ `บัวใหญ่ 30120`
  - รองรับอัลกอริทึมการระบุรหัสไปรษณีย์อย่างแม่นยำ 6 ระดับ:
    1. **Existing Zipcode**: หากข้อความเดิมมีรหัสไปรษณีย์ 5 หลักอยู่แล้ว จะคงเดิมไว้ไม่ใส่ซ้ำ
    2. **Mail Centers (ศูนย์ไปรษณีย์ ศป. ทั่วประเทศ)**: แมปศูนย์ไปรษณีย์หลักทุกภูมิภาค (หาดใหญ่ 90110, เด่นชัย 54110, นครราชสีมา 30000, ขอนแก่น 40000, ฯลฯ)
    3. **Origin Office**: หากตรงกับที่ทำการรับฝากต้นทาง (ปณ.เรณูนคร หรือที่ทำการรับฝากของหน่วยงาน) จะเติมรหัสไปรษณีย์ต้นทาง `48170`
    4. **Destination Office & Address Matching**: วิเคราะห์จับคู่ชื่ออำเภอ/ที่อยู่ปลายทางของผู้รับ หรือเมื่อพัสดุถึงขั้นตอนนำจ่ายสำเร็จ/ออกใบแจ้ง จะผูกกับรหัสไปรษณีย์ปลายทางของผู้รับ (`receiver_zipcode`) โดยตรง
    5. **Comprehensive District Dictionary**: ฐานข้อมูลรหัสไปรษณีย์อำเภอสำคัญทั้งใน จ.นครพนม, มุกดาหาร, สกลนคร, บึงกาฬ, นครราชสีมา, กรุงเทพฯ และปริมณฑล
    6. **Fallback**: แสดงชื่อสถานที่เดิมหากไม่พบข้อมูลรหัสไปรษณีย์
  - แสดงผลครอบคลุมทุกมุมมอง: ตารางแดชบอร์ดหลัก `DepositReportView`, หน้าต่างโมดอล `DepositReportModal`, ไฟล์รายงาน Excel (`.xlsx`), และรายงานทางการ PDF (`.pdf`)
  - คอมไพล์โปรเจกต์ `npm run build` และ Deploy ขึ้นสู่ Vercel Production สำเร็จ 100%

- [x] **4.49 แสดงชื่อผู้รับและผู้ลงนามเมื่อนำจ่ายสำเร็จในไทม์ไลน์และประวัติสถานะพัสดุ (Display Delivered Recipient Name & Signer in Tracking Stepper) (`v2026.0917.1515`)**
  - พัฒนาระบบดึงข้อมูลชื่อผู้รับในขั้นตอน "นำจ่ายถึงผู้รับแล้ว" / "นำจ่ายสำเร็จ" (Step 6 หรือขั้นตอนสุดท้ายของการนำจ่าย) ในหน้าต่างโมดอลไทม์ไลน์ `TrackingTimelineModal` และการ์ดติดตาม `TrackingInquiryView`
  - รองรับการแสดงผลชื่อผู้รับแบบ 2 ลำดับชั้น
  - เพิ่มป้ายกำกับมน `[นำจ่ายสำเร็จ]` สไตล์ `.delivered-badge` สีเขียวมรกตในส่วนหัวของขั้นตอนสุดท้าย
  - ปรับปรุงการแสดงผลสถานที่ในไทม์ไลน์ให้แสดงรหัสไปรษณีย์ 5 หลักด้วย `formatStationWithZipcode`
  - อัปเดตการดึงข้อมูลบน Backend `api/index.py` (`_parse_tracking_events`) ให้รองรับคีย์ `signatureName`, `receiverName`, `customerName`
  - คอมไพล์โปรเจกต์ `npm run build` และ Deploy ขึ้นสู่ Vercel Production สำเร็จ 100%

- [x] **4.50 แยกแสดงผู้รับตามจ่าหน้าและสถานะผู้เซ็นรับจริงพร้อมลิงก์ตรวจสอบ e-AR ในไทม์ไลน์อย่างแม่นยำ (Strict Distinct Addressee vs. Real Signer with e-AR Direct Link) (`v2026.0917.1610`)**
  - จากการตรวจสอบคู่มืออย่างละเอียดในโฟลเดอร์ `คู่มือ API Standard preload Data on e-Parcel` พบว่าระบบ e-Parcel ของไปรษณีย์ไทยออกแบบฟิลด์ `signature` สำหรับชื่อผู้เซ็นรับจริง แยกคนละฟิลด์กับ `customerName` (ชื่อผู้รับตามจ่าหน้า) แต่ในการปฏิบัติงานจริงของ ปณท เจ้าหน้าที่สแกนให้ผู้รับเซ็นภาพลายเซ็นบนเครื่องพกพาเข้าสู่ระบบ e-AR โดยไม่ได้พิมพ์ชื่อเป็นข้อความ Text ส่งมาใน e-Parcel (ทำให้ `signature` ใน e-Parcel เป็นค่าว่าง `""`)
  - เพื่อความถูกต้องชัดเจนตามความต้องการของผู้ใช้งาน (แนวทางที่ 2):
    1. **บรรทัดที่ 1: `👤 ผู้รับตามจ่าหน้า: [ชื่อตามจ่าหน้า/คำขอ]`** แสดงชื่อปลายทางที่ระบุไว้ในเอกสารชัดเจน
    2. **บรรทัดที่ 2: `✍️ ผู้เซ็นรับจริง: [ชื่อจาก signature]`** (หาก ปณ. มีส่งชื่อมา) หรือหาก ปณ. ไม่ได้ส่งชื่อพิมพ์มา จะแสดงข้อความระบุชัดเจนว่า **`(ไม่พบชื่อพิมพ์ในระบบ e-Parcel / ลายเซ็นอยู่ในระบบ e-AR)`** พร้อมลิงก์สีน้ำเงินไฮไลต์ **`ดูภาพลายเซ็นใน e-AR ↗`** ให้คลิกเปิดระบบ e-AR เพื่อตรวจสอบภาพลายเซ็นจริงได้ทันที
  - ปรับแต่งการแสดงผลทั้งในโมดอล `TrackingTimelineModal` และหน้าสืบค้นพัสดุ `TrackingInquiryView` ให้มีดีไซน์สวยงาม รองรับทั้ง Light Theme และ Dark Theme
  - ผ่านการทดสอบ `npm run build` สำเร็จ 100% และ Deploy ขึ้น Production บน Vercel
- [x] **4.51 ระบบคลิกลิงก์ e-AR แล้วสลับเงื่อนไขเป็น "หมายเลขบาร์โค้ด" และค้นหาอัตโนมัติ (Automated e-AR Condition Switching & Barcode Search Engine) (`v2026.0917.1630` / Extension `v1.2.0`)**
  - พัฒนาฟังก์ชันการทำงานเชื่อมโยงระหว่าง C2DPost Web และระบบ e-AR (`https://e-ar.thailandpost.com/ear`) ตามภาพตัวอย่างของผู้ใช้งาน:
    1. เมื่อคลิกลิงก์ **"ดูภาพลายเซ็นใน e-AR ↗"** ในหน้าประวัติสถานะพัสดุ (`TrackingTimelineModal`) หรือหน้าสืบค้นพัสดุ (`TrackingInquiryView`):
       - ระบบจะคัดลอกหมายเลขบาร์โค้ด (เช่น `BC414111081TH`) ลงใน Clipboard อัตโนมัติ เพื่อความสะดวกสูงสุด
       - ส่งสัญญาณ `C2DPOST_SET_EAR_SEARCH` พร้อมหมายเลขพัสดุไปยังส่วนขยาย C2DPost Helper บันทึกลงใน `chrome.storage.local`
       - เปิดหน้าเว็บ `https://e-ar.thailandpost.com/ear#barcode=...` ในแท็บใหม่อัตโนมัติ
    2. ส่วนขยายเบราว์เซอร์ **C2DPost Helper (`v1.2.0`)**:
       - สคริปต์ `external_autofill.js` ที่ทำงานบน `e-ar.thailandpost.com` จะตรวจจับหมายเลขพัสดุที่ส่งมาจาก C2DPost Web (ทั้งผ่าน URL Hash และ Extension Storage)
       - ตรวจจับคอมโพเนนต์ Joy UI Select ของเมนู "เงื่อนไขการค้นหา" (`#select-field-demo-button`) และสั่งเปิดเมนูเลือก `li[role="option"][data-value="barcode"]` ("หมายเลขบาร์โค้ด") อัตโนมัติ
       - รอรับการ Render ช่องกรอก `<input name="barcode">` แล้วกรอกหมายเลขพัสดุลงในช่องด้วย Native Property Descriptor Setter
       - สั่งกดปุ่ม `button[type="submit"]` ("ค้นหา") ให้โดยอัตโนมัติ แสดงผลแถวข้อมูลรายการพัสดุและป้าย "มีลายเซ็น" ทันทีโดยที่ผู้ใช้ไม่ต้องกดค้นหาเอง
       - แสดงป้ายแจ้งเตือนสีเขียวมรกตลอยมุมจอ: `⚡ C2DPost Helper • ค้นหาพัสดุสำเร็จ`
    3. อัปเกรดไฟล์แพ็กเกจส่วนขยาย `c2dpost-extension.zip` และ `C2DPost_Helper_v1.2.0_WebStore.zip` สู่เวอร์ชัน 1.2.0 พร้อมใช้งาน
- [x] **4.52 ดึงข้อมูลฟิลด์ signature จาก getOrderByBarcode มาแสดงที่ "ชื่อผู้รับจริง" (Real Signer & Signature Field Integration) (`v2026.0917.1645`)**
  - จากคู่มือ API `API Standard preload Data on e-Parcel.pdf` ระบุว่าฟิลด์ `signature` คือ `ชื่อ-นามสกุล ผู้รับสินค้า`
  - ปรับปรุง Backend `api/index.py` ในฟังก์ชัน `get_tracking`:
    - ทำการเรียก API `getOrderByBarcode?barcode={barcode}` เพิ่มเติมควบคู่กับ `getHistoryStatus` เพื่อดึงข้อมูล `signature` และ `customerName` จากระบบ e-Parcel อย่างเป็นทางการ
    - นำค่า `signature` ที่ได้ใส่ลงใน Event ขั้นตอน "นำจ่ายสำเร็จ" (`delivered`) และส่งคืนใน Response
  - ปรับปรุง UI ทั้งในโมดอล `TrackingTimelineModal.jsx` และหน้าสืบค้น `TrackingInquiryView.jsx`:
    - เปลี่ยนป้ายกำกับจาก `ผู้เซ็นรับจริง:` เป็น **`ชื่อผู้รับจริง:`** ตามความต้องการของผู้ใช้และคู่มือ ปณท
    - หากในระบบมีค่า `signature` (เช่นชื่อผู้รับที่พิมพ์ไว้) จะแสดงผลทันที: `✍️ ชื่อผู้รับจริง: [signature]`
    - หากระบบ e-Parcel ส่งค่า `signature` เป็นค่าว่าง `""` จะแสดงคำแนะนำพร้อมปุ่มลิงก์เปิดสืบค้นภาพลายเซ็นใน e-AR ให้โดยอัตโนมัติ
  - คอมไพล์โปรเจกต์ `npm run build` และ Deploy ขึ้นสู่ Vercel Production สำเร็จ 100%

- [x] **4.53 สกัดภาพลายเซ็นผู้รับจริง (Thumbnail), ความสัมพันธ์ และปุ่มเปิดดูใบตอบรับ e-AR ฉบับจริง (e-AR Signature & Receipt Integration) (`v2026.0917.2030`)**
  - จากคู่มือทางการของไปรษณีย์ไทย `คู่มือ API e-AR/คู่มือ API เรียกดูใบตอบรับ e-ar.pdf` พบ API ดึงเอกสารใบตอบรับอิเล็กทรอนิกส์ `POST https://e-ar.thailandpost.com/ear-api/print/e-ar` โดยส่ง JSON Body เป็น Array ของบาร์โค้ด
  - พัฒนาระบบดึงข้อมูล e-AR บน Backend (`api/index.py`):
    - ฟังก์ชัน `_fetch_ear_details(barcode)`: เรียกขอไฟล์ PDF จาก e-AR API แล้วใช้เครื่องมืออ่าน PDF สกัดภาพลายเซ็น (หมุนภาพให้เป็นแนวนอนถูกต้องและแปลงเป็น Base64 Data URL) พร้อมทั้งสกัดข้อมูล **ความสัมพันธ์ของผู้รับ** (`relationship`) และ **เจ้าหน้าที่นำจ่าย** (`delivery_officer`) ออกมาโดยอัตโนมัติ
    - เพิ่ม Endpoint Proxy: `GET /api/reports/ear-pdf?barcode={barcode}` ให้ไคลเอนต์สามารถเปิดดูหรือพิมพ์เอกสารใบตอบรับ e-AR (PDF) ฉบับเต็มได้ทันทีในเบราว์เซอร์โดยไม่มีปัญหา CORS หรือข้อจำกัดการยิง POST จากเบราว์เซอร์
  - ออกแบบ UI คอมโพเนนต์ `TrackingTimelineModal.jsx` และ `TrackingInquiryView.jsx`:
    - กล่องหลักฐานการลงนาม (ระบบ e-AR): สไตล์ `.track-step-ear-box` สีเขียวมรกตดูพรีเมียม สบายตา
    - แสดงภาพลายเซ็นขนาดย่อ (Thumbnail): พื้นหลังขาว มีกรอบมน สวยงาม และสามารถคลิกที่ภาพเพื่อเปิดหน้าต่างป๊อปอัปขยายภาพลายเซ็นขนาดใหญ่ (Lightbox Zoom Modal) ได้
    - แสดงข้อมูลความสัมพันธ์ของผู้รับ: ป้ายกำกับมน `.ear-detail-badge` (เช่น `เจ้าหน้าที่เวรรับส่ง`, `ผู้รับรับเอง`, `คนในบ้าน`, `เจ้าหน้าที่ของรัฐ`)
    - แสดงชื่อเจ้าหน้าที่นำจ่ายของไปรษณีย์ไทย
    - ปุ่ม **`📄 ดูใบตอบรับ e-AR (PDF) ↗`**: คลิกเปิดดูไฟล์ใบตอบรับตัวจริงแบบ PDF ในแท็บใหม่ได้ทันที
  - คอมไพล์โปรเจกต์ `npm run build` สำเร็จ 100% และ Deploy ขึ้น Production บน Vercel

- [x] **4.55 เพิ่มปุ่ม "คืนค่าเริ่มต้น" (Reset to Default) ในหน้ารายงานสถานะไปรษณีย์ และจัดระเบียบโครงสร้างแพ็กเกจ Extension (`v2026.0917.2238`)**
  - **เพิ่มปุ่ม "คืนค่าเริ่มต้น" ในหน้ารายงานสถานะไปรษณีย์ (e-Parcel Status Report)**:
    - เพิ่มปุ่ม `.btn-reset-deposit` พร้อมไอคอน Reset ใน `DepositReportView.jsx` และ `DepositReportModal.jsx`
    - เมื่อผู้ใช้กดปุ่ม ระบบจะทำการ:
      1. คืนค่าช่วงวันที่ค้นหากลับสู่วันนี้ (`todayIso`)
      2. ล้างข้อความค้นหาในช่อง Search Box
      3. คืนค่าแท็บตัวกรองสถานะกลับสู่แท็บ "ทั้งหมด" (`all`)
      4. คืนค่าหน้า Pagination กลับสู่หน้าที่ 1
      5. เคลียร์แคชที่บันทึกไว้ใน `sessionStorage`
      6. สั่งดึงข้อมูลรายงานของวันปัจจุบันใหม่ทันทีโดยอัตโนมัติ
  - **จัดระเบียบและกำจัดความซ้ำซ้อนของแพ็กเกจ Extension (Clean Architecture)**:
    - ลบไฟล์ ZIP ที่หลุดออกมาวางซ้ำตรง Root ของโปรเจกต์ `C2DPost_web/`
    - ลบไฟล์ ZIP เวอร์ชันเก่า `v1.1.0` และ `v1.2.0` ออก คงเหลือเฉพาะ `v1.3.0` ล่าสุด
    - ปรับปรุงสคริปต์ `sync_extension_webstore.py` และ `.vercelignore` ป้องกันการสร้างโฟลเดอร์ซ้ำซ้อน



- [x] **4.56 ยกระดับ UX/UI หน้าต่างประวัติสถานะพัสดุ (Tracking Timeline Modal UX/UI Overhaul) (2026.0918.0600)**
  - ปรับปรุงโครงสร้าง Summary Card เป็น 2 ชั้น (Structured 2-Tier Layout) ตรึงป้ายสถานะไว้ที่มุมขวาบนเสมอ ไม่มีการตกแถว
  - เพิ่มปุ่มคลิกคัดลอกหมายเลขบาร์โค้ด [ 📋 คัดลอก ] พร้อม Interactive Feedback
  - แยกแถบแสดงชื่อผู้รับและเลขที่อ้างอิงไว้อีกแถวหนึ่งอย่างเป็นสัดส่วน
  - ตัดกล่องสถานะซ้ำซ้อนออก (เช่น 'นำจ่ายถึงผู้รับแล้ว' และ 'ส่งออกจากศูนย์...')
  - ตรวจจับข้อยกเว้นการนำจ่าย (Exception เช่น บ้านปิด, ออกใบแจ้ง) แสดงไอคอนเตือน ⚠️ สีส้มอำพันเด่นชัดใน Stepper และป้ายสถานะ
  - ขั้นตอนล่าสุดที่อยู่ระหว่างทางแสดงไอคอนรถขนส่ง 🚚 พร้อมเอฟเฟกต์ Pulsing Ring
  - ปรับการแสดงผลส่วน Footer เป็น Capsule Pill และเพิ่ม Custom Scrollbar สวยงาม

- [x] **4.57 ระบบดาวน์โหลดใบตอบรับ e-AR แบบกลุ่มสำหรับรายการนำจ่ายสำเร็จ (Batch e-AR Download) (v2026.0918.0620)**
  - รองรับการดาวน์โหลดใบตอบรับ e-AR พร้อมกันหลายรายการสำหรับพัสดุที่ "นำจ่ายสำเร็จ"
  - มีตัวเลือก 2 รูปแบบ: 📄 ไฟล์ PDF รวม 1 ฉบับ (Multi-Page Stitching ผ่าน PyMuPDF) หรือ 📦 ไฟล์ ZIP บีบอัด (แยกรายไฟล์ตามลำดับ, บาร์โค้ด และชื่อผู้รับ)
  - เชื่อมโยงสถาปัตยกรรม Hybrid Client-to-Backend ดึง PDF ผ่าน C2DPost Helper Extension (Domestic Thai IP) แก้ปัญหา Geoblocking 100%
  - เพิ่ม Checkbox เลือกรายรายการในตารางรายงานสถานะ พร้อมช่องเลือกทั้งหมด (Select All)
  - เพิ่มปุ่มดาวน์โหลด `[ 📥 โหลด e-AR (นำจ่ายสำเร็จ X) ▾ ]` พร้อมเมนู Dropdown ในหน้า `DepositReportView`, `DepositReportModal` และ `TrackingInquiryView`
  - มี Floating Toast แสดงความคืบหน้าการดาวน์โหลดแบบเรียลไทม์

- [x] **4.58 ปรับปรุงสถาปัตยกรรม e-AR PDF แบบ 3 รายการต่อหน้า A4 และระบุวันเวลาดาวน์โหลดที่ท้ายกระดาษ (v2026.0918.0720)**
  - จัดวางใบตอบรับ e-AR ให้แสดงผลได้สูงสุดถึง 3 รายการต่อหน้ากระดาษ A4 (3-up layout) พร้อมเส้นประบางสำหรับตัดแบ่ง
  - ประทับข้อความท้ายกระดาษ (Footer) ทุกหน้า ระบุวันที่และเวลาที่ดาวน์โหลดในรูปแบบ วันที่/เดือน/ปี พ.ศ. (เช่น `ดาวน์โหลดเมื่อ: DD/MM/YYYY HH:MM:SS น.`)
  - จัดระเบียบการซ้อนสระและวรรณยุกต์ภาษาไทย (ยกไม้เอกให้พ้นสระอิ/สระอี) ป้องกันปัญหาสระวรรณยุกต์จมหาย 100%

- [x] **4.59 ยุบเมนูดาวน์โหลด e-AR เป็นปุ่มเดี่ยวกดดาวน์โหลด PDF ทันที และแก้ไขข้อผิดพลาด ReferenceError (v2026.0918.0745)**
  - รวมปุ่ม e-AR ให้กดดาวน์โหลดเป็นไฟล์ PDF ทันทีโดยไม่ต้องเลือกรูปแบบในดรอปดาวน์
  - แก้ไขข้อผิดพลาด `ReferenceError: setShowEarDropdown is not defined` ที่เกิดจากการค้างเรียกใช้ฟังก์ชันหลังลบดรอปดาวน์
  - ปรับปรุงการตรวจสอบสิทธิ์ Extension ให้เป็นแบบ Soft/Non-blocking เพื่อให้ระบบสามารถทดลองดาวน์โหลดตรงผ่านเบราว์เซอร์ (Direct Fetch Fallback) ได้ทันที ทำให้กดดาวน์โหลดได้สำเร็จราบรื่น

- [x] **4.60 ปรับแต่ง Stepper แสดงเครื่องหมายถูกเฉพาะสถานะล่าสุด และเลื่อนอัตโนมัติมายังสถานะล่าสุดเมื่อเปิดหน้าต่าง (v2026.0918.0815)**
  - ปรับปรุงเงื่อนไขแสดงผลเครื่องหมายถูก (`✓`) ในหน้าต่าง `TrackingTimelineModal` และ `TrackingInquiryView` ให้แสดงผลเฉพาะสถานะล่าสุดของพัสดุ (`isLast`) เท่านั้น (เช่น นำจ่ายสำเร็จ หรือรับฝากสำเร็จในกรณีที่มีขั้นตอนเดียว) ส่วนขั้นตอนที่ผ่านมาในอดีตทั้งหมดจะแสดงหมายเลขลำดับเหตุการณ์ (`1`, `2`, `3`...) อย่างเป็นระเบียบชัดเจน
  - เพิ่มระบบ Auto-scroll อัจฉริยะในคอนเทนเนอร์ `.track-timeline-body` ให้เลื่อนลงมาแสดงสถานะล่าสุดที่อยู่ด้านล่างสุดโดยอัตโนมัติทันทีที่เปิดหน้าต่างหรือโหลดข้อมูลเสร็จสิ้น เพื่อให้ผู้ใช้งานเห็นสถานะล่าสุดและกล่องหลักฐาน e-AR ทันทีโดยไม่ต้องคอยเลื่อนหน้าจอเอง

- [x] **4.61 ผนวกรายงานไทม์ไลน์สถานะการนำส่งพัสดุ (Tracking Timeline) เป็นหน้าที่ 2 ในเอกสารใบตอบรับ e-AR (PDF) (v2026.0918.1200)**
  - เมื่อคลิกปุ่ม **`📄 ดูใบตอบรับ e-AR (PDF) ↗`** จากการ์ดพัสดุที่นำจ่ายสำเร็จ ทั้งในหน้า `TrackingInquiryView` และ `TrackingTimelineModal`:
    - **หน้าที่ 1 (Page 1)**: เอกสารใบตอบรับอิเล็กทรอนิกส์ต้นฉบับทางการจากไปรษณีย์ไทย (e-AR Certificate)
    - **หน้าที่ 2 (Page 2)**: บันทึกประวัติสถานะและการนำส่งพัสดุ (e-Parcel Tracking & Delivery Report) ตามข้อมูลจริงในรูปที่ 3 ซึ่งประกอบด้วย:
      1. กล่องสรุปภาพรวม (Summary Box): หมายเลขบาร์โค้ด, สถานะนำจ่ายสำเร็จ, วันเวลาที่นำส่ง, ที่ทำการปลายทาง, ผู้รับตามจ่าหน้า/เลขที่อ้างอิง, ผู้รับจริง/ความสัมพันธ์, จนท.นำจ่าย, จำนวนจุดเช็กพอยต์
      2. ตารางไทม์ไลน์ทุกขั้นตอนละเอียด (Stepper Table): ลำดับ (1, 2, ..., n ✓), วันและเวลา, สถานที่/ที่ทำการ, รายละเอียดขั้นตอน, ป้ายขั้นตอนพร้อมสีกำกับตามประเภทเหตุการณ์
      3. กล่องหลักฐานการลงนาม (ระบบ e-AR): ภาพลายมือชื่อผู้รับจริง (Signature Image), ความสัมพันธ์, ชื่อเจ้าหน้าที่นำจ่าย และวันเวลาลงนาม
      4. ประทับเลขหน้าภาษาไทยท้ายกระดาษอย่างเป็นทางการ (`หน้า 1 จาก 2`, `หน้า 2 จาก 2`) พร้อมระบุวันเวลาที่ดาวน์โหลด
  - เชื่อมต่อ API `POST /api/reports/ear-with-tracking-pdf` และ `GET /api/reports/ear-pdf` ควบรวมเอกสารผ่าน PyMuPDF (fitz) และ ReportLab วาดภาษาไทยด้วย Tahoma และฟังก์ชันตัดวรรณยุกต์ PUA 100% สวยงาม คมชัด พร้อมบันทึกหรือสั่งพิมพ์ได้ในเอกสารเดียว

- [x] **4.62 แก้บั๊กการจำแนกสถานะ "เตรียมนำจ่าย" ร่วมกับ "ถึงปลายทาง" ใน PDF (classify_step_for_pdf Fix) (`v2026.0918.2157`)**
  - แก้ไขโลจิก `classify_step_for_pdf` ใน `api/index.py` ที่จำแนกสถานะ `ถึงที่ทำการไปรษณีย์ปลายทาง (เตรียมการนำจ่าย)` ผิดเป็น "ถึง ปณ.ปลายทาง" เนื่องจากเงื่อนไข `"ถึง" not in desc` ไม่เคยเป็นจริงเพราะคำว่า "ถึง" ปรากฏใน "ถึงที่ทำการไปรษณีย์ปลายทาง"
  - ยกการตรวจ `เตรียมนำจ่าย` / `เตรียมการนำจ่าย` ขึ้นตรวจก่อนกลุ่ม "ถึงปลายทาง" และลบเงื่อนไขขัดแย้งทิ้ง ส่งผลให้สถานะการกระทำล่าสุดแสดงเป็น "เตรียมนำจ่าย" (สีส้ม) อย่างถูกต้อง ขณะที่ `ถึงที่ทำการไปรษณีย์ปลายทาง` ธรรมดายังคงจำแนกเป็น "ถึง ปณ.ปลายทาง" (สีม่วง) ตามเดิม

- [x] **4.63 แก้บั๊กหน้าเว็บขาว (Blank White Page) หลังเพิ่ม Signature Detection (`v2026.0919.1055`)**
  - สาเหตุ: ฟีเจอร์ลายเซ็น (Smart Badge / Signature) อ่านค่า `signature`/`status_description` แล้วเรียก `.trim()` ตรง ๆ หากค่าเหล่านี้ไม่ใช่ string (เช่น object/boolean จากแหล่งข้อมูลจริง) จะทำให้ `TypeError` ระหว่าง render → ทั้งหน้าเทา-ขาวโดยไม่มี Error Boundary ดักจับ
  - แก้ไข: (1) coerce ค่าทุกรายการด้วย `String(...)` ก่อน `.trim()` ใน `TrackingInquiryView.jsx`, `TrackingTimelineModal.jsx`, `DepositReportView.jsx`, `DepositReportModal.jsx`; (2) กัน `events` เป็น `Array.isArray` ก่อนเข้าสาย render ใน `TrackingInquiryView.jsx`; (3) เพิ่ม `<AppErrorBoundary />` ครอบทั้งแอปที่ `main.jsx` แสดงหน้าความผิดพลาดแบบมี UI แทนหน้าเปล่า
  - ผลลัพธ์: `npm run build` ผ่าน 100% ทุก view (gate / demo / admin) render ปกติ headless Chrome

- [x] **4.64 แก้บั๊ก `earInfo is not defined` — Timeline พัสดุที่นำจ่ายสำเร็จ crash (`v2026.0919.1142`)**
  - สาเหตุ: `TrackingTimelineModal.jsx` ใน `statusInfo` (useMemo) สาขา "นำจ่ายสำเร็จ" อ้าง `earInfo?.signature_image` แต่ตัวแปร `earInfo` ไม่เคยถูกประกาศใน component (มีแค่ `clientEarData`/`recInfo`/`trackData`) — `?.` กันไม่ได้กรณีชื่อตัวแปรไม่มีตัวตน → `ReferenceError` ทุกครั้งที่เปิด timeline ของพัสดุที่ส่งถึงแล้ว (Error Boundary ใหม่จับและแสดงข้อความนี้แทนหน้าขาว)
  - แก้ไข: ลบบรรทัด `earInfo?.signature_image ||` ทิ้ง (บรรทัดถัดไป `clientEarData?.signature_image` ครอบคลุมอยู่แล้ว); ตรวจแล้วจุด `earInfo` อื่น (`TrackingTimelineModal.jsx:589`, `TrackingInquiryView.jsx:974`) ประกาศ `const` ถูกต้อง และ `clientEarMap` (state `:246`) ปกติ
  - ผลลัพธ์: bundle ใหม่ไม่มี `earInfo?.signature_image` หลงเหลือ; deploy production ผ่าน health `v2026.0919.1142`


